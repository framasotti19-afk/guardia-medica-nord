# CONTEXT — App Turni Guardia Medica ASFO Distretto Nord

## LEGGERE PRIMA DI QUALSIASI INTERVENTO

Questo file contiene tutto il contesto necessario per lavorare sull'app senza ricominciare da capo.
**Leggi prima il file `turni-guardia-medica.jsx`, poi questo documento, prima di toccare qualsiasi cosa.**

### ⛔ FILE DI TEST E CARTELLA uploads — REGOLA PERMANENTE
- **L'unico file di test AI è `mail_test_prompt_ai.md` nella working directory (repo).** È l'UNICA fonte di verità.
- **Nessun file in `~/.claude/uploads/` esiste ai fini del lavoro.** Ogni copia lì (es. un vecchio `mail_test_prompt_ai.md`) è MORTA: non leggerla, non scriverla, non "riconciliarla", non sincronizzarla. Ignorala.
- Se serve un file che sta in `uploads`: **copialo nel repo UNA VOLTA SOLA**, poi lavora esclusivamente sulla copia del repo. Mai più scritture/letture in `uploads`. (Vedi anche `CLAUDE.md`.)

---

## 0. INVARIANTI INTOCCABILI DEL MOTORE

**Regole che NESSUNA modifica futura deve mai violare — rifattorizzazioni comprese.** Sono ancorate all'implementazione reale (funzioni e righe indicate); prima di toccarle rileggi il codice, non questa parafrasi. Se un intervento sembra richiedere di violarne una, FERMATI e chiedi conferma al coordinatore. La sezione MOTORE è tutto ciò che precede `// ============ COMPONENTE ============`: dopo ogni modifica al motore, `engine_test.mjs` va rigenerato e i suoi test devono restare verdi (vedi INV-11).

1. **Titolarità di sede.** Un contrattualizzato titolare di una sede (campo `sedeContratto`) non perde quella sede a favore di chi non ha un diritto superiore secondo la gerarchia. Enforcement: per il fisico via `correggiTitolarita` (due passate dentro `elaboraTurno`, §3.1a); per la copertura a distanza via il check titolarità dentro `isBetterPriority` (riga ~377). Limite noto documentato: le catene di ricollocazione profonde di FASE 1 con ≥3 contrattualizzati possono lasciare un residuo raro che `correggiTitolarita` non cattura — NON si tenta di correggerlo di più (introduce regressioni, §10), ma è SEGNALATO dal guardiano titolarità (§10 voce 33) e TOLLERATO-ma-riportato nella simulazione (INV-TITOLARE).

2. **Gerarchia decisionale (sequenza esatta).** Su una sede contesa, `isBetterPriority` (righe 371-384) decide in quest'ordine: **(a) bucket** — chi ha debito ordinario residuo (bucket 0) batte sempre chi è senza incarico o ha esaurito il monte ore ma ha turni extra residui (bucket 1); dentro il bucket 1 conta SOLO la graduatoria; **(b)** tra due contrattualizzati del bucket 0, **titolarità di QUELLA sede specifica** (chi è titolare lì batte chi non lo è, qualunque categoria); **(c) categoria/priorità contrattuale** (`CAT_INFO[cat].prio`: INDET 1, DET38 2, DET24=DET12ASAP 3, DET12 4, SENZA 5 — numero più basso vince); **(d) debito residuo** maggiore; **(e) graduatoria** (numero più basso). L'ordinamento generale dei candidati fisici (`candidatiOrdinati`, riga 338) usa `prio → debito → grad` SENZA titolarità: la titolarità è applicata a parte da `correggiTitolarita`. **DET24 e DET12ASAP condividono lo stesso `prio` (3)** e non sono in relazione gerarchica tra loro.

3. **Contrattualizzati vs senza incarico.** I senza incarico (`CAT_INFO[cat].ore === null`, `debiti` null) stanno sempre nel bucket 1 e non scavalcano mai un contrattualizzato che ha ancora debito ordinario residuo (bucket 0). Un contrattualizzato che ha esaurito monte ore + recupero **senza** turni extra dichiarati esce del tutto dai candidati (`candidatiOrdinati`, riga 333: `debiti<=0 && debitiExtra<=0` → escluso); se ha turni extra residui scende nel bucket 1 e compete alla pari coi senza incarico, solo per graduatoria.

4. **Debito orario.** Il debito residuo (`debiti[mid]`, inizializzato da `debitoOrdinarioIniziale` = monte ore della categoria aggiustato per mese §3.11 p.3 + eventuale recupero dichiarato) si consuma coerentemente e non permette assegnazioni oltre il monte ore, SALVO i turni extra volontari dichiarati (`debitiExtra`, 12h ciascuno) e comunque mai oltre il `maxTurniMese`/`capSettimanale` dichiarati. Debito = 0 senza extra → il medico esce dalla competizione (può solo coprire turni altrimenti scoperti, secondo le regole).

5. **Copertura a distanza — vincolo territoriale (formulazione esatta dal codice).** In `risolviBlu`, la funzione `puoCoprireADistanza(mid, si)` (righe 364-368) è un FILTRO applicato PRIMA della gerarchia: **Claut** (indice sede 3) è coperibile a distanza SOLO da chi è fisicamente a **Maniago** (`sedeFisicaDi[mid] === 0`) — unica via; **Anduins** (indice 4) SOLO da chi è fisicamente a **Spilimbergo** (`=== 1`) **oppure Meduno** (`=== 2`) — due vie; le altre sedi (Maniago/Spilimbergo/Meduno a distanza) non hanno vincolo (`return true`). Il vincolo è sulla SEDE FISICA effettiva del coprente (`sedeFisicaDi`), non sulla titolarità. Resta sempre necessaria la dichiarazione blu esplicita e vale il massimo di 1 sede a distanza per medico. Verificato in simulazione da `INV-TERRITORIALE`.

6. **Preferenze verde/blu.** Le sedi dichiarate (verde = fisica, blu = a distanza) e i loro livelli guidano SOLO l'eleggibilità e l'ordine con cui una sede viene assegnata a un vincitore (`ordinaPerLivello`, livelli pari = sedi indifferenti tra cui il motore può spostare il medico); non cambiano MAI chi vince un conflitto. Un medico non viene mai forzato su una sede che non ha dichiarato verde (INV1) né coperto a distanza su una sede non dichiarata blu (INV3).

7. **Distribuzione temporale (§3.11).** La distribuzione mensile "run-then-redistribute": chi vincerebbe più turni del proprio tetto (il più restrittivo tra monte-ore-implicito e `maxTurniMese` dichiarato) cede l'eccesso al candidato successivo in gerarchia, tenendo il sottoinsieme più equidistanziato nel tempo — ma con la **priorità di sede ASSOLUTA sull'equidistanza** (§3.11): il tetto è rigido, la copertura non prevale mai su di esso, e la qualità della distribuzione non deve mai sacrificare indebitamente le priorità di sede. Il **pool** da cui si sceglie è ricalcolato su TUTTO il mese (oracolo che esenta solo quel medico dal blocco monte ore §3.4) OGNI VOLTA che la disponibilità supera il tetto — non solo quando un `maxTurniMese` esplicito è più restrittivo del monte ore, ma anche nel caso comune "disponibile tutto il mese + tetto = monte ore" (§10 voce 48). Il ricalcolo è correttezza-neutra e scatta solo per chi ha esaurito il monte ore in P1 (gate perf). (La qualità della distribuzione è coperta da unit test deterministici, non dagli invarianti della sim densa — §10 voce 20.)

8. **Claut e Anduins solo diurne.** `const sediFisiche = turno.id === "G" ? [0,1,2,3,4] : [0,1,2]` (riga ~491): Claut (3) e Anduins (4) sono sedi FISICHE solo nel turno diurno `"G"` (che esiste unicamente in weekend/festivi/prefestivi); in ogni altro turno le sedi fisiche sono solo Maniago/Spilimbergo/Meduno e Claut/Anduins esistono unicamente come copertura a distanza (blu). La FASE 2 deriva `sitiCoperti` dalle sedi effettivamente fisiche, quindi la coerenza è automatica.

9. **Un cambiamento al motore alla volta.** Ogni modifica al motore è isolata, testata e documentata (una voce nel §10): non si sommano nello stesso intervento logica di assegnazione, UI e refactoring. Le modifiche di sola UI o di solo prompt AI NON toccano la sezione MOTORE e devono lasciare `engine_test.mjs` **byte-identico a HEAD** (verifica esplicita richiesta).

10. **Nessuna rifattorizzazione funzionale implicita.** Spostare codice, rinominare funzioni o separare moduli non deve MAI cambiare il comportamento del motore: un refactoring si considera valido solo se produce output identico (layout fisici e invarianti identici sul motore di HEAD) — vedi il precedente storico di `risolviBlu` (§10 voce 22), estratto a comportamento identico e verificato tale.

11. **Test e simulazioni come fonte di verità.** Ogni modifica rilevante mantiene verdi i **206 unit test** (14 file, vedi §8) e non introduce violazioni nella simulazione (`test_simulazione_completa.mjs`, 100.000 scenari). Invarianti rigidi della sim: INV1 (fisico solo su verde), INV2 (NO mai assegnato), INV3 (a distanza solo da un fisico presente su sede blu dichiarata), max-1-blu, `INV-TITOLARE` (con tolleranza del limite noto §3.1a: ≥3 contrattualizzati → tollerato-ma-riportato), `INV-MAXTURNI`, `INV-TETTO-IMPLICITO`, `INV-FISICO-UNICO`, `INV-DETERMINISMO`, `INV-TETTO-SETTIMANALE`, `INV-TERRITORIALE`. Il motore va toccato solo se, dopo, tutto resta verde (o la nuova tolleranza è esplicitamente giustificata e documentata).

**Nota — espansione degli insiemi di giorni deterministica nel MOTORE (luglio 2026).** Il calcolo di QUALI `{giorno, turno}` compongono un insieme dichiarato in modo compatto ("notturni feriali", "weekend", "tutto il mese", "dal X al Y", **e i giorni della settimana nominati** "il lunedì" / "da martedì a giovedì") è DETERMINISTICO e vive nel motore (`espandiAmbito`, funzione pura sul calendario, subito dopo `turniDelGiorno`), **non** è più a carico del ragionamento dell'AI — che calcolando i giorni "a mente" (Zeller) occasionalmente ne saltava uno. `espandiAmbito`, `diurniNascosti` e `costruisciEntryDispo` sono aggiunte PURE al motore: **non chiamano né alterano `elaboraSchema`/`elaboraTurno`**, quindi gli invarianti 1-11 e la simulazione restano immutati (verificato: sim 100k identica nei conteggi). L'azione AI `dispo_set` (COMPONENTE) si limita a invocarle. Vedi §10 voci 41 e 43.

---

## 1. CONTESTO DEL PROGETTO

App React single-file (`turni-guardia-medica.jsx`) per la gestione mensile dei turni di guardia medica del Distretto Nord ASFO (Azienda Sanitaria Friuli Occidentale). L'utente è il futuro coordinatore.

**Sedi del distretto:** Maniago (MA), Spilimbergo (SP), Meduno (ME), Claut (CL), Anduins (AN). Maniago e Spilimbergo sono le 2 "CDC" (Centri Di Coordinamento), sempre prioritarie.

L'app:
- Permette di inserire disponibilità mensili per ciascun medico: per ogni sede, un menu a tendina con **Non disponibile / Verde 1-5 (sede fisica) / Blu 1-4 (copertura a distanza)**, indisponibilità NO esplicita
- Applica le regole di assegnazione turni (gerarchia, titolarità di sede, debito orario, graduatoria) per produrre uno schema
- Permette correzioni manuali post-elaborazione
- Esporta lo schema in Excel (.xlsx) fedele al formato reale ASFO
- Ha un assistente AI integrato (chiama `https://api.anthropic.com/v1/messages` con claude-sonnet-4-6)
- **NON usa React Router, NON usa librerie esterne** (solo React + useState/useMemo/useRef/useEffect). L'xlsx viene costruito a mano come ZIP binario.
- Anche pubblicata su GitHub Pages (`docs/`), come copia adattata senza bundler — vedi §14.

---

## 2. STRUTTURA DEL FILE (~2500 righe)

```
righe 1-113     → DATI SIMULAZIONE (MEDICI_DEFAULT con sedeContratto, byId, CAT_INFO, SEDI5, CDC, calendari)
righe 114-182   → MOTORE: normDispo, ordinaPerLivello, MAX_LIV_VERDE/BLU, giorniTra, settimanaDi, capSettimanale
righe 184-217   → MOTORE: turnoPrefDi, candidatiOrdinati (preferenza turno §3.9 + turni extra §3.10 + estrazione candidati condivisa)
righe 219-427   → MOTORE: elaboraTurno (cuore dell'algoritmo: fisica + a distanza + tetto settimanale + turni extra §3.10)
righe 429-563   → MOTORE: elaboraSchema (orchestrazione mese, ordine cronologico, poi preferenza turno §3.9)
righe 564-580   → MOTORE: sedePrimaria, notaSlot (helper post-elaborazione)
righe 581-... (nota: numeri di riga oltre questo punto non aggiornati a ogni modifica UI — usa grep per i marker esatti) → COMPONENTE REACT (state, event handlers, EXPORT XLSX, UI, AI, render)
```

**La sezione motore è pura JavaScript** (niente React hooks) — può essere estratta e testata con Node.js:
```bash
# Estrai il motore e crea engine_test.mjs per i test
python3 -c "
src = open('turni-guardia-medica.jsx').read()
end = src.index('// ============ COMPONENTE ============')
engine = src[:end].replace('import { useState, useMemo, useRef, useEffect } from \"react\";\n', '')
open('engine_test.mjs', 'w').write(engine + '\nexport { MEDICI, MEDICI_DEFAULT, setMediciGlobal, byId, CAT_INFO, SEDI5, SEDI_BREVI, CDC, dk, mk, turniDelGiorno, espandiAmbito, costruisciEntryDispo, elaboraSchema, normDispo, ordinaPerLivello, MAX_LIV_VERDE, MAX_LIV_BLU, isDeterminato, isContrattualizzato, MESI_DISPONIBILI, MESI_IT, giorniTra, settimanaDi, capSettimanale, debitoOrdinarioIniziale, tettoDistribuzioneDi };\n')
print('motore estratto')
"
```

---

## 3. REGOLE DI BUSINESS (IMMUTABILI — NON INTERPRETARE, NON SEMPLIFICARE)

### 3.1 Gerarchia categorie

| Priorità (prio) | Categoria | Debito mensile | Spareggio interno |
|----------|-----------|----------------|-------------------|
| 1° | INDET — Indeterminato (qualunque orario) | 96h | debito ↓ → graduatoria |
| 2° | DET38 — Determinato 38h/sett | 168h | titolarità sede → debito ↓ → graduatoria |
| 3° | DET24 — Determinato 24h/sett | 104h | titolarità sede → debito ↓ → graduatoria |
| 3° | DET12ASAP — Determinato 12h/sett ASAP | 52h | titolarità sede → debito ↓ → graduatoria |
| 4° | DET12 — Determinato 12h/sett | 52h | titolarità sede → debito ↓ → graduatoria |
| 5° | SENZA — Senza incarico | null (nessun debito) | solo graduatoria |

**INDET** sostituisce le vecchie categorie IND36/IND24 (fuse in un'unica categoria a priorità massima con un unico monte ore mensile di 96h, indipendentemente dall'orario contrattuale settimanale).

**DET24 e DET12ASAP condividono lo STESSO livello di priorità (prio 3, in `CAT_INFO`).** Non sono in relazione gerarchica tra loro — nessuna delle due batte l'altra per categoria. Uno spareggio diretto tra un DET24 e un DET12ASAP si risolve esattamente come tra due medici della stessa categoria: titolarità sede → debito residuo → graduatoria. Il monte ore mensile resta comunque diverso (104h contro 52h), perché ciascuno matura debito secondo il proprio contratto — la parità riguarda solo la priorità di categoria nel confronto, non le ore.

**DET12** è l'unica categoria "determinata" priva di priorità speciale: perde sempre contro INDET, DET38, DET24 e DET12ASAP, e batte solo i medici senza incarico. Monte ore mensile 52h (12h/sett × 52 settimane ÷ 12 mesi), come DET12ASAP.

**Regola del debito:** chi ha più debito residuo vince; a parità vince chi ha il numero di graduatoria più basso (= posizione migliore). Questo si applica SOLO all'interno dello stesso prio (quindi anche tra DET24 e DET12ASAP, che condividono prio 3).

**La categoria prevale SEMPRE finché il medico ha debito > 0.** Un INDET con un'ora di debito batte qualsiasi DET38, e così via lungo tutta la gerarchia (eccetto tra DET24 e DET12ASAP, che sono a pari livello).

**Debito esaurito (= 0 o negativo):** il medico esce dalla priorità di categoria. L'ordine di precedenza diventa:
1. contrattualizzati con debito > 0 (ordinati per cat → titolarità → debito → grad)
2. senza incarico, oppure contrattualizzati esauriti con turni extra volontari residui (§3.10) — competono insieme, solo per grad
3. **blocco rigido**: un contrattualizzato che ha esaurito sia il debito ordinario (monte ore + recupero) sia gli eventuali turni extra volontari **non è più un candidato per nessun turno**, nemmeno se resterebbe l'unico disponibile — il turno resta SCOPERTO piuttosto che essere coperto oltre il limite dichiarato. Le sue ore assegnate nel mese non possono mai superare monte ore + recupero + turni extra dichiarati.

*(Storico: prima di questa regola un esaurito senza turni extra poteva ancora coprire un turno se restava l'unico candidato disponibile, per non lasciarlo scoperto — comportamento cambiato dopo un bug segnalato in cui un medico finiva con ore assegnate ben oltre il proprio monte ore. Vedi `candidatiOrdinati` in §5: il filtro iniziale esclude ora esplicitamente questi medici da "candidati", non solo dall'ordinamento di priorità.)*

**Recupero ore da mese precedente:** dichiarato esplicitamente al coordinatore. Aumenta il debito mensile: `debito = monte_ore + ore_recupero`. Partecipa normalmente a tutti i conflitti. NON applicabile ai senza incarico (che non hanno debito).

### 3.1a Titolarità di sede (OBBLIGATORIA e universale per ogni contrattualizzato)

Ogni medico **contrattualizzato** (INDET, DET38, DET24, DET12ASAP o DET12 — `isContrattualizzato(mid)`, ha un monte ore) ha **SEMPRE** un campo `sedeContratto`: `"Maniago"` o `"Spilimbergo"` — mai `null` per un contrattualizzato. Solo i **senza incarico** (SENZA) non hanno titolarità (`sedeContratto` sempre `null`, campo disabilitato/"—" in UI). `isDeterminato(mid)` resta una funzione distinta (solo le 4 categorie DET*, usata altrove per altri scopi) — `isContrattualizzato` è la funzione rilevante per la titolarità, e include anche INDET.

**Tra QUALSIASI coppia di contrattualizzati** (INDET incluso, non solo tra determinati) in conflitto sulla sede di cui uno dei due è titolare, il titolare vince **sempre** quella sede — anche contro una categoria nominalmente superiore (es. un DET24 titolare di Maniago batte un INDET non titolare, su Maniago — pur essendo INDET la categoria più alta in assoluto). Questa regola vale **identica sia per l'assegnazione FISICA sia per la copertura A DISTANZA (blu)** — non ci sono due ordini diversi:

```
titolarità sede (per la sede contesa, tra tutti i contrattualizzati) → categoria → debito → graduatoria
```

**Tra due titolari della STESSA sede** (o due non titolari di essa), la titolarità è a parità e decide normalmente categoria → debito → graduatoria — esattamente come se nessuno dei due fosse titolare. La titolarità **non ha mai effetto** se uno dei due contendenti è un senza incarico (mai contrattualizzato, la titolarità è gated su entrambi i lati) e non ha effetto se il contendente è titolare di una sede **diversa** da quella contesa.

**Validazione UI**: nel tab "3 · Medici", la colonna "Titolarità" mostra il selettore (solo Maniago/Spilimbergo, senza opzione "Nessuna") per ogni contrattualizzato; passando un medico a categoria SENZA la titolarità si azzera automaticamente a `null`, passando da SENZA a un contrattualizzato senza titolarità già impostata si applica un default (`"Maniago"`) che il coordinatore corregge dalla stessa colonna. Il form "Aggiungi nuovo medico" include lo stesso selettore (nascosto per SENZA). I dati di default (§4) hanno tutti una titolarità reale assegnata (nessun contrattualizzato con `sedeContratto: null`), coerentemente con l'obbligatorietà.

**Bug storico risolto — 3+ determinati su sedi sovrapposte:** in scenari con 3 o più determinati che si contendono più sedi sovrapposte nello stesso turno, catene di ricollocazione ricorsiva profonde in `provaFisica` potevano raramente convergere a un equilibrio in cui un titolare finiva fisicamente altrove pur avendo diritto alla propria sede — senza che nessun singolo passaggio della catena fosse, isolatamente, scorretto. Un primo tentativo di correzione generico (ripetere l'intero giro di `provaFisica` più volte per far convergere lo stato) è stato scartato perché introduceva regressioni reali (perdita di copertura) in scenari altrove già corretti. Risolto invece con `correggiTitolarita()` — una passata MIRATA solo ai titolari (mai a candidati generici), eseguita sia dopo FASE1 sia dopo la spaziatura temporale (§3.7, che può essa stessa reintrodurre il problema): per ogni titolare non ancora sulla propria sede, se l'occupante attuale non ha davvero priorità superiore (`isBetterPriority`), lo scambia dentro e ridà all'occupante spostato una possibilità di ricollocarsi tramite lo stesso `provaFisica` già usato ovunque. Verificato a 0 violazioni su 100.000 scenari / 25.151.195 check (§10). **Residuo raro non corretto → GUARDIANO (§10 voce 33):** `correggiTitolarita` risolve la stragrande maggioranza dei casi ma un residuo rarissimo può sfuggire (correggerlo di più reintroduce regressioni, quindi si accetta). A valle di TUTTE le correzioni un guardiano puramente additivo lo rileva e genera un **avviso** per il coordinatore (nessuna correzione automatica, nessuna assegnazione cambiata), così quel singolo turno viene sistemato a mano.

### 3.2 Sedi e scenari di copertura — sistema dichiarativo verde/blu

**REGOLA GENERALE (fondamentale): nessuna copertura è automatica.** Tutto dipende da quello che i medici dichiarano. Un medico copre al massimo **1 sola sede a distanza** — se vuole poterne coprire di più deve dichiararle esplicitamente come blu (con livelli di preferenza).

**Maniago e Spilimbergo (le 2 CDC) sono sempre le prime sedi fisiche puntate.**

**Sedi fisiche in base al turno (§10 voce 30):** Maniago, Spilimbergo, Meduno sono sedi fisiche **SEMPRE**. **Claut e Anduins** sono sedi fisiche **SOLO nel turno DIURNO** (id `"G"`, 12h piene) — che nel calendario esiste unicamente nei giorni ad alta domanda (weekend, festivi, prefestivi); nel **notturno** (sempre) e in ogni altro turno Claut e Anduins sono coperte **solo a distanza** (blu). La regola è agganciata all'esistenza del turno diurno (`turno.id === "G"`), non al giorno della settimana. Il target fisico è quindi il prefisso di `[Maniago, Spilimbergo, Meduno, (Claut, Anduins solo se diurno)]`, troncato al numero di medici presenti.

| N. medici | Target fisico — NOTTURNO (e non-diurno) | Target fisico — DIURNO (G) |
|---|---|---|
| 1 | miglior verde tra {Maniago, Spilimbergo, Meduno} | miglior verde tra tutte e 5 le sedi |
| 2 | Maniago + Spilimbergo | Maniago + Spilimbergo |
| 3 | Maniago + Spilimbergo + Meduno | Maniago + Spilimbergo + Meduno |
| 4 | Maniago + Spilimbergo + Meduno (**Claut/Anduins a distanza**) | + **una tra Claut/Anduins secondo la dichiarazione del 4° medico** (§10 voce 32): solo Claut→Claut, solo Anduins→Anduins, indifferente→tappa il buco a distanza / else Claut |
| 5+ | idem (max 3 fisiche; oltre il 3° medico → inutilizzato) | + Claut + Anduins (max 5; oltre il 5° → inutilizzato) |

In tutti i casi la copertura a distanza segue la stessa logica: ogni fisico copre al più 1 sede blu dichiarata, conflitto sulla stessa sede blu → titolarità sede → categoria → debito → graduatoria, sedi senza blu → SCOPERTE. Nel diurno, quando Claut/Anduins diventano fisiche, escono automaticamente dalla copertura a distanza (`sitiCoperti` deriva dalle sole sedi effettivamente fisiche → niente doppione).

**Vincolo TERRITORIALE sulla copertura a distanza di Claut e Anduins (§10 voce 31):** vincolo geografico reale, non ottimizzabile —
- **Claut** può essere coperta a distanza **SOLO dal medico fisicamente a Maniago** (unica via: Val Cellina, si raggiunge da Maniago).
- **Anduins** può essere coperta a distanza **SOLO dal medico fisicamente a Spilimbergo o Meduno** (due vie); se entrambi l'hanno dichiarata blu, si sceglie con la solita gerarchia (titolarità → categoria → debito → graduatoria).
- Le altre sedi a distanza (Maniago/Spilimbergo/Meduno) **non hanno** vincolo geografico.

È un FILTRO applicato **PRIMA** della gerarchia in `risolviBlu`/`provaBlu` (`puoCoprireADistanza(mid, si)`): chi non è nella sede-base ammessa non si candida nemmeno a coprire quella sede; la scelta tra i candidati validi resta governata da `isBetterPriority` (invariata). NON è una copertura automatica: resta sempre necessaria la dichiarazione blu — se il fisico di Maniago non dichiara Claut, **Claut resta SCOPERTA** (mai coperta da altrove). Verificato a scala dall'invariante **INV-TERRITORIALE** (§8).

Con **1 solo medico**, il target fisico non è forzato su Maniago: il medico va fisicamente dove porta la sua migliore preferenza verde, **tra le sedi oggi fisiche** (di notte solo Maniago/Spilimbergo/Meduno; nel diurno anche Claut/Anduins). Una dichiarazione *verde* su Claut/Anduins in un turno notturno è inutilizzabile (lì non sono fisiche): per coprirle di notte servono dichiarazioni *blu* (a distanza).

**Non esistono regole geografiche AUTOMATICHE** (nessun fallback forzato "Claut coperta d'ufficio da Maniago" come nel vecchio sistema piene/ripiego): la copertura a distanza resta SEMPRE dichiarativa (blu). Il vincolo territoriale sopra **restringe** solo CHI può coprire Claut/Anduins (filtro geografico prima della gerarchia), ma non copre mai nulla d'ufficio — è cosa diversa dal vecchio automatismo.

### 3.3 Disponibilità — formato dati (sistema verde/blu)

```javascript
dispo[mid][slotKey] = {
  verde: ["Maniago", "Spilimbergo"],     // sedi FISICHE desiderate, in ordine di preferenza
  verdeLiv: { Maniago: 1, Spilimbergo: 2 }, // livello 1..5 per ogni sede verde
  blu: ["Meduno"],                        // sedi che è disposto a COPRIRE A DISTANZA
  bluLiv: { Meduno: 1 },                  // livello 1..4 per ogni sede blu
  no: false,                              // NO esplicito (protegge dall'inserimento rapido)
}
```

**Verde (sede fisica):**
- Livelli 1..5. Livelli PARI tra più sedi rendono il medico "indifferente" ai fini della ricollocazione: il motore può spostarlo tra quelle sedi per massimizzare le coperture.
- **La parità NON rende però due sedi davvero equivalenti tra loro.** L'ordine in cui il motore le prova a parità di livello segue sempre l'ordine fisso `SEDI5` — Maniago → Spilimbergo → Meduno → Claut → Anduins — **mai** l'ordine in cui il medico le ha dichiarate. Una CDC (Maniago/Spilimbergo) pari con una sede secondaria vince quindi sempre la CDC, esattamente come se fosse un livello migliore: un medico che dichiara Maniago livello 1 e Meduno livello 1 va a Maniago, con lo stesso risultato di Maniago livello 1 + Meduno livello 2 (vedi `ordinaPerLivello` in §5).
- Livello più basso = sede che il medico ha diritto di tenere contro chiunque non lo superi in gerarchia (titolarità → categoria → debito → graduatoria tra determinati; categoria → debito → graduatoria altrimenti).
- **I livelli non cambiano MAI chi vince un conflitto.** Cambiano solo quale sede viene assegnata a ciascun vincitore, massimizzando il numero di medici al lavoro.
- Non esiste più una distinzione piena/ripiego a due livelli: verde è un'unica lista di preferenze fisiche 1-5.

**Blu (copertura a distanza):**
- Livelli 1..4 (non 5: al massimo 4 "altre" sedi da poter coprire oltre alla propria).
- Stessa regola di parità del verde: a livello blu pari tra più sedi, l'ordine di prova segue sempre `SEDI5` (Maniago → Spilimbergo → Meduno → Claut → Anduins), non l'ordine di dichiarazione — stesso helper `ordinaPerLivello` condiviso con il verde.
- Il medico deve essere **fisicamente presente** (verde) da qualche parte nello stesso turno per poter coprire una sede a distanza — un medico che dichiara solo blu (senza alcuna sede verde raggiungibile) non copre mai nulla.
- Copre al massimo **1 sola sede a distanza**, la prima disponibile nel suo ordine blu dichiarato. Se scalzato dalla sua prima scelta blu (da un medico con priorità superiore), riprova con la successiva.
- Conflitto sulla stessa sede blu tra più medici fisici: **titolarità sede → categoria → debito → graduatoria** — stessa identica gerarchia usata per l'assegnazione fisica (vedi §3.1a).
- Il blu non scalza mai una presenza fisica: può competere solo per sedi non fisicamente coperte.

**Nessuna retrocompatibilità con il vecchio formato piene/ripiego:** il salvataggio dati esistente basato su `piene`/`ripiego` non viene automaticamente convertito — è un cambio di formato deliberato (§6, punto "sistema di disponibilità"), le disponibilità già inserite vanno reinserite con il nuovo menu a tendina.

### 3.4 Meccanismo auto-bilanciante del debito

Turni elaborati in ordine cronologico (di calendario): è l'ordine in cui il debito viene consumato progressivamente, e quindi influenza i risultati dei conflitti successivi.

A parità di debito e categoria, vince la graduatoria migliore (numero più basso).
Dopo ogni assegnazione il debito del vincitore scende. Al turno successivo a parità, l'altro medico ha più debito → vince lui. **L'equità emerge automaticamente**, con vantaggio strutturale per chi ha graduatoria migliore (vince i tie-break).

Esempio con 5 turni a parità di debito iniziale:
- A(grad3) vs B(grad124): A 1°, B 2°, A 3°, B 4°, A 5° → risultato 3-2 per A

### 3.5 Preferiti — sede specifica (RIMOSSA, §10 voce 62)

La funzione ☆ "sede preferita" (campo `preferito` nell'entry dispo) è stata **completamente rimossa**: né UI, né motore, né prompt, né avvisi. Con essa è sparito anche l'ordine di elaborazione `conPref`/`resto` (i turni con un preferito venivano elaborati per primi): ora l'elaborazione è **puramente cronologica** (§3.4). Nota: la preferenza di **TURNO** diurno/notturno (`TURNOPREF`, §3.9) è cosa diversa e resta invariata.
- **Non esiste più un preferito "anche in ripiego a distanza"** (il vecchio `preferitoRip`): era ridondante col blu, dato che coprire a distanza richiede comunque una presenza fisica altrove, e comunque il preferito ora è già specifico sulla sede fisica desiderata.

### 3.6 Calendario mensile e fasi

- **Giorno 27**: invio mail richiesta disponibilità ai medici
- **Entro giorno 3** (23:59): scadenza disponibilità → regole ordinarie
- **Giorno 10**: invio primo schema
- **Entro giorno 14** (23:59): modifiche → first come, first served (niente gerarchia)
- **Giorno 15**: invio schema definitivo all'azienda

**First come, first served** si applica solo alle richieste arrivate dopo il giorno 3. Le regole ordinarie (gerarchia, debito, graduatoria) si applicano solo alle disponibilità entro il giorno 3. **Eccezione assoluta:** errori del coordinatore si correggono sempre retroattivamente.

**Mesi disponibili e calcolo di festivi/prefestivi (calendario perpetuo)**: `MESI_DISPONIBILI` copre da Agosto 2026 a Dicembre 2036 (125 mesi). `FESTIVI_MAP`/`PREFESTIVI` non sono più hardcoded: `festiviFissiDi(anno)` genera per qualsiasi anno le festività fisse italiane (1 gennaio, 6 gennaio, 25 aprile, 1 maggio, 2 giugno, 15 agosto, 1 novembre, 8 dicembre, 25 dicembre, 26 dicembre) **più il 31 dicembre — festivo a sé stante per ASFO, non un festivo nazionale italiano ma incluso esplicitamente in `festiviFissiDi` per questo motivo** — più Pasqua/Pasquetta calcolate con `pasquaDi(anno)` (algoritmo di Gauss, verificato contro valori noti: 2026→5 aprile, 2027→28 marzo, 2024→31 marzo). `FESTIVI_MAP` è la fusione di `festiviFissiDi` per gli anni 2026-2037 (l'anno 2037 extra serve solo a calcolare correttamente eventuali prefestivi a cavallo d'anno). `PREFESTIVI` è calcolato automaticamente come il giorno immediatamente precedente a ogni data in `FESTIVI_MAP`: il 30 dicembre è quindi sempre prefestivo del 31 dicembre (non il 31 dicembre stesso, che è festivo con etichetta "SUPERFESTIVO" — il flag `festivo` ha sempre priorità sul flag `prefestivo` nell'etichetta di `turniDelGiorno`, quindi il 31 dicembre mostra "SUPERFESTIVO" anche se tecnicamente rientra anche nel calcolo generico di "giorno precedente a un festivo" rispetto al Capodanno successivo — nessun conflitto, la priorità è esplicita nel codice).

### 3.7 Spaziatura temporale — RIMOSSA

**Questa regola è stata rimossa interamente dal motore** (vedi CONTEXT.md §10 per la cronologia). Esisteva un tempo una regola per cui, tra i turni disponibili di un medico, il motore preferiva quello temporalmente più distante dall'ultimo turno FISICO già assegnato allo stesso medico: se il vincitore "naturale" di una sede aveva lavorato il giorno prima (o lo stesso giorno, su un altro turno — distanza di calendario ≤ 1) ED esisteva un altro candidato che aveva dichiarato verde la STESSA sede e non aveva ancora ottenuto nulla quel turno, la sede passava a quest'ultimo.

**Motivazione della rimozione**: un medico che vince un turno per gerarchia (titolarità → categoria → debito → graduatoria) ha diritto a quel turno — non deve essere ridistribuito a chi sta sotto in graduatoria solo per distanziare le sue presenze, anche se cadono in giorni consecutivi. Chi vince i propri turni se li tiene, punto. La distribuzione temporale VERA e dichiarata (§3.11) resta l'unico meccanismo che può far cedere un turno a un medico, ed è esplicita (tetto mensile) e post-elaborazione, non un'euristica silenziosa attivata turno per turno.

- `giorniTra` (helper di date generico) e `ultimoFisico` (stato che teneva traccia dell'ultimo turno fisico per medico) erano usati SOLO da questa regola: `ultimoFisico` è stato rimosso interamente (parametro/stato morto senza la regola); `giorniTra` è stato mantenuto come utility generica riutilizzabile, testata a parte, pur non essendo più chiamato dal motore.
- Un medico che vince sia il diurno che il notturno lo stesso giorno (weekend/festivo/prefestivo) ora li mantiene ENTRAMBI di default, a meno che non abbia dichiarato esplicitamente quale preferisce (§3.9, meccanismo indipendente e invariato da questa rimozione).
- `correggiTitolarita()` resta richiamata due volte in `elaboraTurno` (dopo FASE1, e di nuovo dopo un rebuild di `slots`): la seconda chiamata non è più giustificata da "la spaziatura può introdurre un nuovo occupante scorretto" (quella regola non esiste più), ma resta una verifica difensiva economica su uno `slots` appena ricostruito pulito dalla fonte di verità (`sedeDi`) — vedi §5.

### 3.8 Tetto settimanale (opzionale)

Il medico può dichiarare, per una specifica settimana (lunedì-domenica), un numero massimo di turni che vuole fare: `dispo[mid]["SETT:" + lunedì] = { maxTurni: N }` — una chiave ortogonale ai normali slotKey `"YYYY-MM-DD|ID"` (mai un turno vero e proprio, va sempre esclusa da qualunque iterazione sui turni di un medico).

- Una volta raggiunto il tetto quella settimana, il medico **non è più candidato per nessuna sede** di nessun turno rimanente della stessa settimana — è come se avesse dichiarato NO per quei turni, ma solo a partire dal momento in cui il tetto viene raggiunto (i turni della settimana già elaborati restano validi).
- **Nessuna copertura automatica di ripiego**: le sedi che sarebbero state sue restano scoperte se nessun altro medico è disponibile — coerente con la filosofia generale del sistema dichiarativo (§3.2).
- Se non dichiarato, nessun limite (comportamento invariato, retrocompatibile).
- Il tetto si applica anche ai turni **extra** (MMG mattina/pomeriggio): contano come "un turno" ai fini del conteggio.
- Impostabile dall'interfaccia nel pannello "Inserimento rapido per intervallo" (campo opzionale "Tetto turni/settimana", applicato a tutte le settimane coperte dal periodo scelto) o via assistente AI (azione `tetto_settimana`).

### 3.9 Preferenza di turno (diurno ☀️ / notturno 🌙), solo giorni con entrambi i turni

Il medico può dichiarare, per un giorno che ha SIA il diurno (G) SIA il notturno (N) — cioè weekend, festivi e prefestivi, gli unici con entrambi — quale dei due preferisce mantenere se li vince ENTRAMBI fisicamente lo stesso giorno: `dispo[mid]["TURNOPREF:" + dataStr] = "G" | "N"` — una chiave ortogonale ai normali slotKey `"YYYY-MM-DD|ID"` (come `"SETT:"`, va sempre esclusa da qualunque iterazione sui turni di un medico, incluso lo stato serializzato per l'assistente AI).

- **Decide SOLO quale dei due turni il medico mantiene se li vince entrambi** — non cambia mai CHI vince un conflitto, non anticipa l'elaborazione, e non decide quale sede riceve. Se il medico vince solo uno dei due turni, la preferenza è un no-op.
- **Non lascia MAI una sede scoperta per questo motivo**: se non esiste un'alternativa valida per il turno NON preferito (nessun altro medico ha dichiarato verde la stessa sede su quel turno), il medico resta assegnato a entrambi — la copertura vince sempre.
- **Dopo lo scambio, la copertura a distanza (blu) del turno ceduto viene RICALCOLATA da capo** (§10 voce 22): il medico uscito potrebbe aver lasciato orfana una propria copertura blu in quel turno (non è più fisicamente presente, quindi non può più coprire nulla a distanza da lì), e il medico entrato potrebbe averne una da offrire. Il ricalcolo usa la STESSA identica risoluzione della FASE 2 dell'elaborazione (helper `risolviBlu`, condiviso proprio per questo): la sede a distanza passa a un altro fisico presente che l'abbia dichiarata blu, con la solita gerarchia (titolarità → categoria → debito → graduatoria) e il solito limite di 1 sede a distanza per medico, e resta SCOPERTA solo se davvero nessun fisico presente la copre — coerente con la filosofia "la copertura vince" (§3.7).
- **Senza una preferenza dichiarata, nessuna cessione avviene**: un medico che vince sia il diurno che il notturno lo stesso giorno li mantiene entrambi di default (dalla rimozione della spaziatura temporale, §3.7 — CONTEXT.md §10 — nessun altro meccanismo cede automaticamente un turno vinto per gerarchia). La preferenza di turno è quindi l'UNICO modo per liberare volontariamente uno dei due turni a favore di un'alternativa: se dichiarata, il turno NON preferito (se ancora assegnato al medico dopo tutta l'elaborazione del mese) viene liberato a favore della stessa gerarchia normale (categoria → debito → graduatoria, tramite `candidatiOrdinati`, condivisa con `elaboraTurno`) — sempre che un'alternativa valida esista.
- Impostabile dal popup di disponibilità (icone ☀️/🌙 accanto al toggle Disponibile/Non disponibile, visibili solo nei giorni con entrambi i turni) oppure via assistente AI (azione `turno_pref`, vedi §13).

### 3.10 Turni extra volontari (oltre il monte ore)

Il coordinatore può dichiarare, per ciascun medico contrattualizzato (non per i senza incarico, che non hanno un concetto di monte ore), un numero di **turni extra volontari** per il mese: `dati.turniExtra[mid] = N` (tab "3 · Medici / ore da recuperare", campo "Turni extra", accanto alle ore extra di recupero). Ogni turno vale sempre 12 ore, quindi il budget in ore è `N × 12`.

- **Pool separato dal debito ordinario**: le ore extra di recupero (`extraOre`) si sommano al monte ore contrattuale — il medico compete con **piena priorità di categoria** finché quel totale (monte + recupero) non è esaurito, esattamente come oggi. I turni extra volontari sono un budget **completamente distinto**, consumato SOLO dopo che monte ore + recupero raggiungono zero.
- **Priorità durante i turni extra**: mentre il budget extra è disponibile (e il debito ordinario è esaurito), il medico compete con la **stessa priorità di un senza incarico** — spareggio SOLO per graduatoria, mai per categoria. Nella pratica, in `candidatiOrdinati` ed `elaboraTurno` viene inserito nello stesso bucket dei senza incarico veri, ordinato insieme a loro puramente per `grad`.
- **Dopo aver esaurito anche il budget extra**, il medico torna al comportamento di "debito esaurito" (§3.1): **blocco rigido**, non è più un candidato per nessun turno, nemmeno per coprire uno slot altrimenti completamente scoperto.
- **Non cambia mai la gerarchia per chi ha ancora debito ordinario positivo**: un medico in bucket 0 (categoria con debito residuo) batte SEMPRE un medico che sta usando i turni extra, indipendentemente dal grad di quest'ultimo — i turni extra non sono mai una scorciatoia per superare la priorità di categoria.
- Implementato con un secondo accumulatore parallelo a `debiti`, chiamato `debitiExtra` (mid → ore residue del budget extra, `null` per i senza incarico), inizializzato in `elaboraSchema` da `turniExtra[mid] × 12` e passato sia a `elaboraTurno` sia a `candidatiOrdinati`. Lo scalo avviene tramite l'helper `scalaDebito` in `elaboraTurno`: se `debiti[mid] > 0` scala il debito ordinario, altrimenti scala `debitiExtra[mid]` — mai entrambi per lo stesso turno. La copertura a distanza (blu) non consuma né l'uno né l'altro pool, coerentemente con la regola generale (§3.2).
- `elaboraSchema(dispo, extraOre, anno, mese, extras, turniExtra = {})`: il nuovo parametro è **opzionale** (default `{}`, nessun turno extra) — tutte le chiamate esistenti restano valide senza modifiche.

### 3.11 Max turni mese e distribuzione temporale reale

**Punto 1 — Max turni mese (tetto mensile, tutte le categorie).** Il coordinatore può dichiarare, per QUALSIASI medico (contrattualizzato O senza incarico), un numero massimo di turni nell'intero mese: `dati.maxTurniMese[mid] = N` (tab "3 · Medici", campo "Max turni mese", accanto a "Turni extra" — colonna sempre attiva, anche per i senza incarico). È un **tetto superiore indipendente dal debito**: può essere anche inferiore al monte ore contrattuale, e il motore si ferma su quel numero anche con debito residuo ampiamente positivo. Diverso dai turni extra (§3.10): i turni extra sono un'ESTENSIONE oltre il monte ore, il Max turni mese è un TETTO che può ridurre il numero di turni effettivi rispetto a quanto il debito permetterebbe.

- Il valore dichiarato è letto da `capMensileDi(maxTurniMese, mid)` (ritorna il numero o `null` = nessun limite) — usato SOLO come input di `tettoDistribuzioneDi` (punto 2 sotto). Non esiste più un filtro separato in `candidatiOrdinati` per questo tetto: dalla v2 in poi Max turni mese ed il monte ore implicito sono UN UNICO meccanismo di enforcement (il tetto di distribuzione), non due controlli distinti — vedi punto 2.
- Wiring AI: azione `{"az":"tetto_mese","medico":"...","maxTurni":N}` (o `null`/assente per rimuovere il tetto), mirror di `tetto_settimana`. La regola "senza incarico — numero di guardie mensili" (§13) genera SEMPRE anche questa azione quando il medico dichiara un numero massimo di guardie: è quello che rende il numero un vincolo REALE nel motore, non solo un avviso testuale.
- `elaboraSchema(dispo, extraOre, anno, mese, extras, turniExtra = {}, maxTurniMese = {})`: 7° parametro, **opzionale** (default `{}`).

**Punto 2 — Distribuzione temporale reale (due passaggi: elabora, poi ridistribuisci).** Quando un medico ha diritto a N turni (il più restrittivo tra monte ore implicito e Max turni mese dichiarato, vedi sotto) ma vincerebbe naturalmente più turni di N nel mese, il motore cede l'eccesso al candidato successivo in gerarchia scegliendo di TENERE il sottoinsieme più equidistanziato possibile — non i primi N cronologici. Riscritto da zero (v2): il vecchio meccanismo a bucket-3-livelli/`calcolaRiservati` (che precalcolava slot "riservati" dalla sola disponibilità dichiarata, PRIMA di eseguire la gerarchia) è stato sostituito da un meccanismo **run-then-redistribute** a due passaggi completi, perché la gerarchia normale (titolarità, spaziatura, ricollocazioni) poteva sovrascrivere le riserve precalcolate e vanificare la distribuzione.

- **Tetto di distribuzione (`tettoDistribuzioneDi`)**: si applica a QUALSIASI medico, il più restrittivo tra il monte ore implicito (arrotondato a turni da 12h — `Math.round(debito/12)`, INDET≈8, DET38≈14, DET24≈9, DET12ASAP/DET12≈4, includendo anche l'eventuale budget di turni extra dichiarato, §3.10 — un residuo inferiore a 6h arrotonda a 0 turni in meno: un medico con debito residuo così piccolo è considerato esaurito ai fini del tetto, il resto va perso, comportamento voluto e non un difetto) e l'eventuale Max turni mese esplicito (punto 1 sopra). Per i **contrattualizzati senza tetto dichiarato**, il monte ore implicito FA SEMPRE da tetto — non c'è modo di disattivare la distribuzione per loro. Per i **senza incarico**, il tetto è `null` (nessuna distribuzione) a meno che non abbia un Max turni mese dichiarato esplicitamente: senza monte ore non c'è alcun riferimento su cui calcolarne uno implicito.
- **I titolari NON sono esenti**: seguono lo stesso tetto di tutti — la titolarità di sede è già protetta separatamente dalla gerarchia normale (§3.1a) finché il medico resta in gara; non è un'esenzione dal proprio tetto di distribuzione.
- **Il tetto è RIGIDO**: non si supera mai. Se un turno ceduto non trova nessun altro candidato disponibile, la sede resta SCOPERTA — la copertura non prevale mai sul tetto dichiarato dal medico (a differenza, per esempio, della spaziatura §3.7 o della preferenza di turno §3.9, dove la copertura vince sempre).
- **Passaggio 1 (oracolo, scartato)**: `elaboraSchema` esegue l'INTERO mese una prima volta con la sola gerarchia esistente (`eseguiMese`, la stessa funzione usata anche dal passaggio 2), senza alcuna esclusione per tetto — serve solo a scoprire, per ogni medico, quali turni vincerebbe naturalmente (non c'è altro modo affidabile di saperlo: dipende da chi altro è disponibile, titolarità, spaziatura...). Il risultato (`risultatiP1`) è scartato subito dopo l'uso.
- **Pool di distribuzione (`poolDi[mid]`, solo presenze fisiche/extra, MAI la copertura blu — stessa regola già usata per il conteggio del tetto)**: l'insieme dei turni tra cui scegliere il sottoinsieme equidistante da tenere. Ha DUE forme, a seconda di cosa "morde":
  - **Caso base — pool = turni vinti nel passaggio 1** (oracolo normale). Vale quando il vincolo che morde è il monte ore (nessun Max turni mese esplicito, o cap ≥ turni impliciti): il medico è genuinamente esaurito dopo i suoi turni, e distribuire su ciò che ha vinto è corretto.
  - **Caso corretto — pool = turni vinti su TUTTO il mese da un oracolo per-medico "senza auto-esaurimento"** (correzione del bug §3.11, vedi §10 voce 20). Vale SOLO quando morde un **Max turni mese ESPLICITO più restrittivo del monte ore** (`cap != null && cap < turni-impliciti`): in quel caso il passaggio 1 normale fa esaurire il medico nella prima settimana, quindi vince solo turni ammucchiati all'inizio, e l'equidistante su quel pool ristretto li terrebbe ammucchiati (bug reale: DET24 disponibile tutte le notti + cap 4 → giorni 1,2,4,7 invece di sparsi). Per questi medici il pool viene ricalcolato con un oracolo dedicato (`eseguiMese(..., new Set([mid]))`) che esenta SOLO quel medico dal blocco rigido del monte ore (parametro `esente` propagato a `candidatiOrdinati`): così "vince" tutti i turni di cui è il legittimo vincitore per gerarchia sull'INTERO mese, e l'equidistante li sparge davvero (→ giorni ~1,10,22,31). Gate ristretto ai soli contrattualizzati: i senza incarico non hanno monte ore, non si esauriscono mai, il loro pool già copre tutto il mese. Il passaggio 2 (blocco monte ore + tetto rigido live) resta INVARIATO e produce il risultato reale — l'oracolo per-medico serve solo a costruire il pool; nessun rischio di superare monte ore o tetto (cap turni ≤ (implicito−1) turni < monte ore, le ore bastano sempre per i turni tenuti).
- **Selezione delle cessioni**: per ogni medico il cui pool supera il proprio tetto, il motore:
  1. Raggruppa i vinti per **livello della sede verde ottenuta** (1 = più desiderata) — la priorità di sede dichiarata dal medico è **ASSOLUTA sull'equidistanza**: un turno di livello migliore va sempre tenuto rispetto a uno di livello peggiore, anche se quest'ultimo sarebbe temporalmente più distanziato.
  2. Riempie il tetto residuo un livello alla volta, dal migliore al peggiore: se un livello entra per intero nel residuo lo tiene tutto (i suoi giorni si aggiungono ai "giorni fissi" per il punto successivo) e passa al livello successivo; altrimenti (livello più numeroso del residuo) sceglie tra quel livello il sottoinsieme da tenere e azzera il residuo — tutto il resto di quel livello E tutti i livelli peggiori successivi sono interamente marcati "da cedere". Un livello viene anche solo toccato SOLO dopo che tutti i livelli migliori sono stati riempiti per intero: la priorità di sede resta assoluta, nessun livello peggiore può mai "rubare" spazio a uno migliore.
  2b. **Selezione CROSS-LIVELLO** dentro il livello da ridurre: il PRIMO livello mai ridotto (nessun livello migliore fissato prima, o unico/primo livello con vittorie per quel medico) usa la pura equidistanza posizionale (`scegliIndiciEquidistanti(k, n)`, indici il più possibile equidistanti tra 0 e k-1). Un livello successivo ridotto (con giorni già fissati da livelli migliori) usa invece `scegliConRiferimento(candidati, n, giorniFissi)`: una selezione greedy "farthest-point" che ad ogni passo sceglie il candidato con la distanza MINIMA più alta possibile da tutti i giorni di riferimento raccolti finora (giorni fissi + scelte già fatte in questo stesso livello, che si aggiungono al riferimento a mano a mano) — così il livello corrente si distribuisce bene sia rispetto ai livelli migliori sia al proprio interno, incastrandosi con essi invece di sovrapporsi in giorni consecutivi. A parità di distanza, vince il candidato cronologicamente più vicino tra i rimanenti (determinismo). Questo cambia SOLO la selezione dentro il livello ridotto, mai quali livelli vengono riempiti per primi.
  3. Con il pool del **caso base** (monte ore che morde, nessun cap esplicito più restrittivo) il pool resta concentrato nella finestra in cui il medico è naturalmente il candidato più forte (tipicamente i primi turni del mese finché il debito non si esaurisce): è una conseguenza accettata della gerarchia, non un difetto — quando il tetto coincide col numero di vittorie naturali non c'è nulla da cedere e nessuna distribuzione ha effetto. Con il pool del **caso corretto** (Max turni mese esplicito più restrittivo del monte ore) il pool copre invece tutto il mese (oracolo per-medico senza auto-esaurimento) e l'equidistante sparge davvero i turni tenuti — è la correzione del bug §3.11 (§10 voce 20).
- **Passaggio 2 (rielaborazione pulita e DEFINITIVA)**: rifà l'intero mese da zero (stato iniziale, nessun residuo dal passaggio 1) applicando le cessioni decise sopra come esclusioni per singolo slot (`escludiPerSlot`), più un controllo LIVE del tetto (`contoMensile[mid] >= tetto[mid]`) applicato uniformemente a ogni turno — non solo ai medici segnalati dal passaggio 1: le esclusioni cambiano le dinamiche del mese (un concorrente escluso oggi può far vincere un altro medico un giorno che nel passaggio 1 non avrebbe vinto), quindi il conteggio va tenuto vivo turno per turno per garantire che il tetto non sia MAI superato, in nessun caso. Il risultato di questo passaggio è quello REALE, definitivo — il passaggio 1 non compare mai nell'output finale.
- `elaboraSchema(dispo, extraOre, anno, mese, extras, turniExtra = {}, maxTurniMese = {})`: stessa firma di prima (7° parametro invariato).

**Punto 3 — Aggiustamento mensile del monte ore (bilanciamento turni annui, DET38, DET24 e DET12/DET12ASAP).** Il monte ore BASE resta sempre quello dichiarato in `CAT_INFO` (invariato: DET38=168h, DET24=104h, DET12/DET12ASAP=52h), ma viene aggiustato di ±8h/±12h in mesi specifici PRIMA di calcolare `debitoOrdinarioIniziale` — e quindi anche il tetto automatico di distribuzione (`Math.round(debito/12)`, punto 2 sopra) — così da bilanciare il numero di turni sull'intero anno invece di lasciarlo fisso ogni mese.

- Tabella `AGGIUSTAMENTO_MESE_ORE` (per categoria, mesi 0-based come `MESI_IT`): `DET38: { mesi: [1,3,8], delta: -12 }` (Febbraio, Aprile, Settembre: 168h→156h, 14→13 turni impliciti); `DET24: { mesi: [1,3,8,10], delta: -8 }` (Febbraio, Aprile, Settembre, Novembre: 104h→96h, 9→8 turni impliciti); `DET12ASAP` e `DET12: { mesi: [2,4,7,11], delta: 8 }` (Marzo, Maggio, Agosto, Dicembre: 52h→60h, 4→5 turni impliciti). Negli altri mesi dell'anno il monte ore resta quello base (168h/14 per DET38, 104h/9 per DET24, 52h/4 per DET12/DET12ASAP).
- Totali annui risultanti: **DET38 = 165 turni/anno** (9 mesi da 14 + 3 mesi da 13); **DET24 = 104 turni/anno** (8 mesi da 9 + 4 mesi da 8); **DET12/DET12ASAP = 52 turni/anno** (8 mesi da 4 + 4 mesi da 5) — stesso numero di turni annuo di prima, solo ridistribuito diversamente tra i mesi.
- **Solo INDET non ha mai aggiustamento**: monte ore fisso in ogni mese (96h/8 turni), nessuna voce in `AGGIUSTAMENTO_MESE_ORE`.
- `debitoOrdinarioIniziale(mid, extraOre, mese)`: firma cambiata, ora riceve anche `mese` (già disponibile in `elaboraSchema`, che lo passa al momento di popolare `debiti0`). Le ore extra di recupero (`extraOre`) si sommano SEMPRE dopo l'aggiustamento mensile, mai prima.
- Nessuna modifica a gerarchia, conflitti o logica di assegnazione: l'aggiustamento tocca SOLO il valore iniziale del debito, esattamente come un `extraOre` dichiarato dal coordinatore — tutto il resto del motore (titolarità, categoria, spaziatura, distribuzione) resta invariato e agisce sul nuovo valore esattamente come faceva prima su quello fisso.

Test dedicati: `test_max_turni_mese.mjs` (7 casi), `test_distribuzione_temporale.mjs` (13 casi, inclusi un caso dedicato alla priorità di sede assoluta sull'equidistanza, uno alla selezione cross-livello, i casi di correzione del pool — turni sparsi su tutto il mese OGNI volta che la disponibilità supera il tetto, anche senza cap esplicito e col titolare, §10 voci 20 e 48 — e i casi degli slot obbligatori come punti fissi §10 voce 49), `test_aggiustamento_mensile.mjs` (16 casi: tetto DET38 14/13, DET24 8/9, DET12/DET12ASAP 5/4 nei mesi giusti e sbagliati, INDET invariato, totali annui 165/104/52). La QUALITÀ della distribuzione (turni sparsi, non ammucchiati) è coperta SOLO da questi unit test deterministici, MAI dalla simulazione massiva: un invariante di distribuzione nella sim è stato tentato e rimosso perché l'ammucchiamento legittimo (cap piccolo, competizione, priorità di sede) non è distinguibile dal bug senza replicare il motore nel test (§10 voce 20). Gli invarianti della simulazione (§8, §12) restano solo quelli robusti: tetto mensile mai superato (INV-MAXTURNI), assegnazioni valide (INV1/2/3), titolarità sempre rispettata (INV-TITOLARE, col limite noto residuo §3.1a).

---

## 4. GRADUATORIA (14 medici reali: 8 titolari determinati + 1 INDET fuori graduatoria + 5 senza incarico)

```
ZURLO(id1, DET38, grad2, tit.Maniago)            TRIGODKO(id2, DET24, grad4, tit.Maniago)
PITAU(id3, DET24, grad14, tit.Maniago)           BEKAEVA(id4, DET24, grad17, tit.Maniago)
MORANO(id5, DET12, grad72, tit.Maniago)          FOSCHIANI(id6, DET38, grad3, tit.Spilimbergo)
MARTINETTI(id7, DET24, grad5, tit.Spilimbergo)   VALERI(id8, DET12ASAP, grad25, tit.Spilimbergo)
BERTUZZI(id9, INDET, grad108, tit.Spilimbergo)
PRESSACCO(id10, SENZA, grad57)  CERVESATO(id11, SENZA, grad63)  DE CANDIDO(id12, SENZA, grad83)
MERLINO(id13, SENZA, grad105)   IENGO(id14, SENZA, grad107)
```

8 titolari determinati (5 su Maniago: ZURLO/TRIGODKO/PITAU/BEKAEVA/MORANO — 3 su Spilimbergo: FOSCHIANI/MARTINETTI/VALERI), 1 INDET fuori graduatoria ufficiale titolare di Spilimbergo (BERTUZZI, grad108 deliberatamente pessimo — perde ogni spareggio per grad/categoria, ma è titolare di Spilimbergo e vince lì per titolarità nonostante l'INDET normalmente perda solo contro un'altra INDET), 5 medici SENZA incarico (nessuna titolarità: erano determinati ma non hanno preso il contratto). Nessuna sovrapposizione di grad tra i 14. Tutti i 9 contrattualizzati (8 determinati + BERTUZZI) hanno una titolarità reale assegnata (obbligatoria per ogni contrattualizzato, §3.1a): nessun `sedeContratto: null` per loro; i 5 SENZA hanno invece sempre `sedeContratto: null`.

La lista è modificabile dall'interfaccia (tab "3 · Medici / ore da recuperare": categoria, graduatoria, titolarità di sede per ogni contrattualizzato) e salvata nello store persistente. In `store.medici` se presente, altrimenti `MEDICI_DEFAULT`.

---

## 5. ARCHITETTURA DEL MOTORE

### elaboraTurno (cuore)

```javascript
function elaboraTurno(d, turno, slotKey, dispo, debiti, debitiExtra, settimanaCount) {
  // 1. Trova candidati con disponibilità valida (verde o blu) per questo slotKey, ESCLUSI quelli
  //    che hanno già raggiunto il tetto settimanale dichiarato per la settimana di questo turno
  //    (capSettimanale, §3.8) — se non dichiarato, nessuna esclusione (comportamento invariato) —
  //    ED ESCLUSI i contrattualizzati con debito ordinario e turni extra ENTRAMBI esauriti
  //    (blocco rigido oltre il monte ore, §3.1/§3.4): non sono più candidati per nessun turno,
  //    nemmeno se resterebbero l'unico disponibile, il turno resta SCOPERTO. Il Max turni mese e
  //    la distribuzione temporale (§3.11) NON sono un filtro qui: sono applicati interamente in
  //    un secondo passaggio di post-elaborazione in elaboraSchema (vedi sotto) tramite esclusioni
  //    per singolo slot passate dall'esterno (dispo effettiva già modificata per quello slotKey).
  // 2. Li ordina in DUE bucket (bucketDi/bucketOf): [conDeb (cat→deb→grad, bucket 0),
  //    senzaInc+turniExtra (grad, bucket 1)] — ordine globale, la titolarità NON entra in questo
  //    ordinamento globale (è specifica per sede). Un contrattualizzato con debito esaurito ma con
  //    debitiExtra[mid] > 0 (turni extra volontari residui, §3.10) rientra nel bucket 1 (è già
  //    filtrato fuori se anche i turni extra sono esauriti, vedi punto 1).
  //    Scala il debito del vincitore con scalaDebito(mid, ore): se debiti[mid] > 0 scala il
  //    debito ordinario, altrimenti scala debitiExtra[mid] — mai entrambi per lo stesso turno.
  // 3. FASE 1 — assegnazione fisica (verde):
  //    - sediFisiche = turno.id === "G" ? [MA,SP,ME,CL,AN] : [MA,SP,ME] — Claut/Anduins fisiche
  //      SOLO nel diurno (§3.2, §10 voce 30); target = prefisso troncato al n° di medici (dinamico
  //      per n=1: miglior verde tra le sedi oggi fisiche)
  //    - Itera TUTTI i candidati di "ordinati", senza fermarsi quando i target sono già pieni
  //      (§10): "ordinati" è ordinato per bucket/categoria/debito/graduatoria, MAI per
  //      titolarità — un titolare può comparire più avanti nell'elenco di un non titolare di
  //      categoria migliore che ha già occupato la sua sede, e deve comunque avere la possibilità
  //      di contestarla. provaFisica() è sicuro da richiamare per ogni candidato: se non ha una
  //      pretesa reale non cambia nulla.
  //    - provaFisica() — assegnazione con ricollocazione e scalzamento:
  //      - Rispetta livelli verdi (veto se livello migliore); a parità di livello l'ordine di
  //        prova segue sempre SEDI5 (Maniago→Spilimbergo→Meduno→Claut→Anduins), MAI l'ordine
  //        di dichiarazione — vedi ordinaPerLivello
  //      - Ricollocazione "a costo zero" (pari/miglior livello, indifferenza dichiarata): se
  //        l'occupante è indifferente, si sposta — MA MAI se l'occupante è TITOLARE proprio
  //        della sede contesa e il richiedente non ha priorità realmente superiore (§10): la
  //        titolarità garantisce quella sede specifica, non solo "una sede accettabile qualunque".
  //      - Scalzamento: solo se il richiedente ha priorità superiore secondo isBetterPriority()
  //        (titolarità sede → categoria → debito → graduatoria tra determinati)
  //    - correggiTitolarita() [prima chiamata]: per ogni titolare non ancora sulla propria sede
  //      dichiarata come prima preferenza oggi, se l'occupante attuale non ha davvero priorità
  //      superiore (isBetterPriority), lo scambia dentro e ridà all'occupante spostato una
  //      possibilità di ricollocarsi tramite lo stesso provaFisica (§3.1a, §10)
  //    - Rebuild slots da sedeDi (fonte di verità), poi correggiTitolarita() [seconda chiamata,
  //      verifica difensiva economica contro eventuali "fantasmi" residui della ricollocazione
  //      ricorsiva — non più giustificata da una regola di spaziatura temporale intermedia, RIMOSSA
  //      — §3.7, §10), poi un secondo rebuild finale di slots da sedeDi
  //    - [DIURNO] step additivo "4° medico" (§10 voce 32, PERCORSO SEPARATO: FASE1 intatta): con
  //      sole assegnazioni dirette (nessuna ricollocazione), (A) se Anduins è libera piazzaci un
  //      medico eleggibile rimasto inutilizzato che l'abbia dichiarata verde; (B) tie-break: se
  //      Claut è occupata da un "indifferente" (Claut+Anduins stesso livello) e Anduins è l'unico
  //      buco a distanza, spostalo su Anduins ("tappa il buco"), else resta su Claut
  //    - Scala i debiti dei fisici, incrementa settimanaCount
  // 4. FASE 2 — copertura a distanza (blu): delegata all'helper di modulo risolviBlu(fisMids,
  //    sedeFisicaDi, ...), condiviso con la preferenza turno §3.9 in elaboraSchema (unica sorgente
  //    di verità della logica blu — §10 voce 22). Riceve la mappa medico→sede-fisica (sedeDi) per il
  //    vincolo territoriale; sitiCoperti è derivato da essa:
  //    - Solo i FISICI di questo turno tentano, nell'ordine di gerarchia (bucket → categoria →
  //      debito → grad, lo stesso di candidatiOrdinati)
  //    - Vincolo TERRITORIALE prima della gerarchia (§3.2, §10 voce 31): puoCoprireADistanza(mid,si)
  //      — Claut(3) solo se il medico è fisico a Maniago(0); Anduins(4) solo se a Spilimbergo(1) o
  //      Meduno(2); altre sedi nessun vincolo. Chi non è nella base ammessa non si candida
  //    - provaBlu() — schema ricorsivo di bump/retry, con la STESSA isBetterPriority()
  //      usata per il fisico (titolarità sede → categoria → debito → graduatoria tra determinati)
  //    - Ogni medico copre al massimo 1 sede a distanza
  // 5. Avviso per qualunque sede (fisica o a distanza) rimasta scoperta
}
```

**Nota storica (§3.1a, §10)**: in scenari con 3+ determinati che si contendono più sedi sovrapposte nello stesso turno, catene di ricollocazione profonde in `provaFisica` potevano raramente lasciare un titolare fuori dalla propria sede — risolto con una passata di correzione mirata `correggiTitolarita()`, eseguita due volte (dopo FASE1, e di nuovo dopo un rebuild di slots — vedi sopra; la spaziatura temporale §3.7, motivo originale della seconda chiamata, è stata da allora rimossa — §10).

**INVARIANTI DEL MOTORE (non devono mai essere violati):**
- INV1: nessun medico fisico senza sede VERDE dichiarata per quella sede
- INV2: nessun medico con NO esplicito assegnato (né fisico né a distanza)
- INV3: coperture a distanza solo da medici fisicamente presenti nel turno, e solo su sedi che hanno dichiarato come BLU
- INV_BLU1: un medico copre al massimo 1 sede a distanza per turno
- INV_GER: nessun medico con priorità inferiore (titolarità sede → categoria → debito → grad — stessa identica gerarchia sia per il fisico che per il blu) occupa una sede che un medico con priorità superiore voleva e non ha ottenuto
- INV_SETT: nessun medico risulta fisico più volte di quante dichiarate dal proprio tetto settimanale (se dichiarato) per la settimana di quel turno
- INV_MESE (INV-MAXTURNI nella simulazione, §8): nessun medico risulta fisico/extra più volte di quante permesse dal proprio tetto di distribuzione (il più restrittivo tra monte ore implicito e Max turni mese dichiarato, §3.11), per l'intero mese — verificato nel passaggio 2 definitivo di elaboraSchema

**Invarianti storiche RIMOSSE con il nuovo sistema** (non più valide, sostituite dal modello dichiarativo):
- ~~INV4: Claut a distanza viene sempre da Maniago~~ — ora dipende esclusivamente da chi dichiara blu su Claut.

### elaboraSchema (orchestratore)

```javascript
function elaboraSchema(dispo, extraOre, anno, mese, extras, turniExtra = {}, maxTurniMese = {}) {
  // Inizializza debiti0: CAT_INFO[cat].ore + (extraOre[mid] || 0) (debitoOrdinarioIniziale)
  // Inizializza debitiExtra0: (turniExtra[mid] || 0) × 12 — budget separato, §3.10 (turniExtra è
  // opzionale, default {}: tutte le chiamate esistenti restano valide senza modifiche)
  // Costruisce la lista di TUTTI i turni del mese (voci, ordine di calendario) e l'ordine di
  // elaborazione ORDINE CRITICO: [...conPref, ...resto]
  //   conPref = turni dove almeno un medico ha marcato con ★ una sua sede verde (preferito != null)
  //   Questo ordine cambia i debiti progressivi — il checker di gerarchia DEVE rispettarlo
  //
  // eseguiMese(debiti, debitiExtra, settimanaCount, escludiPerSlot, dopoTurno):
  //   esegue ordineVoci chiamando elaboraTurno per ognuno, con un'eventuale esclusione live per
  //   singolo slot (escludiPerSlot(slotKey) -> Set<mid>, forza "no" SOLO per quel medico/slot,
  //   senza toccare il resto della sua dispo) e un callback dopoTurno(turno, turnoOut) opzionale.
  //
  // ---- PASSAGGIO 1 (§3.11, punto 2): oracolo, gerarchia pura, nessun tetto ----
  // risultatiP1 = eseguiMese(copia di debiti0/debitiExtra0, {}, {}, null, null) — scoperto subito
  // dopo l'uso: serve solo a sapere quali turni ogni medico vincerebbe naturalmente.
  //
  // tetto[mid] = tettoDistribuzioneDi(mid, debiti0[mid], debitiExtra0[mid], maxTurniMese) — il più
  // restrittivo tra monte ore implicito (incluso il budget turni extra) e Max turni mese esplicito;
  // null solo per i senza incarico senza tetto dichiarato (nessuna distribuzione per loro).
  //
  // vintiDi[mid] = turni fisici/extra EFFETTIVAMENTE vinti nel passaggio 1, in ordine cronologico,
  // ciascuno con il livello della sede verde ottenuta (livelloVintoDi) — la copertura blu non
  // conta mai (stessa regola usata per il conteggio del tetto, §3.11 punto 1).
  //
  // Per ogni medico con vintiDi[mid].length > tetto[mid]: raggruppa per livello (1 = migliore),
  // riempie il tetto residuo livello per livello dal migliore al peggiore (un livello che eccede
  // il residuo usa scegliIndiciEquidistanti(k, n) se è il primo livello ridotto, altrimenti
  // scegliConRiferimento(candidati, n, giorniFissi) per tenere conto anche della distanza dai
  // giorni già fissati dai livelli migliori — §3.11 punto 2 sotto-punto b), marca tutto il resto
  // "da cedere" in cessioniPerSlot (slotKey -> Set<mid>).
  //
  // ---- PASSAGGIO 2 (§3.11, punto 2): rielaborazione pulita e DEFINITIVA ----
  // Riparte da debiti0/debitiExtra0 (nessun residuo dal passaggio 1). escludiPerSlot(slotKey)
  // unisce le cessioni precalcolate per quello slot E, live, chiunque abbia già raggiunto il
  // proprio tetto (contoMensile[mid] >= tetto[mid]) in QUESTO passaggio — non solo chi era stato
  // segnalato dal passaggio 1, perché le esclusioni cambiano le dinamiche del mese. Il risultato
  // di questo passaggio (risultati) è quello REALE — il tetto non è mai superato: se un turno
  // ceduto non trova un'alternativa disponibile resta SCOPERTO (la copertura non prevale mai sul
  // tetto dichiarato).
  //
  // PREFERENZA TURNO (§3.9): dopo che TUTTO il mese (passaggio 2) è elaborato, per ogni giorno con
  // G e N, per ogni medico che vince fisicamente ENTRAMBI e ha dichiarato una preferenza, libera
  // il turno non preferito a favore della stessa gerarchia (candidatiOrdinati) — solo se
  // l'alternativa non ha già raggiunto il proprio tetto di distribuzione (contoMensile/tetto) — se
  // un'alternativa esiste. Storna/scala lo stesso pool (debiti o debitiExtra) che il turno aveva
  // effettivamente consumato per ciascuno, in base al segno corrente di debiti[mid] (identica
  // logica di scalaDebito), e aggiorna contoMensile per entrambi. Eseguita in un passaggio a
  // parte, dopo l'intero ciclo conPref+resto, perché deve conoscere l'esito di entrambi i turni
  // dello stesso giorno indipendentemente da quale dei due è stato elaborato per primo.
  // Dopo ogni scambio il turno ceduto ha un nuovo insieme di fisici: la sua copertura a distanza
  // (blu) viene ricalcolata da capo con risolviBlu (§3.9, §10 voce 22), così una copertura
  // rimasta orfana passa a un altro fisico presente o resta scoperta — mai da un non-fisico.
  // Raccoglie avvisi: copertura scoperta (per sede)
}
```

---

## 6. FEATURE IMPLEMENTATE (tutte complete e testate)

1. **Disponibilità dicotomiche** (verde disponibile / rosso non disponibile) — visivamente 2 stati, internamente 3 (no esplicito, non specificato, disponibile)
2. **NO esplicito** — protegge l'indisponibilità dall'inserimento rapido massivo
3. **Inserimento rapido per intervallo** — compila blocchi di disponibilità (verde e/o blu) con periodi di eccezione
4. **Menu a tendina per sede** — sostituisce il vecchio ciclo a tocchi: per ogni sede, un `<select>` con Non disponibile / Sede principale 1-5 / Copertura a distanza 1-4 (etichette solo UI: `verde`/`blu` restano i nomi interni nel motore — vedi §3)
5. **Sistema verde/blu** — verde = sede fisica (unificata, niente più piena/ripiego a due livelli), blu = disponibilità a coprire a distanza (nessuna copertura automatica, un medico copre al massimo 1 sede a distanza)
6. **Titolarità di sede obbligatoria per ogni contrattualizzato** — campo `sedeContratto` (Maniago/Spilimbergo, obbligatorio per INDET/DET38/DET24/DET12ASAP/DET12, mai per SENZA), decide i conflitti sulla sede contesa tra QUALSIASI coppia di contrattualizzati prima della categoria (§3.1a)
7. **Avvisi post-elaborazione** per sedi scoperte — `dati.avvisi` continua a essere generato dal motore e inviato all'assistente AI (`stato.avvisiScenari`), ma dalla UI non è più visualizzato: la sezione "⚠ Avvisi — richieste da fare ai medici" nel tab Schema turni è stata rimossa (era troppo dispersiva)
8. **Esportazione Excel** — layout identico al file reale ASFO (costruito a mano come ZIP OOXML). Sede scoperta: Maniago/Spilimbergo → cella "SCOPERTO" (maiuscolo) rossa grassetto (stile 11, emergenza); Meduno/Claut/Anduins → cella "scoperto" (minuscolo) grigio scuro `#666666` non grassetto (stile 13, neutro, sede secondaria) — mai vuota, mai rossa, per distinguere visivamente un buco su una CDC da uno su una sede minore. Questo vale anche quando un'ALTRA sede dello stesso turno è coperta (es. scenario 1 con un solo medico fisico su una sede diversa da Maniago): Maniago/Spilimbergo mostrano comunque "SCOPERTO" invece di una cella vuota (bug corretto — in precedenza il ramo `t.slots.some(Boolean)` di `buildSheetXML` gestiva l'assenza di medico solo per Meduno/Claut/Anduins, lasciando Maniago/Spilimbergo senza testo in quel caso). Etichette dei turni adattate SOLO per l'export (`ETICHETTE_EXPORT` in `buildSheetXML`, la griglia a schermo resta invariata): il diurno feriale/weekend "semplice" perde l'orario e diventa solo "DIURNO" (prefestivo e superfestivo restano con l'orario completo); le colonne MMG mattina/pomeriggio diventano "ANTICIPO DIURNO MMG e PLS 8-14" / "...14-20", con tutte e 5 le sedi mostrate (Maniago = il medico assegnato o SCOPERTO; Spilimbergo sempre SCOPERTO rosso; Meduno/Claut/Anduins sempre "scoperto" grigio, perché il turno MMG non le copre mai).
10. **Spaziatura temporale** — a parità di alternative valide, evita di assegnare due turni consecutivi allo stesso medico; non lascia mai sedi scoperte per questo (§3.7)
11. **Tetto settimanale opzionale** — il medico dichiara un massimo di turni per settimana, impostabile da UI (Rapido) o AI (§3.8)
12. **AI integrata** — conosce tutte le regole (incluse titolarità e verde/blu), può modificare disponibilità e schema tramite JSON
13. **Medici modificabili** — categoria, graduatoria e titolarità di sede modificabili dall'UI, aggiunta/rimozione medici
14. **Azzera mese con doppio tocco** — sicuro, posizionato lontano dai pulsanti di esportazione
15. **Undo/redo** — history completo di tutte le azioni
16. **Storage persistente** — `window.storage` (API Claude.ai), chiave `gm-turni-store-v3`
17. **Pubblicazione GitHub Pages** — copia in `docs/` con React/Babel vendorizzati localmente (vedi §14)
18. **Categorie DET12ASAP e DET12** — determinati 12h/sett, 52h mensili; DET12ASAP a pari priorità con DET24 (spareggio diretto per titolarità → debito → graduatoria), DET12 sotto entrambi, sopra solo ai senza incarico (§3.1)
19. **Preferenza di turno stesso giorno (☀️/🌙)** — solo nei giorni con diurno e notturno: decide quale dei due il medico mantiene se li vince entrambi (senza dichiarazione, li mantiene entrambi di default); impostabile dal popup di disponibilità o via assistente AI (azione `turno_pref`) (§3.9)
20. **Colonne "Ore assegnate" / "Ore mancanti" nel tab Medici** — sola lettura, visibili solo dopo l'elaborazione dello schema del mese ("—" altrimenti). "Ore assegnate" = somma delle ore dei turni in cui il medico compare FISICAMENTE nello schema elaborato (stessa logica di scalo del debito nel motore — la copertura a distanza non consuma ore proprie, coerente con `elaboraTurno`), contato UNA SOLA VOLTA per turno anche se lo stesso medico compare in più sedi fisiche dello stesso turno (bug corretto: l'editor manuale `setSlot` nel tab Schema turni riassegna `slots` ma non aggiorna mai `fis`, quindi una correzione manuale può in teoria lasciare lo stesso medico su 2 sedi fisiche dello stesso turno — `oreAssegnateDi` deduplica con un `Set` per evitare di contare le sue ore due volte). "Ore mancanti" = monte ore + ore extra − ore assegnate; per i medici senza incarico (nessun monte ore) mostra sempre "—", anche a schema elaborato. Calcolate interamente lato UI da `dati.schema` — nessuna modifica al motore
21. **Pulsante "Nuova conversazione" nel pannello AI** — azzera chat, proposta in sospeso e registro anti-loop `azioniEseguite` (§13) senza dover ricaricare la pagina
22. **Turni extra volontari** — campo "Turni extra" nel tab Medici (accanto alle ore extra di recupero): budget separato dal debito ordinario, consumato SOLO dopo aver esaurito monte ore + recupero, con priorità da senza incarico (solo graduatoria) (§3.10)
23. **Blocco rigido oltre il monte ore (fix bug)** — un contrattualizzato con debito ordinario e turni extra ENTRAMBI esauriti non è più un candidato per nessun turno, nemmeno se resterebbe l'unico disponibile: il turno resta SCOPERTO invece di essere assegnato oltre il limite dichiarato (§3.1). Prima di questo fix un medico rimasto l'unico candidato disponibile per molte notti consecutive continuava a essere assegnato ben oltre il proprio monte ore (bug segnalato: 228h assegnate su un monte ore di 96h). Filtro applicato in `candidatiOrdinati`, `bucketOf` semplificato di conseguenza (§5)
24. **Selettori diretti mese/anno nell'header** — accanto alle frecce ‹/› (che restano per il caso d'uso "mese successivo/precedente"), due `<select>` compatti e affiancati (mese, poi anno), stile "app nativa", al posto di un unico menu combinato. Le opzioni del mese si filtrano in base all'anno scelto (`mesiDelAnno`, derivato da `MESI_DISPONIBILI`): il 2026 ha solo 5 mesi (agosto-dicembre), tutti gli altri anni 12. Cambiando l'anno mentre è selezionato un mese non valido per quel nuovo anno (es. "Marzo" tornando al 2026), `vaiAMese` ricade automaticamente sul primo mese disponibile per l'anno scelto — mai una combinazione inesistente in `MESI_DISPONIBILI`. Stato invariato (`meseIdx`), nessuna nuova variabile di stato. Testato in browser (Playwright): opzioni mese filtrate correttamente per anno (5 per il 2026, 12 per gli altri), cambio anno con mese preservato quando valido, fallback al primo mese disponibile quando non valido, nessun errore in console.
25. **Max turni mese** — campo "Max turni mese" nel tab Medici, valido per QUALSIASI categoria (anche senza incarico): tetto rigido indipendente dal debito, collegato all'azione AI `tetto_mese` e alla regola "numero di guardie mensili" per i senza incarico (§3.11 punto 1)
26. **Distribuzione temporale reale (v2, run-then-redistribute)** — QUALSIASI medico (titolari inclusi, nessuna esenzione) il cui tetto di distribuzione (il più restrittivo tra monte ore implicito e Max turni mese dichiarato) è inferiore ai turni che vincerebbe naturalmente, cede l'eccesso al candidato successivo in gerarchia tenendo il sottoinsieme più equidistanziato possibile dei turni EFFETTIVAMENTE vinti (con priorità di sede assoluta sull'equidistanza); il tetto è rigido, mai superato, coverage non prevale mai su di esso (§3.11 punto 2)
27. **Aggiustamento mensile del monte ore (bilanciamento turni annui)** — DET38 perde 12h (168h→156h, 14→13 turni impliciti) a Febbraio/Aprile/Settembre; DET24 perde 8h (104h→96h, 9→8 turni impliciti) a Febbraio/Aprile/Settembre/Novembre; DET12 e DET12ASAP guadagnano 8h (52h→60h, 4→5 turni impliciti) a Marzo/Maggio/Agosto/Dicembre; compensato sugli altri mesi (165 turni/anno per DET38, 104 per DET24, 52 per DET12/DET12ASAP). Solo INDET invariato ogni mese. Tocca solo il valore iniziale del debito (`debitoOrdinarioIniziale`, tabella `AGGIUSTAMENTO_MESE_ORE`), nessuna modifica a gerarchia/conflitti/assegnazione (§3.11 punto 3)
28. **Equità sui turni extra nel tab Medici (SOLO visualizzazione)** — a schema elaborato, per ogni medico che ha CHIESTO turni extra (`turniExtra > 0`) la colonna "Turni extra" mostra "Extra: X/Y": Y = extra richiesti, X = extra OTTENUTI = turni assegnati oltre il monte ore ordinario. X è derivato dallo schema già prodotto (`turni vinti − ceil(monteOrdinario/12)`, con `monteOrdinario = debitoOrdinarioIniziale` = monte ore aggiustato per mese + recupero, cioè la stessa soglia oltre cui il motore consuma il budget extra §3.10), poi limitato a [0, Y]; **nessuna modifica al motore/assegnazione**. In cima alla lista un'etichetta di **"iniquità percepita"** (misura RELATIVA — il confronto tra colleghi, non una soglia assoluta): dal divario in punti percentuali tra la soddisfazione (X/Y) più alta e la più bassa tra chi ha chiesto extra → Nessuna (≤15) / Bassa (16-35) / Media (36-55) / Alta (56-75) / Altissima (>75), con un **correttivo "chi sta peggio" PESATO sul numero di penalizzati** (§10 voce 29): un medico è "penalizzato" se sotto il 25% di soddisfazione; se c'è disparità reale (max > min) e almeno un penalizzato l'etichetta sale di uno scatto, se i penalizzati sono almeno la metà dei richiedenti sale di due. L'etichetta **specifica il numero** ("Alta — 3 medici penalizzati") solo da Media in su (a Nessuna/Bassa mostra il solo livello); stesso criterio (< 25%) per correttivo e per il numero mostrato. Casi speciali: un solo richiedente → "Nessuna"; nessun richiedente → etichetta assente; tutti alla stessa soddisfazione, anche tutti a zero → "Nessuna" (nessuna disparità = nessuna iniquità percepita). Soglie (15/35/55/75 e il 25%) raccolte in `INIQUITA_SOGLIE` / `INIQUITA_SOGLIA_RISENTIMENTO` in cima al COMPONENTE, facilmente tarabili (da tarare sul campo dopo qualche mese reale). Verificato: logica di X confrontata con l'output reale di `elaboraSchema` su scenari controllati, mappatura etichetta+correttivo pesato su ~18 casi sintetici (2/3/5 medici), rendering reale in browser (Playwright) senza errori console; 189/189 unit test invariati (motore intatto, `engine_test.mjs` non rigenerato, `docs/app.jsx` in sync)

---

## 7. FEATURE NON IMPLEMENTATE / POSSIBILI FUTURI

- Integrazione con graduatoria reale definitiva (attualmente lista simulata).
- Titolarità di sede reali per i determinati (attualmente tutte `null` nei dati simulati).
- Export del Progetto Claude per elaborare email di disponibilità → vedi prompt separato.

**Risolto nella revisione verde/blu:** lo scenario "1 medico con sede preferita SP" (il motore forzava sempre il target su Maniago) è stato corretto — ora con 1 solo medico il target fisico è dinamico e segue la sua migliore preferenza verde.

---

## 8. SUITE DI TEST (Node.js, usa engine_test.mjs estratto)

Tutti i file di test usano `import` da `./engine_test.mjs`. Devono girare **tutti verdi** dopo qualsiasi modifica al motore.

```bash
# Estrai motore (da fare dopo ogni modifica al file .jsx)
python3 -c "
src = open('turni-guardia-medica.jsx').read()
end = src.index('// ============ COMPONENTE ============')
engine = src[:end].replace('import { useState, useMemo, useRef, useEffect } from \"react\";\n', '')
open('engine_test.mjs', 'w').write(engine + '\nexport { MEDICI, MEDICI_DEFAULT, setMediciGlobal, byId, CAT_INFO, SEDI5, SEDI_BREVI, CDC, dk, mk, turniDelGiorno, espandiAmbito, costruisciEntryDispo, elaboraSchema, normDispo, ordinaPerLivello, MAX_LIV_VERDE, MAX_LIV_BLU, isDeterminato, isContrattualizzato, MESI_DISPONIBILI, MESI_IT, giorniTra, settimanaDi, capSettimanale, debitoOrdinarioIniziale, tettoDistribuzioneDi };\n')
"

# Lancia tutti i test
node run_tests2.mjs            # 48 test runtime (gerarchia, titolarità universale, scenari verde/blu, debito)
node test_rapido2.mjs          # 18 test inserimento rapido, menu a tendina e protezione NO
node test_livelli_verde_blu.mjs # 11 test livelli verde 1-5 e blu 1-4
node test_stesso_cat2.mjs      # 9 test conflitti stessa categoria
node test_nuove_funzioni.mjs   # 16 test livelli verde, titolarità e medici modificabili
node test_spaziatura_settimana.mjs  # 11 test tetto settimanale (§3.8); §3.7 rimossa, vedi intro del file
node test_categorie_12h.mjs    # 11 test DET12ASAP e DET12 (§3.1)
node test_preferenza_turno.mjs # 13 test preferenza di turno stesso giorno G/N (§3.9)
node test_turni_extra.mjs      # 7 test turni extra volontari oltre il monte ore (§3.10)
node test_max_turni_mese.mjs   # 7 test tetto mensile dichiarato dal coordinatore, tutte le categorie (§3.11)
node test_distribuzione_temporale.mjs  # 15 test distribuzione run-then-redistribute, priorità di sede, cross-livello, pool su tutto il mese OGNI volta che disponibilità > tetto (voce 48), slot obbligatori + ⚓ pin sede + MMG come punti fissi automatici (voce 49) (§3.11)
node test_aggiustamento_mensile.mjs  # 16 test aggiustamento mensile del monte ore DET38/DET24/DET12/DET12ASAP, totali annui (§3.11 punto 3)
node test_simulazione_completa.mjs  # ~25M check su scenari randomici (800 semi × 125 mesi, agosto 2026-dicembre 2036, con titolarità/turni extra/tetti mensili)
node test_simulazione_email.mjs     # simulazione leggibile di un mese intero (14 medici via "email")
node test_guardiano_titolarita.mjs  # 3 test guardiano titolarità §3.1a: avviso additivo su residuo titolare fuori sede (§10 voce 33)
node test_espansione_ambito.mjs     # 154 test espandiAmbito/diurniNascosti: espansione insiemi giorni (feriali/weekend/mese/intervallo/giorni-settimana) + diurno "a sorpresa" vs calcolo indipendente, 4 mesi-trappola (§10 voci 41, 43)
node test_dispo_set_equivalenza.mjs # 245 test dispo_set ≡ N dispo_aggiungi per-giorno + chiusura del cerchio su liste hardcodate (§10 voce 41)
node test_stato_azzera.mjs          # 7 test statoRealeMedico (stato reale dai dati) + azzeraDispoMedico (azzera atomico senza residui, caso Iengo) (§10 voce 44)
node test_tetto_settimana_ui.mjs    # 15 test enumerazione settimane + tetti per-settimana uniformi/divergenti + pallino festivo (voce 45) + settimana a cavallo V-A: dichiarato/effettivo, ricalcolo luglio (voce 46 PASSO 1)
node test_cavallo_distribuzione.mjs # 5 test distribuzione sopra-tetto con riferimento alla settimana a cavallo: turni tenuti si allontanano da luglio, tetto invariato, chi-vince invariato, path di default identico, nessun effetto sotto-tetto (voce 46 PASSO 2)
node test_turno_precedente.mjs      # 10 test azione turno_precedente: registrazione/cancellazione in chat dei turni di fine luglio (settimana a cavallo), rete di sicurezza (cavolate 1-4), tetto effettivo ricalcolato, domanda diurno/notturno sui giorni con entrambi (voce 47)
```

**Il test di simulazione** (`test_simulazione_completa.mjs`) è il più importante: genera scenari casuali con tutti i 14 medici (incluse titolarità, turni extra e tetti mensili casuali) e verifica gli invarianti su ogni singolo turno. Invarianti (tutti vincoli RIGIDI, nessuna eccezione legittima → nessun falso positivo possibile): `INV1` (fisico solo su verde dichiarata), `INV2` (NO mai assegnato), `INV3` (copertura a distanza solo da un fisico presente, su sede blu dichiarata), max 1 sede a distanza per medico, `INV-TITOLARE` (titolare non scavalcato sulla propria sede, con esclusioni note §3.1a), `INV-MAXTURNI` (Max turni mese dichiarato mai superato), `INV-TETTO-IMPLICITO` (nessun contrattualizzato supera min(monte-ore-implicito, Max turni mese) — estende INV-MAXTURNI a chi non ha cap dichiarato, budget calcolato staticamente via `debitoOrdinarioIniziale`+`tettoDistribuzioneDi`, §10 voce 21), `INV-FISICO-UNICO` (un medico mai fisico in due sedi dello stesso turno), `INV-DETERMINISMO` (stesso input → stesso output, su un campione ~5%), `INV-TETTO-SETTIMANALE` (§3.8, censimento #11: per ogni medico e ogni settimana lun-dom, turni assegnati fisici+extra ≤ `capSettimanale` dichiarato; la copertura a distanza non conta — §10 voce 23), `INV-TERRITORIALE` (§3.2, §10 voce 31: se Claut è coperta a distanza il coprente è fisico a Maniago; se Anduins, a Spilimbergo o Meduno — vincolo geografico rigido). La generazione degli scenari esercita anche il tetto settimanale §3.8 (chiave `SETT:`) e la preferenza turno §3.9 (chiave `TURNOPREF:`), §10 voce 22, e le sedi verdi a LIVELLI DIVERSI nello stesso turno (§10 voce 24). **Verifica solo violazioni "vietate", MAI la qualità della distribuzione temporale** (§3.11, §10 voce 20 — quella è coperta dagli unit test deterministici). 0 violazioni reali su 100.000 scenari / 34.281.414 check. Nota su `INV-TITOLARE`: le violazioni che corrispondono ESATTAMENTE al limite noto §3.1a (catena FASE1 con **≥3 contrattualizzati** presenti) sono **tollerate ma RIPORTATE** (stampate come `ℹ️ TOLLERATO`, non fanno fallire — fixare la catena FASE1 causa regressioni peggiori, §10 voce 22); una violazione con ≤2 contrattualizzati non è quel pattern e resta un fallimento vero.

**Quando si aggiunge un test:** scrivilo in Node.js puro (ESM, `import`), con `process.exit(0/1)` e output `✅ TUTTI I TEST SUPERATI` o `❌ N FALLITI`. Aggiungilo al blocco `# Lancia tutti i test` sopra.

---

## 8bis. TEST AUTOMATICO DEL PROMPT AI (email simulate contro l'API reale)

A differenza della suite sopra (pura, deterministica, offline), questo harness testa il **prompt di sistema** di `chiediAI` (interpretazione email dei medici) chiamando davvero l'API Anthropic. È diviso in tre file indipendenti:

- **`test_email_generator.mjs`** — genera un corpus di 1478 email simulate in italiano su 39 categorie (sedi fisiche, copertura a distanza, indisponibilità/ferie, recupero ore in ore e in turni, turni extra in tutte le varianti, MMG/PLS attivo e non attivo, weekend ambiguo, notti esplicite, condizionali, contraddizioni, senza-incarico con richieste improprie, sede non identificabile, date vaghe, tetto settimanale, tetto mensile in tutte le varianti (§3.11 punto 1), preferenza turno), con un RNG seedato (mulberry32, seed fisso) per riproducibilità totale. Ogni caso ha un `atteso`: azioni che devono comparire, azioni che NON devono mai comparire (`azioniVietate`, usato per testare le regole di protezione come senza-incarico + recupero/turni-extra), domande Sì/No attese o vietate, avvisi 🔴 ATTENZIONE attesi. Girato da solo scrive `test_email_corpus.json` (non committato, vedi `.gitignore`).
- **`test_email_runner.mjs`** — **estrae il prompt "sys" direttamente dal sorgente** di `turni-guardia-medica.jsx` (stesso principio di sezionamento di `engine_test.mjs`, cercando il marker `const sys = \`` fino a `STATO ATTUALE: ${JSON.stringify(stato)}\`;`) e lo compila con `new Function`, così il test resta sempre sincronizzato col prompt reale senza copie manuali. Per ogni caso costruisce lo `stato` JSON esatto e chiama `api.anthropic.com/v1/messages` (richiede `ANTHROPIC_API_KEY` in env o in un `.env` locale MAI committato). Ha una **guardia di sicurezza**: oltre 20 casi richiede il flag esplicito `--yes` (altrimenti si ferma con un avviso di costo), più un flag `--dry-run` che valida solo il rendering dei prompt senza fare alcuna chiamata di rete. Supporto a `--limit N` (run di prova), `--concurrency N` (default 6), salvataggio incrementale ogni 50 casi. Scrive `test_email_results.json` (non committato).
  - **Modalità Message Batches API** (`--batch-submit`, `--batch-status <id>`, `--batch-fetch <id>`) — ~50% più economica dell'API sincrona, consigliata per corpus grandi. `--batch-submit` costruisce tutte le richieste (stessi prompt renderizzati dallo stesso `renderSys`), le invia in un'unica chiamata a `/v1/messages/batches` e salva lo stato locale (mapping custom_id → caso, necessario per ricostruire i risultati) in `test_email_batch_state.json` (non committato). Il batch è asincrono lato Anthropic: `--batch-status` interroga i conteggi senza costo, `--batch-fetch` scarica il risultato (JSONL da `results_url`) solo quando `processing_status === "ended"` e lo UNISCE per id nel file risultati esistente (preserva gli esiti già presenti per i casi non inclusi in quel batch). Flag `--retry-failed <file>` (usabile sia in modalità sincrona sia batch): filtra il corpus ai soli casi che in quel file di risultati avevano `erroreRete`, per ripetere a basso costo solo ciò che è fallito (es. per credito esaurito a metà run). **Guardia**: `--batch-status`/`--batch-fetch` senza un batch_id valido come argomento successivo generano un errore esplicito invece di ricadere silenziosamente sul flusso sincrono — durante lo sviluppo un id dimenticato ha fatto ripartire per un attimo il flusso normale (quasi un'altra run reale accidentale, vedi nota di sicurezza sotto).
- **`test_email_report.mjs`** — legge i risultati e verifica ogni caso con un matcher **strutturale/parziale** (non uguaglianza esatta sull'intero JSON): un'azione attesa deve comparire con i campi discriminanti giusti, un'azione vietata non deve mai comparire, domande/avvisi attesi devono essere presenti. Produce: % di successo globale, breakdown per categoria, pattern di errore ricorrenti (categoria × tipo errore, es. "azione_vietata", "domanda_mancante", "avviso_mancante"), suggerimenti euristici precompilati per le categorie più delicate, e in coda il dettaglio di ogni fallimento. Output sia a console sia su `test_email_report.md` (non committato). Flag opzionale `--ai-suggestions` (richiede anch'esso `ANTHROPIC_API_KEY`): fa una chiamata finale in più per suggerimenti in prosa scritti dall'AI a partire dai fallimenti reali, invece della sola euristica.

```bash
node test_email_generator.mjs                  # genera test_email_corpus.json (1478 casi)
node test_email_runner.mjs --dry-run            # verifica il rendering dei prompt, ZERO chiamate API
node test_email_runner.mjs --limit 20           # run di prova economica (sotto la soglia --yes)
node test_email_runner.mjs --yes                # run completa sull'intero corpus (a pagamento)
node test_email_report.mjs                      # report di verifica semantica + suggerimenti

# Modalità Message Batches API (~50% più economica), utile anche per ripetere solo i casi
# falliti (es. per credito esaurito a metà run precedente):
node test_email_runner.mjs --batch-submit --retry-failed test_email_results.json --yes
node test_email_runner.mjs --batch-status <batch_id>     # nessun costo, interroga i conteggi
node test_email_runner.mjs --batch-fetch <batch_id>      # solo a batch "ended": unisce i risultati
```

Nota di sicurezza: durante lo sviluppo di questo harness, importare `test_email_runner.mjs` senza controllarne l'entry point ha innescato una run reale accidentale — per questo il file ha una guardia esplicita (`main()` parte solo se eseguito come CLI diretta, mai se importato come modulo) e la soglia `--yes` sopra descritta.

**DECISIONE — accorciamento del prompt AI (luglio 2026): NON accorciare per ora.** Il prompt (`const sys` in `chiediAI`, ~14.400 parole) è in gran parte "cicatriziale" (difese anti-bug reali): il margine sicuro è piccolo e il rischio alto e **non verificabile senza credito API**. **Principio guida:** preferire SEMPRE un **principio consolidante** (es. `REGOLA GENERALE: AZIONE vs CONTESTO`, §10 voce 47) agli esempi sparsi — la domanda giusta è la **coerenza/ordine**, non la brevità.

---

## 9. COME VERIFICARE CHE NON HAI ROTTO NIENTE

```bash
# 1. Controlla compilazione (solo errori reali, ignora type inference)
cd /tmp && cp ../turni-guardia-medica.jsx check.tsx
npx tsc --jsx preserve --noEmit --allowJs check.tsx 2>&1 | grep -E "error TS(1[0-9]{3}|2304|2339|2552|2454)[^0-9]"
# output vuoto = ok

# 2. Verifica nessuna funzione duplicata
for fn in setSedeOpzione setNoCella setPreferitoSede setTurnoPref toggleExtra elabora azzeraMese \
  setMedici aggiornaMedico aggiungiMedico rimuoviMedico setSlot applicaRapido \
  applicaProposta applicaAzioni riepilogoDi rispondiDomanda nomeToId elaboraSchema elaboraTurno normDispo \
  ordinaPerLivello isDeterminato setMediciGlobal giorniTra settimanaDi capSettimanale \
  turnoPrefDi candidatiOrdinati oreAssegnateDi scalaDebito chiediAI estraiJsonBilanciato \
  pasquaDi festiviFissiDi capMensileDi tettoDistribuzioneDi scegliIndiciEquidistanti \
  scegliConRiferimento livelloVintoDi debitoOrdinarioIniziale correggiTitolarita; do
  n=$(grep -c "const $fn = \|function $fn(" turni-guardia-medica.jsx)
  [ "$n" != "1" ] && echo "DUPLICATA: $fn"
done

# 3. Estrai motore e lancia tutti i test
python3 -c "..."  # vedi sopra
node run_tests2.mjs && node test_rapido2.mjs && \
  node test_livelli_verde_blu.mjs && node test_stesso_cat2.mjs && \
  node test_nuove_funzioni.mjs && node test_spaziatura_settimana.mjs && \
  node test_categorie_12h.mjs && node test_preferenza_turno.mjs && node test_turni_extra.mjs && \
  node test_max_turni_mese.mjs && node test_distribuzione_temporale.mjs && \
  node test_aggiustamento_mensile.mjs && \
  node test_simulazione_completa.mjs && node test_simulazione_email.mjs

# 4. Se si tocca turni-guardia-medica.jsx, rigenera anche docs/app.jsx (copia GitHub Pages) —
#    vedi §14 per le 2 modifiche minime da riapplicare dopo la copia.
```

---

## 10. BUG NOTI E STORICI (risolti)

Questi bug sono stati trovati e corretti durante lo sviluppo. Se riappaiono è una regressione.

1. **Phantom slot bug** — la ricollocazione ricorsiva lasciava un "fantasma" in `slots` non corrispondente a `sedeDi`. Fix: rebuild di `slots` da `sedeDi` dopo tutti i passaggi.

2. **delete sedeDi[occ] prematuro** — in `provaFisica()`, il `delete sedeDi[occ]` avveniva dopo la ricollocazione riuscita, cancellando la nuova sede dell'occupante. Fix: `provaFisica()` usa `maxLiv` per permettere/negare la ricollocazione, e gestisce correttamente il ripristino.

3. **normDispo con oggetti residui di formati legacy** — rompeva silenziosamente il motore. Fix: `normDispo` normalizza sempre a `{verde, verdeLiv, blu, bluLiv, no, preferito}`.

4. **Meduno a distanza usava il solo grad invece della gerarchia completa** (bug storico del vecchio sistema automatico, non più applicabile: nel sistema verde/blu ogni copertura a distanza è dichiarativa e il conflitto usa sempre categoria→titolarità→debito→graduatoria).

5. **Livello blu oltre il cap (5) su un elenco max 4** — `ordinaPerLivello` con `maxLivello=4` ignora silenziosamente un livello 5 mai raggiunto dal ciclo `for l=1..maxLivello`: comportamento corretto e verificato da test dedicato, ma da tenere a mente se si costruiscono dati di test blu manualmente (livelli validi: 1-4, non 1-5 come per verde).

6. **Spaziatura temporale ignora il bucket del candidato (introdotto e corretto durante la v1 — a 3 bucket — della distribuzione temporale, §3.11; oggi in parte superato dalla v2)** — la regola di spaziatura (§3.7) sceglieva l'"alternativa" a cui cedere la sede controllando solo che non avesse già vinto nulla quel turno, non il suo bucket di priorità: un contrattualizzato demosso (bucket 2, fuori dai propri giorni riservati) poteva scavalcare il vincitore bucket 1 (o bucket 0) semplicemente perché quest'ultimo "aveva lavorato ieri", vanificando sia la distribuzione sia — nel caso peggiore — la titolarità. Fix v1: l'alternativa era ammessa solo se `bucketOf(alternativa) <= bucketOf(vincitore attuale)`. Con la v2 (bucket tornati a 2: solo con-debito/senza-incarico, nessun bucket 2 da confrontare) quel controllo di parità di bucket non serve più ed è stato rimosso — ma la parte sulla titolarità è rimasta, perché è un fix indipendente e tuttora necessario: se il vincitore attuale è titolare della sede contesa, l'alternativa deve ESSA STESSA essere titolare della stessa sede.

7. **Titolare mai invitato a contestare la propria sede se `ordinati` si esaurisce prima del suo turno** — nel ciclo FASE1 di `elaboraTurno`, l'iterazione su `ordinati` si fermava non appena tutti i target fisici erano occupati (ottimizzazione presente da sempre). Poiché `ordinati` è ordinato per bucket/categoria/debito/graduatoria e MAI per titolarità, un titolare poteva comparire più avanti nell'elenco di un non titolare che aveva già occupato la sua sede — e non riceveva mai la possibilità di contestarla. Fix: il ciclo itera SEMPRE tutti i candidati di `ordinati` (`provaFisica` è comunque un no-op sicuro per chi non ha una pretesa reale).

8. **3+ determinati su sedi sovrapposte (fix mirato, non generico)** — anche dopo i fix 6 e 7, in scenari con 3+ determinati che si contendono più sedi sovrapposte nello stesso turno, catene di ricollocazione ricorsiva profonde in `provaFisica` potevano raramente convergere a un equilibrio in cui un titolare finiva fisicamente altrove pur avendone diritto, senza che nessun singolo passaggio della catena fosse isolatamente scorretto. Un primo tentativo di correzione GENERICO (ripetere l'intero ciclo FASE1 più volte per far convergere lo stato) è stato **scartato**: introduceva regressioni reali (perdita di copertura, sedi che restavano scoperte pur avendo un candidato disponibile) in scenari altrove già corretti — la ricollocazione ricorsiva non è idempotente su stati già stabili, e ripetere l'intero ciclo perturba anche sistemazioni per indifferenza non correlate alla titolarità. Risolto con una funzione dedicata `correggiTitolarita()`, MIRATA solo ai titolari (mai a candidati generici, tocca solo la loro sede specifica): per ogni titolare non ancora sulla propria sede dichiarata come prima preferenza oggi, se l'occupante attuale non ha davvero priorità superiore (`isBetterPriority`), lo scambia dentro e ridà all'occupante spostato una possibilità di ricollocarsi tramite lo stesso `provaFisica`. Eseguita in DUE punti: subito dopo FASE1, e di nuovo dopo la spaziatura temporale (§3.7) — che può essa stessa reintrodurre il problema spostando un occupante non-titolare (legittimo, es. un INDET) e rimpiazzandolo con un candidato senza alcun diritto sulla sede. Verificato dalla simulazione massiva (`test_simulazione_completa.mjs`, invariante `INV-TITOLARE`): **0 violazioni su 100.000 scenari / 25.151.195 check**, nessuna regressione sui 12 file di test dedicati.

9. **Distribuzione temporale v1 (precalcolo + 3 bucket) sovrascritta dalla gerarchia — riscritta come v2 run-then-redistribute (§3.11 punto 2)** — la v1 precalcolava (`calcolaRiservati`, RIMOSSA) un sottoinsieme di slot "riservati" dalla sola disponibilità DICHIARATA, prima di eseguire qualunque turno, e usava un bucket 2 dedicato per demuovere il medico fuori dai suoi giorni riservati. Segnalato dal coordinatore: la gerarchia normale (titolarità, spaziatura, ricollocazioni) poteva sovrascrivere quelle riserve durante l'elaborazione reale, facendo concentrare i turni invece di distribuirli — il precalcolo non rifletteva ciò che il medico avrebbe VERAMENTE vinto. Riscritta come meccanismo a due passaggi completi: passaggio 1 (oracolo, gerarchia pura, nessun tetto) scopre quali turni ogni medico vince EFFETTIVAMENTE; solo chi supera il proprio tetto cede l'eccesso (scelto per livello di sede — assoluto sull'equidistanza — poi equidistanza dentro il livello) al passaggio 2, che rifà l'intero mese da zero applicando le cessioni più un controllo live del tetto per chiunque lo raggiunga durante questo stesso passaggio (non solo chi era stato segnalato dal passaggio 1: le esclusioni cambiano le dinamiche del mese). Bucket tornati a 2 (con-debito/senza-incarico), `meseCount` sostituito da `contoMensile` (vivo solo nel passaggio 2). Conseguenza nota e accettata (non un difetto): poiché il pool di partenza sono vittorie EFFETTIVE — non disponibilità dichiarata — quando il tetto implicito coincide con le vittorie naturali (il caso più comune, nessun Max turni mese esplicito più restrittivo) non c'è nulla da cedere e la distribuzione non ha alcun effetto pratico; il pattern osservato in quei casi era, all'epoca, in parte prodotto dalla spaziatura temporale §3.7 allora ancora presente (RIMOSSA nella voce successiva) — oggi lo stesso scenario produce semplicemente i primi N giorni consecutivi, vedi voce 10. Verificato dalla simulazione massiva (`INV-MAXTURNI`): **0 violazioni su 100.000 scenari / 25.163.865 check**, nessuna regressione sui file di test dedicati.

10. **Spaziatura temporale (§3.7) rimossa interamente** — la regola per cui un medico che aveva lavorato il giorno prima (o lo stesso giorno, su un altro turno) cedeva la sede a un'alternativa disponibile è stata rimossa completamente su richiesta esplicita: un medico che vince un turno per gerarchia (titolarità → categoria → debito → graduatoria) ha diritto a quel turno, e non deve essere ridistribuito a chi sta sotto in graduatoria solo per distanziare le sue presenze, anche a giorni consecutivi. Restano intatti e verificati indipendenti: la distribuzione temporale VERA (§3.11, l'unico meccanismo che può ancora far cedere un turno vinto, in modo esplicito e dichiarato) e la preferenza di turno (§3.9, verificata riga per riga per funzionare in modo autosufficiente dall'esito finale — chi vince FISICAMENTE sia G che N lo stesso giorno — indipendentemente da come il doppio turno sia stato ottenuto, mai da §3.7). `ultimoFisico` (stato usato solo da questa regola) rimosso interamente; `giorniTra` (utility di date generica) mantenuto, pur non più chiamato dal motore. `correggiTitolarita()` resta chiamata due volte in `elaboraTurno` (la sua doppia chiamata non dipendeva in realtà dalla logica di spaziatura in sé, ma da un rebuild difensivo di `slots` — vedi §5) — nessuna regressione: verificato su tutti i file di test dedicati e sulla simulazione massiva (100.000 scenari / 25.179.596 check, 0 violazioni).

11. **Selezione cross-livello nella distribuzione post-elaborazione (§3.11 punto 2b)** — quando un medico supera il proprio tetto su PIÙ livelli di sede verde (es. livello 1 riempito per intero, livello 2 ancora da ridurre), la selezione del sottoinsieme di livello 2 da tenere usava `scegliIndiciEquidistanti` guardando SOLO alla posizione dei candidati di livello 2 tra loro, ignorando dove fossero già stati fissati i giorni di livello 1: poteva scegliere un giorno di livello 2 vicinissimo a un giorno di livello 1 già tenuto, quando un altro candidato di livello 2 (magari meno "centrale" tra i soli candidati di livello 2) si sarebbe incastrato meglio. Aggiunta `scegliConRiferimento(candidati, n, giorniFissi)`: una selezione greedy "farthest-point" che, quando esistono giorni già fissati da livelli migliori, sceglie ad ogni passo il candidato con la distanza minima più alta possibile da TUTTI i giorni di riferimento raccolti finora (giorni fissi + proprie scelte precedenti, che si aggiungono al riferimento via via) — il livello 1 (o qualunque primo livello ridotto senza riferimento) continua a usare la pura equidistanza posizionale, invariata. La priorità di sede resta assoluta: un livello viene anche solo toccato dopo che tutti i livelli migliori sono stati riempiti per intero, questa selezione cambia SOLO quali giorni specifici di un livello già "in coda" vengono scelti. Verificato con un test dedicato (`test_distribuzione_temporale.mjs`) che dimostra la differenza concreta rispetto alla pura equidistanza posizionale, e sulla simulazione massiva: **0 violazioni su 100.000 scenari / 25.179.578 check**, nessuna regressione sui file di test dedicati.

12. **2 test obsoleti in `run_tests2.mjs` su "esauriti" — non un bug del motore, test scritti prima del blocco rigido (§3.4)** — "fascia 3 (esauriti): competono solo per grad tra loro" e "un esaurito copre comunque un turno se non c'è nessun altro candidato" davano a un contrattualizzato debito 0 (via `extraOre` negativo) SENZA alcun turno extra dichiarato, aspettandosi che restasse comunque candidato. Il blocco rigido oltre il monte ore (voce implicita in `candidatiOrdinati`, §3.4 — CONTEXT.md punto 23 in §6) esclude ESPLICITAMENTE dai candidati chi ha esaurito SIA il debito ordinario SIA i turni extra (`debiti[m.id] <= 0 && (debitiExtra[m.id] || 0) <= 0`), introdotto DOPO questi 2 test e mai retroapplicato a loro: il comportamento reale (slot scoperto) era già corretto e già verificato altrove (`test_stesso_cat2.mjs`, "blocco rigido oltre il monte ore"), erano i 2 test a essere scollegati dalla regola. **Regola corretta, verificata e non modificata nel motore**: un esaurito (debito ordinario 0) copre un turno anche in assenza di alternative SOLO se ha turni extra dichiarati (`turniExtra > 0`, §3.10) — senza turni extra dichiarati resta escluso ed il turno resta SCOPERTO, mai assegnato oltre il limite. Fix: i 2 test aggiornati per dichiarare `turniExtra` quando lo scenario richiede che l'esaurito resti candidato, più un nuovo test esplicito che conferma il caso SENZA turni extra (slot scoperto, `null`). Nessuna modifica al motore — verificato su tutta la suite (47/47 in `run_tests2.mjs`) e sulla simulazione massiva: 0 violazioni su 100.000 scenari.

13. **Titolarità di sede resa OBBLIGATORIA e universale per ogni contrattualizzato (§3.1a), non più solo tra determinati** — su richiesta esplicita, la titolarità (`sedeContratto`, Maniago o Spilimbergo) è ora obbligatoria per QUALSIASI contrattualizzato (INDET incluso), non più un concetto riservato ai soli determinati (DET36/DET24/DET12ASAP/DET12). Aggiunta `isContrattualizzato(mid)` (`CAT_INFO[cat].ore !== null`, include INDET) come funzione rilevante per la titolarità — `isDeterminato` resta distinta e invariata, usata altrove. `isTitolareDi` e la condizione in `isBetterPriority` (che prima era `isDeterminato(aId) && isDeterminato(bId)`) ora usano `isContrattualizzato`: su qualunque sede contesa, chi ne è titolare vince prima ancora del confronto di categoria, anche contro un INDET (categoria nominalmente più alta in assoluto) — a parità di titolarità sulla sede contesa (entrambi titolari, o nessuno dei due) decide normalmente categoria → debito → graduatoria. **MEDICI_DEFAULT sostituito interamente** con 14 medici reali (rimossi i precedenti 26 dati di test), tutti i contrattualizzati con una titolarità reale assegnata (nessun `sedeContratto: null` — l'unico INDET, BERTUZZI, ha grad666 deliberatamente pessimo per perdere ogni spareggio di grad/categoria, titolare di Spilimbergo). UI: colonna "Titolarità" ora mostrata per ogni contrattualizzato (non solo determinati), senza opzione "Nessuna"; il cambio di categoria verso/da SENZA aggiorna automaticamente `sedeContratto` (default "Maniago" verso un contrattualizzato, azzerato verso SENZA); il form "Aggiungi nuovo medico" include lo stesso selettore di titolarità. Aggiornato anche il prompt AI (gerarchia categorie + sezione titolarità). Riscritti tutti i 13 file di test unitari del motore per usare i nuovi 14 medici (i "senza incarico" necessari nei test, assenti di default, si ottengono per override temporaneo di categoria — stesso pattern già usato per DET12/DET12ASAP). Verificato su tutta la suite (174 test) e sulla simulazione massiva: **0 violazioni su 100.000 scenari / 25.238.128 check**.

14. **`test_email_generator.mjs` reso robusto all'assenza di medici senza incarico** — con 0 medici SENZA nella nuova lista di default (voce 13), i 5 punti che pescavano da `pick(senzaIncarico)` (categorie `senza_incarico_recupero`, `senza_incarico_turni_extra`, `senza_incarico_no_numero_guardie`, `senza_incarico_con_numero_guardie`, `tetto_mese_senza_incarico`) andavano in eccezione (`Cannot read properties of undefined`). Fix: ogni categoria ora controlla `senzaIncarico.length` e salta silenziosamente i propri casi se vuoto (`times(N, () => { if (!senzaIncarico.length) return; ... })`), invece di creare un override sintetico locale: un override qui non si rifletterebbe in `test_email_runner.mjs`, che costruisce `stato.medici` per l'AI leggendo le categorie REALI da `MEDICI_DEFAULT` — produrrebbe casi falsati (l'AI vedrebbe la categoria vera del medico, non SENZA), non semplicemente assenti. Corpus passato da 36 a 31 categorie con la lista attuale (0 medici SENZA); tornerà a generare quelle 5 categorie automaticamente se in futuro un medico verrà impostato come SENZA incarico. Verificato: `node test_email_generator.mjs` non va più in eccezione.
15. **Categoria DET36 rinominata in DET38** — il contratto da 36h/sett non esiste più, sostituito da 38h/sett (il contratto 24h e quello da 12h restano invariati). In `CAT_INFO`: `DET36` → `DET38`, label `"Det. 38h"`, monte ore `156h` → `168h` (= 14 turni base invece di 13), stessi `prio: 2` e stessi colori di prima. Aggiustamento mensile (§3.11 punto 3, stesso meccanismo di DET24/DET12): DET38 ora perde `12h` (168h→156h, 14→13 turni impliciti) a Febbraio, Aprile, Settembre — negli altri 9 mesi resta a 168h/14 turni. Totale annuo: **165 turni/anno** (9×14 + 3×13), verificato sia analiticamente sia via `test_aggiustamento_mensile.mjs`. Rinominato ovunque: i 5 medici DET36 di default (ZURLO, FOSCHIANI, BEKAEVA, CERVESATO, IENGO) diventano DET38, `isDeterminato`, prompt AI (sezioni gerarchia categorie, titolarità di sede, framework debito orario), tutti i file di test (rename meccanico via `sed` in 11 file). In 3 file il semplice rename testuale non bastava perché il valore delle ore di base è cambiato (156h→168h) e DET38 ha ora un proprio aggiustamento mensile diverso da quello (nullo) del vecchio DET36: `test_turni_extra.mjs` (valore `extraOre` di CERVESATO ricalcolato da `-156` a `-168` per azzerare esattamente il debito), `test_distribuzione_temporale.mjs` (il test "titolare di sede" usa Agosto, mese NON aggiustato per DET38 — il tetto corretto è 168h/14 turni, non più 156h/13 come per il vecchio DET36 fisso), `test_aggiustamento_mensile.mjs` (riscritto: 4 nuovi test dedicati a DET38 — 14 turni a Gennaio, 13 a Febbraio, 13 ad Aprile/Settembre, 14 negli altri 9 mesi — più un nuovo test sul totale annuo 165; il vecchio test combinato INDET+DET36 "nessun aggiustamento" è stato scisso, dato che ora vale solo per INDET). Suite completa rilanciata dopo il rename: 188 test unitari su 13 file, tutti verdi. Simulazione da 100.000 scenari (`test_simulazione_completa.mjs`) rilanciata per intero: **0 violazioni su 25.238.607 check**.
16. **MEDICI_DEFAULT sostituito con una nuova lista di 14 medici: 8 titolari determinati + 1 INDET fuori graduatoria + 5 senza incarico** — su richiesta esplicita, ristruttura completa della composizione (grad e id invariati come valori, ma redistribuiti): PRESSACCO, CERVESATO, DE CANDIDO, MERLINO e IENGO (in precedenza tutti determinati) diventano SENZA incarico (nessun contratto preso), reintroducendo la categoria SENZA nei dati di default dopo che la voce 13 l'aveva azzerata. BEKAEVA passa da DET38 a DET24 (resta titolare di Maniago). Nuova distribuzione: Maniago ha 5 titolari (ZURLO/DET38, TRIGODKO/PITAU/BEKAEVA tutti DET24, MORANO/DET12); Spilimbergo ha 3 titolari determinati (FOSCHIANI/DET38, MARTINETTI/DET24, VALERI/DET12ASAP) più l'unico INDET (BERTUZZI, titolare Spilimbergo). Corretta anche la graduatoria di BERTUZZI da 666 a 108 (resta deliberatamente la peggiore dei 14, fuori graduatoria ufficiale, ma con un valore meno vistoso). Nuovi id 1-14 riassegnati in base alla nuova disposizione (vedi tabella §4).
    Riadattati tutti i 13 file di test unitari del motore: dove un test usava uno dei 5 medici ora-SENZA come determinato per isolare un confronto puro di categoria/debito/grad (tipicamente coppie entrambe titolari della STESSA sede), è stato "resuscitato" nel suo ruolo storico (categoria+titolarità originali, grad invariato) tramite il nuovo helper condiviso `comeStorico(lista, ...id)` + tabella `RUOLO_STORICO` (`test_utils.mjs`) — non un override sintetico ad-hoc per ogni file, ma una mappa unica e riusabile. Dove invece un test usava uno di questi medici semplicemente come backup "senza incarico" generico (es. `test_max_turni_mese.mjs`, `test_distribuzione_temporale.mjs`, `test_aggiustamento_mensile.mjs`), l'override artificiale è stato rimosso del tutto in favore dei medici SENZA nativi (PRESSACCO/IENGO), semplificando quei file. In `test_aggiustamento_mensile.mjs` la promozione manuale di MERLINO a DET12/DET12ASAP non serve più: i 4 titolari nativi (ZURLO/DET38, TRIGODKO/DET24, MORANO/DET12, VALERI/DET12ASAP) coprono già tutte e 4 le categorie aggiustabili.
    `test_email_generator.mjs`: aggiornato il commento sul pool `senzaIncarico` (non più vuoto: 5 medici), corpus tornato a generare le 5 categorie `senza_incarico_*`/`tetto_mese_senza_incarico` (36 categorie totali, da 31). Nessuna modifica al motore stesso (`turni-guardia-medica.jsx`): solo dati (`MEDICI_DEFAULT`) e un paio di esempi del prompt AI (`ore_extra`/`turni_extra`) che usavano PRESSACCO come esempio di medico contrattualizzato, ora sostituiti con MARTINETTI (rimasto determinato). Verificato: 188 test unitari su 13 file tutti verdi, `test_email_generator.mjs` genera correttamente i casi senza incarico, simulazione da 100.000 scenari rilanciata per intero: **0 violazioni su 24.933.908 check**.
17. **Pulizia generale (solo file scratch, commenti e documentazione — nessuna modifica al motore né ai test)** — scansione mirata dopo la stabilizzazione di categorie/contratti/lista medici. (a) Rimossi dal disco 3 file scratch già `.gitignore`-d e mai committati (`test_email_results.json`, `test_email_batch_state.json`, `test_email_report.md`) — residui dell'ultima run batch email, rigenerabili, unico posto dove sopravvivevano ancora i vecchi `DET36` e i vecchi nomi di test (GHIZZO, PARRONI, COLOSETTI, DE MARCHI L). (b) Corretto in `test_simulazione_email.mjs` il commento d'intestazione "26 medici ipotetici" → "i 14 medici" (il codice girava già su `MEDICI.length`, era solo il commento a essere obsoleto). (c) In CONTEXT.md (§8): aggiunto `test_aggiustamento_mensile.mjs` al blocco one-liner "Lancia tutti i test" (prima lo ometteva) e aggiornati i conteggi del corpus email da "1420 casi / 34 categorie" a "1450 / 36" (le 5 categorie `senza_incarico_*` sono tornate con i medici SENZA della voce 16). **Deliberatamente NON toccati** (decisioni esplicite): `giorniTra` (utility di date non più chiamata dal motore ma tenuta apposta, §10 voce 10 — resta con il suo unico test in `test_spaziatura_settimana.mjs`); il nome-segnaposto `WANG` negli esempi del prompt AI (verrà sistemato insieme al prossimo aggiornamento del prompt, per toccarlo una volta sola); le voci storiche §6 (descrivono correttamente il passato: DET36, "26 medici", grad666, "36→31" restano dove sono). Verificato che la pulizia non ha alcun effetto sul comportamento: 188 test unitari verdi + simulazione da 100.000 scenari **0 violazioni su 24.933.908 check** (conteggio identico alla run della voce 16, a conferma che il motore è invariato).
18. **Restyling grafico — passo 1: solo la cornice comune (nessuna modifica alla logica)** — primo intervento di un restyling "moderno pulito" da fare una schermata alla volta. Questo passo tocca ESCLUSIVAMENTE stile/colori/spaziature della cornice condivisa da tutte le schermate: header, i 4 tab di navigazione e la palette generale (sfondo, testi, bordi). Introdotta una costante palette `T` (nel corpo del componente `App`, accanto a `btn`): verde principale `#1c8066` (versione moderna e meno cupa del vecchio `#12312a`), testo `#2c3733` (grigio-verde leggibile invece del quasi-nero `#22252a`), bordi morbidi `#e5e9e6`, sfondo `#f5f7f6`, verde tenue `#e7f3ef` per il tab attivo — riutilizzabile dalle prossime schermate. Applicata a: container radice (sfondo/testo/font-stack ampliata con fallback `-apple-system`/`Segoe UI`/`Roboto`), header (nuovo verde + padding più ampio + ombra sottile in basso, pulsante "Assistente AI" allineato al nuovo verde), strip dei tab (bordo più morbido, `gap`, padding verticale), i 4 tab (da underline netto 3px a **pill arrotondate**: attivo = sfondo verde tenue + testo verde scuro bold, inattivo = trasparente + grigio, transizione morbida). **NON toccati** in questo passo (staging deliberato, verranno allineati schermata per schermata): lo stile condiviso `btn` e i pulsanti azione della strip (Annulla/Ripeti/Elabora/Esporta), il contenuto delle 4 schermate, il pannello AI interno, tabelle e popup. Zero modifiche al motore/logica/stato (la sezione MOTORE è intatta, `engine_test.mjs` non rigenerato): 188/188 test unitari verdi. `docs/app.jsx` rigenerato e in sync.
19. **Restyling grafico — passo 2: tutte le schermate (nessuna modifica alla logica)** — completa il restyling applicando lo stile moderno a tutto il contenuto (Disponibilità, MMG/PLS, Medici, Schema turni, pannello AI, popup, pulsanti azione). La palette `T` è stata estesa con token semantici (`surface`, `surfaceAlt`, `textMuted`/`textFaint`, `border`/`borderStrong`, `divider`, `danger`/`dangerBg`/`dangerBorder` per i rossi tenui "non disponibile/scoperto", `blu`/`bluDark`/`bluTint` per la copertura a distanza) e `btn` è stato allineato ad essa (bordo morbido, radius 8), con nuovi helper `btnPrimary`/`card`. I colori sono stati applicati a TUTTA la sezione di render tramite uno script mirato che opera solo dopo la definizione di `card` (protegge automaticamente `CAT_INFO` nel motore e le costanti-colore dell'export Excel, entrambe più in alto): i literal quotati `"#vecchio"` sono diventati riferimenti `T.token`, quelli embedded in stringhe CSS (`"1px solid #vecchio"`) sono passati al nuovo valore hex — nessun cambiamento di struttura o comportamento, solo valori di colore. Modifiche strutturali mirate: badge-pill di categoria coi colori di `CAT_INFO` (colonna medico in Disponibilità e select categoria in Medici), `SCOPERTO` reso una pill rosso-tenue nello Schema. **I colori delle categorie restano ESCLUSIVAMENTE quelli di `CAT_INFO`** (letti sempre via `CAT_INFO[...]`, mai riscritti come literal — verificato). Zero modifiche a motore/assegnazione/calcoli/stato: **188/188 test unitari verdi**, rendering dell'app reale (docs/) verificato con screenshot su tutte le schermate senza errori console, `engine_test.mjs` non rigenerato (MOTORE intatto), `docs/app.jsx` rigenerato e in sync.
20. **Distribuzione temporale §3.11 — turni ammucchiati all'inizio del mese con Max turni mese esplicito (bug reale da collaudo, corretto)** — trovato in collaudo: un DET24 disponibile TUTTE le notti di agosto con Max turni mese 4 otteneva i giorni 1,2,4,7 (ammucchiati nella prima settimana) invece di ~4,12,20,28 sparsi. **Causa** (confermata sul codice): la distribuzione (passaggio 2) sceglieva il sottoinsieme equidistante tra i turni EFFETTIVAMENTE vinti nel passaggio 1 (`vintiDi`), ma il passaggio 1 applica il blocco rigido del monte ore (§3.4): un contrattualizzato disponibile su gran parte del mese esaurisce le ore nei primi giorni, quindi "vince" solo turni iniziali — l'equidistante su quel pool ristretto li teneva ammucchiati. `scegliIndiciEquidistanti` era corretto; il **pool** era sbagliato (finestra del monte ore, non del mese). **Correzione** (Opzione A, discussa e approvata): quando morde un Max turni mese **ESPLICITO più restrittivo del monte ore** (`cap != null && cap < turni-impliciti`), il pool viene ricalcolato con un **oracolo per-medico che esenta SOLO quel medico dal blocco monte ore** (nuovo parametro `esente` propagato a `candidatiOrdinati`/`elaboraTurno`/`eseguiMese`): così "vince" tutti i turni di cui è legittimo vincitore per gerarchia su TUTTO il mese, e l'equidistante li sparge (→ ~1,10,22,31). Il passaggio 2 (blocco monte ore + tetto rigido live) è INVARIATO — l'oracolo serve solo a costruire il pool; nessun rischio di superare monte ore o tetto (cap turni ≤ (implicito−1) < monte ore). **Gate ristretto**: solo contrattualizzati (i senza incarico non si esauriscono, pool già completo); nessun cap esplicito o cap ≥ monte ore → comportamento invariato (`poolDi = vintiDi`, il caso "solo monte ore che morde" resta com'era, voce 9). `vintiDi` rinominato `poolDi`, aggiunto helper `estraiVinti`. **Perché i test non l'avevano preso**: (a) `test_distribuzione_temporale.mjs` CODIFICAVA il bug — il vecchio test "Max turni mese 3" asseriva `[1,5,8]` (ammucchiato) come corretto; aggiornato a `[1,16,31]` (sparso) con commento sul perché, + nuovo test di regressione dedicato al caso DET24-tutto-il-mese `[1,10,22,31]`. (b) La simulazione da 100.000 scenari controllava SOLO violazioni vietate (INV-MAXTURNI = tetto mai superato, INV1/2/3, INV-TITOLARE), MAI la qualità della distribuzione.
    **Invariante di distribuzione nella sim: TENTATO e RIMOSSO (memoria del perché non c'è).** Si è provato ad aggiungere `INV-DISTRIBUZIONE`, un controllo che, per un contrattualizzato con cap esplicito che morde, "isolato" sulle proprie sedi, pretendeva turni tenuti sparsi (campata ≥ 0.4 × campata disponibilità). Alla prova sui 100.000 scenari ha prodotto **33 falsi positivi, zero bug veri** — rimosso. Motivo di fondo (investigato caso per caso): in uno scenario random denso l'ammucchiamento **legittimo** è indistinguibile dal bug con un controllo a posteriori sullo schema finale, per **tre** fonti indipendenti che nessun gate riesce a separare: (1) **cap piccolo** — con cap 1-2 i turni tenuti sono pochissimi e la loro campata è naturalmente ~0; (2) **competizione al tempo-oracolo** — i competitori modellano il pool nel passaggio 1 e poi si esauriscono, lasciando gli slot SCOPERTI nel finale, quindi il check crede il medico "isolato" quando NON lo è (verificato: rieseguendo lo stesso scenario con solo quel medico, i turni si sparpagliano); (3) **priorità di sede assoluta** (§3.11) — i turni tenuti si concentrano nella finestra dei livelli-verdi migliori, che può essere stretta. Un invariante robusto dovrebbe conoscere il pool dell'oracolo e i livelli di sede, cioè **ri-eseguire la logica del motore dentro il test** (motore-nel-motore): ma un test deve conoscere la risposta giusta per una via INDIPENDENTE da ciò che testa, e nella sim densa quella risposta non è calcolabile senza rifare il motore. Perciò la qualità della distribuzione è coperta dai **due unit test deterministici** (caso isolato, single-livello, cap ≥ 3 — senza i confondenti competizione/livelli), il posto corretto; nella sim resta una NOTA che spiega perché l'invariante non c'è, così nessuno lo rimette in futuro pensando sia una dimenticanza. Verificato: **189/189 unit test verdi**; simulazione da 100.000 scenari (soli invarianti robusti): **0 violazioni su 24.936.217 check**.
21. **Censimento della copertura + 3 nuovi invarianti "puliti" nella simulazione** — dopo la lezione della voce 20 (la sim generava molti scenari ma controllava poche proprietà, per questo il bug §3.11 era sfuggito), si è fatto un censimento di TUTTE le regole del motore distinguendo: (a) violazioni vietate già coperte; (b) buchi controllabili con un invariante PULITO (vincolo rigido, nessuna eccezione legittima → impossibile falsare); (c) proprietà di qualità NON controllabili nella sim densa (come la distribuzione, coperte da unit test); (d) feature nemmeno esercitate dalla generazione (tetto settimanale §3.8 e preferenza turno §3.9 — mai generati nella sim, solo unit test). Aggiunti i 3 invarianti di categoria (b), tutti vincoli rigidi: **`INV-TETTO-IMPLICITO`** — estende `INV-MAXTURNI` a OGNI contrattualizzato (non solo quelli con Max turni mese dichiarato): nessuno supera `min(monte-ore-implicito, Max turni mese)`, col monte ore che fa sempre da tetto. Budget calcolato STATICAMENTE riusando le funzioni-dato del motore (`debitoOrdinarioIniziale` = monte ore aggiustato per mese §3.11 p.3 + recupero; `tettoDistribuzioneDi` coi turni extra ×12), esportate apposta da `engine_test.mjs` — via indipendente dal consumo effettivo durante l'assegnazione (si testa che l'ASSEGNAZIONE rispetti il tetto; la correttezza del tetto in sé è già negli unit test aggiustamento/max_turni). **`INV-FISICO-UNICO`** — un medico non è mai fisicamente presente in due sedi diverse dello stesso turno (una persona non si sdoppia; la copertura a distanza è un'altra cosa, già limitata a 1). **`INV-DETERMINISMO`** — stesso input → stesso output: su un campione ~5% degli scenari si riesegue `elaboraSchema` con input clonati e si confronta l'output (isola il puro determinismo da eventuali mutazioni degli input). Nessuno dei tre può dare falsi positivi (sono vincoli rigidi, a differenza dell'INV-DISTRIBUZIONE rimosso). Verificato: **189/189 unit test verdi**; simulazione da 100.000 scenari con i 3 nuovi invarianti attivi: **0 violazioni su 29.959.936 check** (nessun bug scoperto — il motore rispettava già tutte e tre le proprietà; ora sono verificate a ogni run). Restano buchi CONSAPEVOLI documentati: nella generazione della sim non sono ancora esercitati il tetto settimanale (§3.8) e la preferenza turno G/N (§3.9), coperti per ora solo dagli unit test dedicati. **(Gap chiuso nella voce 22: entrambi ora esercitati dalla generazione — la §3.9 ha fatto emergere un bug reale, corretto.)**
22. **Gruppo E del censimento — la generazione della sim ora esercita tetto settimanale (§3.8) e preferenza turno (§3.9); INV3 orfano + gap titolarità dello scambio scoperti e corretti** — chiuso il buco documentato in fondo alla voce 21: la generazione degli scenari ora produce ogni tanto (a) cap settimanali bassi sui medici (chiave `SETT:`, §3.8) e (b) preferenze di turno stesso giorno G/N (chiave `TURNOPREF:`, §3.9), così due percorsi di motore prima mai attraversati dalla simulazione vengono finalmente esercitati a scala. Il §3.8 è passato subito verde. Il §3.9 (scambio turno stesso giorno) ha fatto emergere **due** difetti, entrambi latenti perché lo scambio non era mai stato esercitato dalla sim:
    - **Bug 1 — copertura a distanza (blu) ORFANA dopo lo scambio (INV3), CORRETTO nel motore.** Quando un medico vince fisicamente sia G che N e cede il turno non preferito, lo scambio riassegnava lo slot FISICO all'alternativa ma lasciava intatta l'eventuale copertura blu che il medico ceduto stava fornendo in quel turno: risultato, una sede coperta "a distanza" da un medico non più fisicamente presente in quel turno (INV3). Riprodotto su caso singolo (CERVESATO copre Anduris nel notturno pur essendo passato al solo diurno). **Correzione (Opzione B, "la copertura vince" §3.7):** dopo ogni scambio la copertura a distanza del turno ceduto viene RICALCOLATA da capo con il nuovo insieme di fisici, così la sede passa a un altro fisico presente che l'abbia dichiarata blu, o resta SCOPERTA — mai da un non-fisico. La logica blu della FASE 2 di `elaboraTurno` è stata estratta in un helper di modulo `risolviBlu(fisMids, sitiCoperti, ...)` (unica sorgente di verità), richiamato sia in FASE 2 sia dopo lo scambio: refactoring a comportamento identico (tutti gli unit test verdi, la FASE 2 produce esattamente lo stesso output di prima). Innocenza del fix provata: sul motore da git HEAD i layout fisici e gli invarianti di titolarità risultano identici (il fix tocca solo slot non-fisici).
    - **Bug 2 — lo scambio poteva introdurre una violazione di titolarità (INV-TITOLARE), CORRETTO nel motore.** L'alternativa che rileva il turno ceduto viene piazzata con un'assegnazione diretta che NON passa dalla correzione di titolarità di FASE1 (`correggiTitolarita` gira solo dentro `elaboraTurno`): così lo scambio poteva portare nel turno un titolare di un'ALTRA sede, mettendolo sulla sede ceduta mentre la sua sede di titolarità era tenuta da un altro determinato non titolare — il furto che §3.1a vieta (riprodotto: FOSCHIANI, titolare di Spilimbergo, portato a Meduno mentre Spilimbergo era di ZURLO). **Correzione (approccio "esclusione", scelto perché chirurgico e privo di rischio — evita di estrarre la ricorsione delicata di `provaFisica`/`correggiTitolarita`, storicamente fonte di regressioni §10):** la scelta dell'alternativa esclude ora ogni candidato che, piazzato sulla sede ceduta, creerebbe quella violazione (helper `rompeTitolarita`, che ricalca esattamente la condizione di INV-TITOLARE, titolari con turni extra esclusi come da §3.1a). Se l'unica alternativa è "colpevole", lo scambio semplicemente non avviene e il medico resta su entrambi i turni (stato legittimo pre-scambio: lo scambio è facoltativo, la correttezza vince sempre). Provato caso per caso che lo scambio è la causa (disabilitandolo la violazione sparisce) e che il fix la elimina senza toccare gli altri.
    - **Limite noto §3.1a nella sim — reso TOLLERATO ma RIPORTATO, non mascherato.** L'attivazione di #22 ha cambiato lo stream RNG e quindi il campione di 100.000 scenari: sono così emerse anche 2 violazioni INV-TITOLARE che NON dipendono dallo scambio (presenti anche con lo scambio disattivato) né dal fix (identiche sul motore HEAD) — sono il **limite residuo documentato §3.1a/§10** (catena di ricollocazione ricorsiva di FASE1 con 3+ determinati che si contendono sedi sovrapposte). Fixare quella catena in FASE1 introduce regressioni peggiori (§10), quindi NON si tocca: la sim ora **tollera** questo pattern SPECIFICO (violazione di titolarità con **≥3 contrattualizzati fisicamente presenti** nel turno — il titolare spostato + l'occupante della sua sede + almeno un terzo che chiude la catena) senza fallire, ma lo **stampa** come `ℹ️ TOLLERATO` per non nasconderlo. Si contano i **contrattualizzati** (determinati **e** INDET), non i soli determinati: l'INDET partecipa sia alla catena di ricollocazione sia alla priorità di titolarità (§3.1a, "tra tutti i contrattualizzati, INDET incluso"), e un caso reale (seed 28: catena BERTUZZI INDET + 2 determinati) ha mostrato che contare i soli determinati sottostima la catena e lascerebbe passare come "fallimento" ciò che è invece il limite noto. Una violazione di titolarità con **≤2 contrattualizzati** presenti non è quel pattern (catena troppo corta, `correggiTitolarita` converge sempre) → resta un **fallimento vero** (così la tolleranza non maschera bug futuri diversi). Verificato: **189/189 unit test verdi**; simulazione da 100.000 scenari con #21/#22 attivi: **0 violazioni reali** (INV3 e caso-3 titolarità inclusi), 2 violazioni di titolarità tollerate e riportate come limite noto §3.1a (seed 28 e seed 321).
23. **Invariante #11 del censimento — `INV-TETTO-SETTIMANALE` (§3.8) aggiunto alla simulazione** — completato il censimento: ora che la generazione esercita il tetto settimanale (#21, voce 22), è stato aggiunto il quarto invariante "pulito" (dopo #9 `INV-TETTO-IMPLICITO`, #10 `INV-FISICO-UNICO`, #20 `INV-DETERMINISMO`). Vincolo RIGIDO: per ogni medico e ogni settimana lun-dom, il numero di turni assegnati (fisici + extra; la copertura a distanza NON conta, stesso criterio del tetto mensile e del `settimanaCount` interno del motore) non supera mai `capSettimanale` dichiarato; nessun tetto dichiarato per quella settimana → nessun limite. Conteggio accumulato per `${mid}|${wk}` in `verificaTurno` (wk = `settimanaDi` del turno) e confrontato con `capSettimanale` importata dal motore. Nessun falso positivo possibile (vincolo rigido applicato live in `candidatiOrdinati`). Come atteso il motore lo rispettava già: **0 violazioni**. Non vacuo — su uno smoke da 5.000 scenari il check è stato eseguito 22.014 volte con un cap dichiarato, di cui 19.232 con il medico portato ESATTAMENTE al tetto (`usati === cap`) senza mai superarlo: l'invariante è quindi stretto e realmente esercitato, non passa "a vuoto". Verificato: **189/189 unit test verdi**; simulazione da 100.000 scenari: **0 violazioni reali** (2 titolarità tollerate come limite noto §3.1a, invariate).
24. **Censimento CHIUSO — la generazione della sim ora produce sedi verdi a LIVELLI DIVERSI nello stesso turno (#23)** — ultimo buco di generazione: la sim metteva SEMPRE le due sedi verdi di un medico allo stesso livello (`verdeLiv[verde2] = verdeLiv[casa]`, "pari livello: indifferenti"), quindi la logica di scelta tra sedi verdi di priorità DIVERSA (`ordinaPerLivello`/`livelloVerdeDi` e i rami "livello peggiore" / ricollocazione condizionata di `provaFisica`) non era mai attraversata dalla simulazione. Aggiunto un flag per-medico `verde2DiffLiv` (≈40% dei medici con una seconda verde): quando attivo, il livello di `verde2` viene rerollato per-giorno a un valore DIVERSO da quello di casa (a volte casa è migliore, a volte verde2), esercitando entrambe le direzioni. Esercizio reale confermato non-vacuo: su uno smoke da 5.000 scenari **116.848 slot** hanno ora due verdi a livelli diversi (prima: 0). **Risultato: tutto verde — 0 violazioni reali su 100.000 scenari / 30.395.474 check.** Il motore gestiva già correttamente le sedi verdi a livelli diversi (come per gli altri percorsi del gruppo E). L'unica INV-TITOLARE tollerata è cambiata (ora seed 132, MORANO, prima seed 28/321): è il consueto **resampling** dovuto allo shift dello stream RNG (la nuova generazione consuma `rnd()` diversi), NON un effetto di #23 — verificato caso per caso che MORANO ha le proprie verdi allo STESSO livello (quindi #23 non è causalmente coinvolto), che è una violazione pura di FASE1 (persiste con lo scambio §3.9 disattivato, che si limita a cambiare l'occupante da ZURLO a TRIGODKO), e che corrisponde esattamente al limite noto §3.1a (titolare DET12 scavalcato da un DET38 non titolare, catena con 3 contrattualizzati) — tollerato e riportato dalla logica già esistente (voce 22). **Con questo il censimento della copertura è CHIUSO**: generazione ed esercizio coprono tutti i percorsi individuati (titolarità, categorie, debito, distribuzione §3.11, tetto mensile, tetto settimanale §3.8, preferenza turno §3.9, turni extra §3.10, livelli verde/blu), e gli invarianti rigidi (INV1/2/3, max-1-blu, INV-TITOLARE con tolleranza del limite noto, INV-MAXTURNI, INV-TETTO-IMPLICITO, INV-FISICO-UNICO, INV-DETERMINISMO, INV-TETTO-SETTIMANALE) li verificano a ogni run.
29. **Indicatore "iniquità percepita" sui turni extra — scala RELATIVA + correttivo pesato sul numero di penalizzati** — la metrica dell'etichetta (tab Medici, §6 voce 28) è deliberatamente RELATIVA: misura il **divario tra le soddisfazioni dei colleghi** (max − min di X/Y tra chi ha chiesto extra), non una soglia assoluta — è la versione "confronto tra pari" dell'iniquità percepita. Conseguenza voluta: se tutti hanno la stessa soddisfazione (anche tutti a zero) il divario è 0 → "Nessuna" (nessuna disparità percepibile). Discusso con l'utente il rischio che con pochi medici la scala saltasse sempre in cima: verificato con tabelle 2/3/5 medici che le fasce intermedie si usano davvero (compaiono con soddisfazioni PARZIALI, cioè quando qualcuno ha chiesto Y ≥ 2); con richieste da 1 solo turno l'esito è per forza binario (Nessuna o Altissima). Decisa e implementata l'**Opzione 2 (correttivo pesato)**: il vecchio correttivo "+1 se qualcuno sotto il 25%" è diventato **+1 se c'è almeno un penalizzato, +2 se i penalizzati sono almeno la metà dei richiedenti** (`penalizzati*2 >= n`), sempre solo con disparità reale (max > min). Così "1 su 5 a zero" e "4 su 5 a zero" si distinguono: nel caso 100-vs-0 il livello satura entrambi a Altissima ma il **testo** riporta il numero ("1 medico penalizzato" vs "4 medici penalizzati"), e nella banda media cambia anche il livello (`50,50,50,50,0` → Alta — 1 pen; `50,0,0,0,0` → Altissima — 4 pen). Il numero di penalizzati è mostrato in etichetta **solo da "Media" in su** (a Nessuna/Bassa solo il livello — Modo 1 concordato), con lo stesso criterio del correttivo (< 25%). Soglie tutte tunable in cima al COMPONENTE. SOLO visualizzazione: motore intatto, 189/189 unit test verdi, `engine_test.mjs` non rigenerato, `docs/app.jsx` in sync.
30. **Claut e Anduins fisiche solo nel turno diurno (cambio di copertura fisica)** — su richiesta, cambiata la regola delle sedi fisiche, identica per Claut e Anduins. Prima: le sedi fisiche erano 4 (Maniago, Spilimbergo, Meduno, Claut), Claut fisica sempre, Anduins solo a distanza. Ora: **Maniago, Spilimbergo, Meduno sono fisiche sempre; Claut e Anduins sono fisiche SOLO nel turno diurno** (id `"G"`, 12h piene) — che nel calendario esiste unicamente nei giorni ad alta domanda (weekend, festivi, prefestivi). Nel notturno (sempre) Claut e Anduins sono coperte solo a distanza (blu), come Anduins già faceva; anche Claut, prima fisica sempre, ora è a distanza fuori dal diurno. Ragionamento dell'utente (interpretazione B): un festivo/prefestivo infrasettimanale è ad alta domanda esattamente come una domenica, quindi la regola si aggancia all'**esistenza del turno diurno** (`turno.id === "G"`), non a "sabato/domenica" in senso stretto — e il diurno non esiste mai in un feriale semplice. **Implementazione**: unica modifica alla FASE 1 di `elaboraTurno` — `const sediFisiche = turno.id === "G" ? [0,1,2,3,4] : [0,1,2]`, `target` = prefisso troncato al numero di medici (dinamico per n=1: miglior verde tra le sole sedi oggi fisiche). Priorità invariata (Maniago, Spilimbergo, poi Meduno, poi Claut, poi Anduins); Claut/Anduins fisiche solo se avanzano medici dopo le prioritarie, altrimenti a distanza; oltre il n° di sedi disponibili i medici in più restano inutilizzati (idle, debito non consumato). **FASE 2 non toccata**: `sitiCoperti` deriva dalle sedi effettivamente fisiche, quindi Claut/Anduins sono coperte a distanza quando non fisiche ed escluse dalla distanza quando fisiche (niente doppione) — automatico. Verificato con esempi reali (sabato diurno 3/5/6 medici, martedì notte, prefestivo venerdì diurno) prima di scrivere il codice. Conseguenza documentata: una dichiarazione *verde* su Claut/Anduins in un notturno è inutilizzabile (lì non sono fisiche) — per coprirle di notte servono dichiarazioni *blu* (eventuale adeguamento UI/prompt AS separato). Aggiornati: testo "SEDI E SCENARI DI COPERTURA" nel prompt AI, §3.2, §5. Test: `run_tests2.mjs` — il vecchio test "n=4 → 4 sedi fisiche" (che codificava il comportamento vecchio su un notturno) riscritto come "NOTTE: solo 3 fisiche, Claut/Anduins a distanza", più 3 nuovi test sul diurno (Claut fisica con 4 medici, Anduins fisica con 5, priorità invariata con 3 → Claut/Anduins a distanza): **192/192 unit test verdi** (erano 189, +3 netti); simulazione da 100.000 scenari: **0 violazioni su 30.391.357 check**. `engine_test.mjs` e `docs/app.jsx` rigenerati e in sync.
31. **Vincolo territoriale nella copertura a distanza di Claut e Anduins + invariante INV-TERRITORIALE** — su richiesta, aggiunto un vincolo geografico (reale, non ottimizzabile) su CHI può coprire a distanza Claut e Anduins. Prima: `risolviBlu`/`provaBlu` (FASE 2) non aveva vincoli territoriali — qualunque fisico che dichiarava blu una sede poteva coprirla da qualunque sede fisica. Ora: **Claut** coperta a distanza SOLO dal medico fisicamente a **Maniago** (unica via — Val Cellina); **Anduins** SOLO dal medico fisicamente a **Spilimbergo o Meduno** (due vie, tra loro decide la gerarchia titolarità→categoria→debito→grad). Le altre sedi (Maniago/Spilimbergo/Meduno a distanza) restano senza vincolo. **Implementazione**: in `risolviBlu` un filtro `puoCoprireADistanza(mid, si)` applicato PRIMA della gerarchia in `provaBlu` (chi non è nella sede-base ammessa non si candida nemmeno; la scelta tra i validi resta `isBetterPriority`, invariata). La firma di `risolviBlu` ora riceve la mappa medico→sede-fisica (`sedeFisicaDi`) invece di `sitiCoperti` (derivato internamente); aggiornati i due call site (FASE 2 di `elaboraTurno` passa `sedeDi`; il ricalcolo post-scambio §3.9 costruisce la mappa da `out.fis`). Regole preservate: solo i fisici coprono, copertura solo se dichiarata blu (mai forzata: se il fisico di Maniago non dichiara Claut, Claut resta SCOPERTA — mai coperta da altrove), Meduno a distanza invariato (nessun vincolo), max 1 sede a distanza, gerarchia per i conflitti tra candidati validi. Verificato con esempi reali (vecchio vs nuovo) prima del codice. **Nuovo invariante rigido `INV-TERRITORIALE`** nella simulazione: per ogni sede coperta a distanza, se è Claut il coprente dev'essere fisico a Maniago, se è Anduins a Spilimbergo/Meduno. Non vacuo: su uno smoke da 5.000 scenari verificato su **39.370 coperture di Claut** e **67.764 di Anduins** a distanza, 0 violazioni. Test: aggiornati 2 test "bump → blu successivo" (il fallback usava Claut da Spilimbergo, ora territorialmente vietato → cambiato in Anduins, via valida da Spilimbergo, meccanismo identico) + 5 nuovi test territoriali dedicati. **197/197 unit test verdi** (erano 192, +5 netti); simulazione da 100.000 scenari con INV-TERRITORIALE attivo: **0 violazioni su 34.281.414 check**. `engine_test.mjs` e `docs/app.jsx` rigenerati e in sync.
32. **Fix "4° medico sprecato" nel target fisico diurno (percorso separato, FASE 1 intatta)** — problema individuato analizzando la FASE 1: nel diurno con 4 medici il target è il prefisso `[Maniago, Spilimbergo, Meduno, Claut]`, quindi apre sempre la 4ª sede su Claut; se il 4° medico aveva dichiarato verde SOLO Anduins non poteva andare su Claut (giustamente non forzato) e Anduins non era target con 4 medici → il medico restava inutilizzato e sia Claut sia Anduins scoperte. **Regola (dal 4° medico):** dichiara solo Claut → Claut; solo Anduins → Anduins; indifferente (entrambe, stesso livello) → tappa il buco della copertura a distanza (la sede che altrimenti resterebbe scoperta: Claut coperibile dal fisico di Maniago, Anduins da Spilimbergo/Meduno per il vincolo territoriale §3.2 voce 31), se entrambe o nessuna sarebbero coperte a distanza → Claut (più popolosa). **Scelta di implementazione discussa con l'utente: PERCORSO SEPARATO (opzione B) invece di "target a 5 sedi" (opzione A).** L'opzione A (offrire `[0,1,2,3,4]` con ≥4 medici) faceva girare `provaFisica` su 5 sedi per l'intera classe 4-medici-diurno, allargando lo spazio della ricollocazione ricorsiva che il §10 documenta come sensibile alle regressioni. L'opzione B lascia la FASE 1 (provaFisica/correggiTitolarità, target) **byte-identica** e aggiunge dopo il rebuild finale uno **step additivo con sole assegnazioni dirette** (nessuna ricollocazione): (A) se Anduins è libera, piazzaci un medico eleggibile rimasto inutilizzato che l'abbia dichiarata verde; (B) tie-break: se Claut è occupata da un indifferente e Anduins è l'unico buco a distanza, spostalo su Claut→Anduins. Verificato che A e B danno risultato IDENTICO sugli 8 esempi; B cambia il comportamento SOLO nei due casi (4° sprecato + tie-break), tutto il resto identico a oggi (trade-off accettato: il caso raro delle catene di ricollocazione resta come oggi, non migliorato — scelta conservativa). Da preservare, verificato: non forzare mai un medico su una sede non dichiarata verde (INV1), sedi prioritarie sempre coperte prima, vincolo territoriale §3.2 intatto, Scenario 5 (5 medici) invariato, notte invariata. Test: **6 nuovi test** in `run_tests2.mjs` (i 4 casi del 4° medico + notte invariata + solo-Anduins). **203/203 unit test verdi** (erano 197, +6); simulazione da 100.000 scenari: **0 violazioni su 34.341.312 check**. `engine_test.mjs` e `docs/app.jsx` rigenerati e in sync.
33. **Guardiano titolarità §3.1a — validazione additiva a valle (solo avviso, nessuna correzione)** — su richiesta esplicita, aggiunto un controllo finale ("scatola/guardiano") che NON tocca la logica esistente (FASE 1, `correggiTitolarita` — le due passate — tutto invariato) e si limita a SEGNALARE il residuo raro che le catene di ricollocazione profonde §3.1a lasciano sfuggire. **Perché solo avviso e non correzione:** il §10 documenta che tentare di correggere di più la catena di FASE1 introduce regressioni peggiori (perdita di copertura) → decisione conservativa, si segnala al coordinatore per un intervento manuale, non si corregge. **Cosa rileva:** dopo TUTTE le correzioni, per ogni turno non-extra, se un titolare determinato della sede S (con S prima scelta verde del giorno, non in turni extra) risulta assegnato fisicamente ad un'ALTRA sede mentre la sua sede S è tenuta da un altro determinato NON titolare di S → genera l'avviso `"titolare [nome] di [S] assegnato altrove ([sede]); la sua sede di titolarità è coperta da [nome] (non titolare). Valutare un intervento manuale su questo turno."` tramite lo stesso meccanismo degli avvisi esistenti (`avvisiRaw.push`). **Correttezza per costruzione:** la condizione del guardiano ricalca ESATTAMENTE l'`INV-TITOLARE` della simulazione (stesse esclusioni: medici in turni extra, sede non prima scelta verde, occupante non-determinato o titolare della stessa sede), quindi scatta sul medesimo caso reachable che la sim tollera come limite noto §3.1a. **Additività provata:** il guardiano fa SOLO `push` di avvisi, non cambia NESSUNA assegnazione → la simulazione da 100.000 scenari dà **0 violazioni su 34.341.312 check**, conteggio IDENTICO alla run pre-guardiano (voce 32) — conferma zero regressioni. **Casi reali di attivazione trovati** (via scansione della generazione della sim, per verificare che il guardiano scatti davvero su un residuo reale): seed 1212 (2032-11, g14 notturno: BEKAEVA titolare Spilimbergo a Meduno, sede coperta da FOSCHIANI); seed 1390 (2033-8, g6 diurno: VALERI titolare Spilimbergo a Claut, coperta da ZURLO); seed 1612 (2030-12, g8: PITAU titolare Maniago a Spilimbergo, coperta da ZURLO). **Test:** nuovo file `test_guardiano_titolarita.mjs` con 3 test (2 casi reali che devono generare l'avviso atteso — BEKAEVA/Spilimbergo e VALERI/Spilimbergo — + 1 caso pulito senza residuo che NON deve generare avvisi). **206/206 unit test verdi** (erano 203, +3). `engine_test.mjs` e `docs/app.jsx` rigenerati e in sync (guardiano presente in tutti e 3 i file).
34. **Correzioni lato AI/testi (nessuna modifica al motore di assegnazione)** — due interventi sul lato assistente AI e sui testi, verificati nel codice:
    - **(1) Sede di titolarità passata all'AI.** L'oggetto `stato.medici` inviato a `chiediAI` non conteneva `sedeContratto`, quindi l'AI "non conosceva la città" di un medico anche quando implicita dalla titolarità. Aggiunto il campo `titolare: m.sedeContratto` all'oggetto medici (Maniago/Spilimbergo per i contrattualizzati, `null` per i senza incarico) + una riga nella sezione TITOLARITÀ DI SEDE del prompt che istruisce l'AI a usarlo ("la sede di titolarità di ogni medico è nel campo titolare … NON dire mai che non conosci la città/sede di un medico contrattualizzato").
    - **(2) Rimossa la contraddizione "da qualunque sede fisica" (residuo pre-voce 31).** La regola territoriale corretta era già nella sezione SEDI E SCENARI DI COPERTURA del prompt (voce 31), ma 3 punti col vecchio testo "da qualunque sede fisica" la contraddicevano ancora: la **definizione BLU** nel prompt AI, il **testo di aiuto della cella** (guida utente nel tab Disponibilità) e un **commento nel motore**. Tutti e tre riallineati al vincolo territoriale (la copertura a distanza dipende dalla sede fisica: Claut solo dal fisico di Maniago, Anduins solo da Spilimbergo/Meduno).
    Solo testi/prompt/stato-AI e un commento: **nessuna modifica alla logica di assegnazione**. Il `titolare` e la riga di prompt vivono nel COMPONENTE (fuori da `engine_test.mjs`); il commento BLU è nella sezione MOTORE, quindi `engine_test.mjs` rigenerato (diff = solo quel commento). Verificato: **206/206 unit test verdi**, simulazione da 100.000 scenari **0 violazioni su 34.341.312 check** (identica alla voce 33 → motore byte-identico), `docs/app.jsx` rigenerato e in sync.
35. **Altre 3 correzioni lato AI/validazione (motore di assegnazione byte-identico)** — tutte nel COMPONENTE (interpretazione email / applicazione azioni), nessuna nella sezione MOTORE:
    - **(3) Giorno fuori range validato in `applicaAzioni`.** L'azione `dispo_aggiungi` (e ogni altra che cita un giorno: `mmg`, `schema`, `tetto_settimana`, `turno_pref`, `dispo_togli`/`dispo_no`) costruiva `dk(anno, mese, a.giorno)` senza verificare che il giorno esistesse nel mese: un `giorno: 31` in un mese di 30 (o il 30 febbraio, da una data che non torna nell'email) creava una disponibilità per un giorno inesistente. Aggiunta una guardia unica in cima al `forEach` delle azioni: `giorniNelMese = new Date(anno, mese+1, 0).getDate()` (gestisce anche febbraio bisestile) e, se `a.giorno` è presente ma non intero / <1 / >giorniNelMese, l'azione viene scartata con errore chiaro ("giorno 31 non esiste in Novembre 2026 (Novembre ha 30 giorni)"). Le azioni senza giorno (`ore_extra`, `turni_extra`, `tetto_mese`, `elabora`, `vai_mese`) non sono toccate.
    - **(4) Abbinamento nome→medico blindato contro l'ambiguità.** `nomeToId` usava un fallback `startsWith` permissivo (`=== n || nome.startsWith(n) || n.startsWith(nome)`) che coi 14 cognomi attuali non collide (verificato) ma era latente: aggiungendo un cognome che è l'inizio di un altro, avrebbe agganciato il medico sbagliato in modo silenzioso. Ora: la corrispondenza ESATTA vince sempre ed è univoca; il match per prefisso si accetta SOLO se identifica un unico medico (nuovo helper `mediciCheMatchano`). Se il prefisso corrisponde a più medici → `nomeToId` ritorna `undefined` (non indovina) e il nuovo helper `erroreMedico` produce un messaggio che distingue "ambiguo" (`nome "X" ambiguo: corrisponde a A, B — specifica il cognome completo`) da "non trovato". I 7 punti che usavano `medico X non trovato` ora chiamano `erroreMedico(a.medico)`. Verificato: i 14 cognomi risolvono tutti in modo univoco (nessun cambio di comportamento oggi), la rilevazione d'ambiguità è pronta se un domani si aggiunge un cognome-prefisso.
    - **(5) "sera" da sola riconosciuta come notturno.** Bug osservato: "lavoro domenica sera" faceva chiedere all'AI "vuoi anche il diurno?", ma "sera" = notturno, senza ambiguità. Nel prompt "sera" era riconosciuta solo in contesti specifici ("solo il pomeriggio/sera") e in "mattina e sera" vale (correttamente) ENTRAMBI. Aggiunto alla lista SOLO NOTTURNO un bullet dedicato ("di sera / la sera / serale / lavoro la sera / …" quando "sera" è l'unico turno nominato, senza mattina/diurno → solo N, MAI la domanda sul diurno) + "sera"/"serale" tra le parole-trigger dell'header SOLO NOTTURNO e della sezione WEEKEND AMBIGUO, con l'eccezione esplicita "mattina e sera = ENTRAMBI" preservata.
    Nessuna modifica alla sezione MOTORE: `engine_test.mjs` **byte-identico** a HEAD (diff vuoto), quindi i test motore non cambiano. Verificato: **206/206 unit test verdi**, simulazione da 100.000 scenari **0 violazioni su 34.341.312 check** (identica alla voce 34); logica di validazione giorno e abbinamento nome verificata a parte (conteggi giorni nov/feb/feb-bisestile/ago corretti, 14 cognomi tutti univoci). `docs/app.jsx` rigenerato e in sync.
36. **Tre buchi del prompt AI colmati (emersi da un collaudo "a tavolino" del prompt)** — a valle di un'analisi delle attese di un batch di collaudo email, il coordinatore ha deciso come l'AI deve comportarsi su tre casi che il prompt non copriva. Solo modifiche al prompt di `chiediAI` (sezione `sys`), **nessuna modifica al motore** (`engine_test.mjs` byte-identico a HEAD). Filo conduttore: **interpreta quando l'intenzione è chiara e inequivoca, CHIEDI quando indovinare sarebbe rischioso** (coerente col principio "mai forzare/mai inventare").
    - **Buco 1 — frasi vaghe sui feriali.** (1a) "soliti notturni infrasettimanali" / "notturni feriali" / "le notti durante la settimana" e equivalenti CHIARE (senza qualificatori di eccezione) → nuova regola nella sezione TURNI: interpreta come **tutte le notti dei giorni feriali del mese** (N su ogni feriale semplice, calcolati col CALENDARIO PERPETUO; nei feriali esiste solo il notturno); insieme determinato, NON una "data vaga". (1b) "quasi sempre" / "di solito" / "spesso" / "in genere" / "il più delle volte" (qualificatori di eccezione non specificata) → nuova voce "ECCEZIONI NON SPECIFICATE" in CASI DA SEGNALARE: **non inserire nulla**, avviso "🔴 ATTENZIONE: [nome] ha scritto '[frase]' — non è chiaro quali notti/giorni escludere. Verificare." I due casi si distinguono esplicitamente (è solo il qualificatore di eccezione a spostare dalla via "interpreta" alla via "chiedi").
    - **Buco 2 — riferimenti al mese.** Blindato nel CALENDARIO PERPETUO: il mese/anno per calcolare giorni-settimana, festività e date è SEMPRE `stato.mese`; qualunque "il mese prossimo" / "il mese entrante" / nome di mese scritto nella mail NON cambia il mese di riferimento (per il medico "il mese prossimo" = il mese che il coordinatore sta già elaborando). Il mese si cambia SOLO con `vai_mese` e SOLO su richiesta esplicita del coordinatore, mai dal testo di una mail — reconciliato esplicitamente con l'azione `vai_mese` esistente.
    - **Buco 3 — sede mancante su disponibilità ordinaria.** Nuova regola in fondo alla sezione SEDI: se il medico dichiara giorni/turni ma NON nomina alcuna sede → se ha titolarità (campo `titolare` valorizzato) usa **quella** come verde di prima scelta (mai chiedere); se NON ha titolarità (`titolare` null = senza incarico) **non inventare**, avviso "🔴 ATTENZIONE: [nome] non ha specificato la sede e non ha titolarità — verificare". Distinta da "sede vaga" (resta a CASI DA SEGNALARE) e da "qualsiasi sede/ovunque" (resta → tutte e 5).
    - **Interazione con la regola senza-incarico (segnalata prima dell'implementazione, non un conflitto):** per un senza incarico il controllo sul numero di guardie mensili (regola in cima alla sezione, "PRIMA di ogni altra regola") scatta per primo: se manca il numero, l'avviso è quello sulle guardie, non quello sulla sede (Buco 3) — un solo avviso, quello che scatta per primo. Reso esplicito nel testo della regola di Buco 3.
    - Verificato: nessun conflitto duro con le regole esistenti (Buco 1b e Buco 3-avviso riusano il meccanismo "🔴 ATTENZIONE" già previsto per date-vaghe/sede-non-identificabile; Buco 2 rafforza un ancoraggio a `stato.mese` già presente). Sintassi OK (esbuild), **206/206 unit test verdi**, `engine_test.mjs` byte-identico a HEAD (motore intatto, niente sim 100k necessaria), `docs/app.jsx` rigenerato e in sync.
37. **Quattro regole nuove al prompt AI, emerse dal collaudo a tavolino su 600 mail reali** — quattro divergenze di Categoria 1 (buchi veri del prompt) trovate giudicando 10 file di collaudo manuale contro le regole del `sys`. Tutte nel prompt di `chiediAI`, **motore intatto** (`engine_test.mjs` byte-identico a HEAD). Ogni regola verificata prima contro quelle esistenti (nessun conflitto duro; due interazioni segnalate).
    - **Regola 1 — copertura territorialmente impossibile.** Nella sezione COPERTURA A DISTANZA — BLU: un medico può coprire a distanza Claut solo se fisico a Maniago, Anduins solo se fisico a Meduno/Spilimbergo. Se dichiara una copertura non raggiungibile dalla sua sede fisica (verde dichiarata, o titolarità se non dichiarata) → NON registra la blu, avviso "🔴 ATTENZIONE: [nome] dichiara di coprire [sede] a distanza ma dalla sua sede [X] non è raggiungibile…". Il vincolo era già DESCRITTO (SEDI E SCENARI) ma il prompt non istruiva a segnalare una dichiarazione impossibile — ora sì. (Casi collaudo: FILE-05 #16/#48/#59, FILE-09 #15/#29.)
    - **Regola 2 — conflitto nome-giorno vs numero.** Nuova voce "GIORNO INCOERENTE" in CASI DA SEGNALARE: se il nome della settimana e il numero non combaciano (calcolo sul CALENDARIO PERPETUO, su `stato.mese`) → NON indovina, NON inserisce nulla per quel giorno, avviso "🔴 ATTENZIONE: [nome] ha scritto '[frase]' ma il [numero] è [giorno reale] — verificare…". (Casi: FILE-09 #5/#25, dove il batch era esso stesso incoerente.)
    - **Regola 3 — titolarità dichiarata ≠ sistema.** Nella sezione TITOLARITÀ DI SEDE: se il medico afferma una titolarità diversa dal campo "titolare", la titolarità valida resta quella del sistema (non cambia), ma segnala "🔴 ATTENZIONE: [nome] dichiara titolarità a [X] ma nel sistema risulta [Y] — verificare." **Interazione segnalata** con la regola SEDI "sono titolare a X → sede X": se il medico chiede comunque di lavorare in quella sede è una normale richiesta fuori-titolarità (si registra, perde solo la priorità lì). (Caso: FILE-09 #50.)
    - **Regola 4 — tetto SOLO settimanale dei senza-incarico (carve-out della 1883).** Nella sezione MEDICO SENZA INCARICO: se un senza dichiara il numero solo a settimana ("2 a settimana") e non c'è un totale mensile → NON bloccare (come farebbe la 1883): rispetta ENTRAMBI i limiti (ritmo settimanale + totale mensile): imposta `tetto_settimana:N` (il valore settimanale dichiarato, ripetuto per ogni settimana lun-dom del mese) E `tetto_mese:M` (M ≈ N × settimane del mese), inserisce le disponibilità, e segnala "ℹ️ [nome] ha indicato [N] a settimana → impostati [N]/settimana e circa [M]/mese (verificare)". **Interazione con la 1883 verificata e voluta**: è un'eccezione esplicita al blocco (il tetto settimanale ora soddisfa il requisito del "numero mensile" via conversione). Decisione del coordinatore: impostare sia il settimanale reale sia il mensile convertito, per onorare quello che il medico ha chiesto senza settimane sovraccariche. (Caso: FILE-07 #22.)
    - Verificato: sintassi OK (esbuild), **206/206 unit test verdi**, `engine_test.mjs` byte-identico a HEAD (niente sim 100k), `docs/app.jsx` rigenerato e in sync.
38. **Pacchetto grafico UI (5 punti) — SOLO visualizzazione, motore byte-identico** — tutte modifiche nel COMPONENTE (render + palette `T`), NESSUNA modifica al MOTORE, alla logica di assegnazione, titolarità, livelli, categorie. Verificato: `engine_test.mjs` **byte-identico a HEAD** (diff vuoto), **206/206 unit test verdi**, `docs/app.jsx` in sync, rendering reale controllato con screenshot Playwright (nessun errore console).
    - **(1) Celle "non disponibili" grigio tenue.** Nella griglia disponibilità la cella non-disponibile passa da rosso-tenue `dangerBg` + "✕" a grigio neutro `neutralCell` senza ✕ (nuovo token `T.neutralCell`); le celle disponibili restano verde pieno → peso visivo INVERTITO (il verde risalta). Sistema dicotomico invariato (righe 1870/3095): cambia solo il colore, non lo stato.
    - **(2) Leggibilità header + resa "che respira" del contenuto cella (revisione successiva).** Numeri dei giorni nell'header più grandi/marcati (fontSize 12.5, peso 800) e sigle giorno/turno con più contrasto (`textMuted`). Il contenuto della cella disponibile è stato poi RIFATTO per "far respirare" la griglia (prima era verde pieno con testo denso "1·CL¹", troppo carico): ora sfondo NEUTRO (`T.surface`) con **pallini colorati** — pallino verde `T.primary` = sede fisica, pallino blu `T.blu` = copertura a distanza. Per ogni tipo: se **una sola** sede → pallino + **sigla** (es. 🟢 MA · 🔵 CL); se **due o più** → pallino + **numero** (es. 🟢 3 · 🔵 2) con sotto, in grigetto tenue (`textFaint`, fontSize 8), l'elenco delle sigle raggruppate ("MA SP ME · CL AN") — il grigetto compare SOLO quando c'è un conteggio, mai nelle celle semplici. **Livelli (1ª/2ª scelta) e ★ preferiti NON sono più mostrati in griglia** — si vedono/impostano aprendo la cella (popup): alleggerisce molto. Cella non disponibile: grigio tenue `neutralCell` con un puntino discreto (4px, faint). **Solo visualizzazione**: i dati (V1..V5, B1..B4, preferiti) restano invariati, cambia solo come vengono mostrati. Aggiornati i due testi d'aiuto (il vecchio "In cella: 2·CL¹…" → nuova legenda pallini + "livelli e ★ nel popup").
    - **(3) Notturno: Claut/Anduins senza tendina fisica.** Nel render del notturno (`t.id === "N"`) Claut/Anduins non mostrano più la tendina di assegnazione fisica (il motore già sa che di notte non sono fisiche, riga 491). Stati mostrati: coperta a distanza → casella blu "Claut ← [nome] · a distanza da [sede fisica]"; dichiarata a distanza ma scoperta → bordo giallo "a distanza —"; nessuno l'ha dichiarata a distanza → casella grigia tenue "solo diurno". Di giorno (`t.id === "G"`) tendine normali per tutte e 5. SOLO render: nessun calcolo del motore, usa `t.slots`/`t.fis`/`g.key` già prodotti.
    - **(4) Avviso "scoperto" granulare.** Sostituito il vecchio "SCOPERTO" (solo se tutto vuoto) con un'etichetta per-turno che elenca le sedi mancanti ("Scoperto: Spilimbergo, Meduno"), colore per gravità (rosso `dangerBg` se manca Maniago o Spilimbergo, giallo `warningBg` se solo minori Meduno/Claut/Anduins — nuovi token `warning`/`warningBg`/`warningBorder`), e bordo colorato discreto sulle celle scoperte (rosso per le CDC, giallo per le minori). Nessuna etichetta quando è tutto coperto. Conta come "da coprire" solo le sedi esistenti nel turno: di notte Maniago/Spilimbergo/Meduno (+ Claut/Anduins solo se dichiarate a distanza e rimaste scoperte), di giorno tutte e 5. Gli MMG (extra) mantengono il vecchio "SCOPERTO".
    - **(5) Menu a tendina disponibilità.** Opzioni del `<select>` colorate per gruppo (`<optgroup>` "Sede principale (fisica)" in verde `primaryTint`/`primary`, "Copertura a distanza" in blu `bluTint`/`bluDark`) e rinominate: "Sede principale 1..5" → "1ª..5ª scelta", "Copertura a distanza 1..4" → "A distanza · 1ª..4ª scelta". **I valori restano identici** (V1..V5, B1..B4): cambia solo l'etichetta, MAI il dato passato al motore (verificato via DOM: le opzioni valgono sempre gli stessi V/B). Per Claut/Anduins in un turno NOTTURNO il menu mostra SOLO le opzioni "A distanza" (filtro UI: `soloDistanza = (Claut|Anduins) && turnoId === "N"`), di giorno entrambi i gruppi — verificato via DOM (diurno Claut = 10 opzioni, notturno Claut = 5 senza le fisiche). Aggiornati anche i due testi d'aiuto (popup e intro tab) coi nuovi nomi. **Non toccata** la logica dei livelli/indifferenza (da fare separatamente con cautela sul motore).
39. **Regola nuova al prompt AI — titolare che chiede SOLO un'altra sede → domanda di conferma al coordinatore** — aggiunta nella sezione TITOLARITÀ DI SEDE (subito dopo la regola della "titolarità affermata diversa", riga ~1849). Solo prompt di `chiediAI`, **motore intatto** (`engine_test.mjs` byte-identico a HEAD). Caso: un medico contrattualizzato (con titolarità nel campo `titolare`) dichiara disponibilità ESCLUSIVAMENTE per sedi fisiche (verdi) diverse dalla propria titolarità (la sua sede non compare mai tra le verdi) SENZA affermare nulla sulla titolarità → l'AI **non inserisce** e pone una "domanda" di conferma al **coordinatore** (meccanismo `domande`: `seSi` = dispo_aggiungi sulle sedi/giorni richiesti, `seNo` = []): "❓ [nome] è titolare di [sua sede] ma ha chiesto solo [sede/i] (giorno [X]) — confermi che è voluto? Se sì lo inserisco lì, altrimenti indicami la sede corretta." Blocco in attesa di risposta (niente inserimento finché il coordinatore non conferma). **Condizioni** (tutte): contrattualizzato + titolarità NON tra le verdi + nessuna affermazione sulla titolarità. **Non scatta** se nomina anche la sua titolarità (es. titolare Spilimbergo che chiede "Spilimbergo o Maniago"), né per i senza incarico, né sulle sedi blu (a distanza). **Confini con le regole vicine** (verificati, nessun conflitto duro): distinto dall'avviso 🔴 della "titolarità AFFERMATA diversa" (riga 1849, invariata) — se il medico afferma una titolarità vale quella regola (registra fuori-titolarità + 🔴), non questa; distinto da "SEDE NON MENZIONATA AFFATTO" (riga ~2116) che scatta solo quando NON c'è alcuna sede verde. Verificato: esbuild OK, **206/206 unit test verdi**, `engine_test.mjs` byte-identico a HEAD, `docs/app.jsx` rigenerato e in sync.
40. **Export Excel — rimosso " (MMG)" accanto al nome nella cella MMG** — nella generazione `.xlsx` (funzione export, riga ~1665) la cella del turno MMG scriveva `[nome] (MMG)` (es. "BERTUZZI (MMG)"); ora scrive solo il nome (`byId[t.slots[0]].nome`). L'etichetta "(MMG)" era ridondante: l'intestazione della colonna dice già "ANTICIPO DIURNO MMG e PLS…". Solo export Excel, nessun impatto su motore né interfaccia: `engine_test.mjs` byte-identico a HEAD, 206/206 unit test verdi, `docs/app.jsx` in sync.

41. **`dispo_set` — espansione deterministica degli insiemi di giorni spostata dall'AI al MOTORE (3 passi + raffinamento)** — problema di affidabilità: l'AI espandeva "a mente" gli insiemi di giorni ("notturni feriali", "weekend", "tutto il mese", "dal X al Y") in tante `dispo_aggiungi` giorno-per-giorno, calcolando i giorni col calendario di Zeller e occasionalmente saltandone uno (es. un venerdì feriale perso). Soluzione: una sola azione `dispo_set` con campo `ambito` polimorfico, espansa da una funzione PURA e deterministica del motore. Sviluppato UN PASSO ALLA VOLTA con ok esplicito dell'utente tra un passo e l'altro (e giudizio "a tavolino" offline prima del push).
    - **Passo 1 — motore (`espandiAmbito`).** Nuova funzione pura nella regione CALENDARIO (subito dopo `turniDelGiorno`, prima di `// ===== MOTORE =====`): `espandiAmbito(ambito, turniRichiesti, escludi, anno, mese, extras)` → `[{giorno,turno}]` deterministico e ordinato (giorni crescenti, G prima di N). `ambito`: `"feriali"` (lun-ven non festivi/prefestivi → solo N), `"weekend"` (sab/dom), `"mese"` (ogni giorno), `{da,a}` (intervallo, robusto anche a da>a). Interseca i turni richiesti con quelli che ESISTONO davvero quel giorno (`turniDelGiorno(...).turni`): un "G" su un feriale è ignorato da sé, gli MMG non sono mai prodotti. Test `test_espansione_ambito.mjs`: **46 casi** che confrontano l'output con un calcolo INDIPENDENTE (JS `Date` + festivi/prefestivi HARDCODATI, senza riusare le funzioni del motore) su 4 mesi-trappola — agosto/Ferragosto, dicembre (31 → prefestivo cross-mese 30, e 31 festivo ASFO), aprile/Pasqua (30 → prefestivo del 1° maggio, cross-mese), settembre pulito.
    - **Passo 2 — azione + refactor (`costruisciEntryDispo`).** Estratta la costruzione dell'entry dispo (verde/blu + livelli + preferito) dalle righe inline di `dispo_aggiungi` in una funzione PURA del motore (dopo `espandiAmbito`), unica fonte di verità condivisa da `dispo_aggiungi` (un solo slot) e dalla nuova `dispo_set` (stessa entry replicata, **copia indipendente per slot**, niente aliasing). Refactor comportamentalmente identico (stessi messaggi d'errore, default livello 1, regola preferito-solo-se-verde). Nuova azione `dispo_set` in `applicaAzioni` (COMPONENTE): valida il medico, costruisce l'entry una volta, chiama `espandiAmbito`, scrive gli slot; + `etichettaAmbito` per anteprima/errori/riepilogo. Test `test_dispo_set_equivalenza.mjs`: **245 casi** che provano `dispo_set` ≡ N `dispo_aggiungi` per-giorno equivalenti (4 mesi × 10 ambiti × 6 spec-sedi con verde/blu/livelli/preferito), + **chiusura del cerchio** con liste di giorni HARDCODATE (agosto feriali = `GIORNI_FERIALI_SEMPLICI`; intervallo 10-14 espanso a mano), + contenuto entry, sedi-non-valide (nessuno slot + errore), no-aliasing.
    - **Passo 3 — prompt AI.** Nuova sezione "INSIEMI DI GIORNI → USA dispo_set" (prima di NOTTI DEI GIORNI FERIALI) + dichiarazione dell'azione nel formato: mappa frase→ambito, `turni`, `escludi`, sede (titolarità se non dichiarata). Aggiornata la regola "NOTTI DEI GIORNI FERIALI" (prima diceva "inserisci N su OGNI feriale") → emette `dispo_set` feriali/["N"]. **5 regole di PRECEDENZA** esplicite che valgono PRIMA e sostituiscono dispo_set se scattano: (1) senza-incarico senza numero guardie → blocco+avviso; (2) weekend/festivo con turno NON chiaro → resta WEEKEND AMBIGUO (dispo_set su weekend/mese SOLO se il turno è esplicito); (3) "quasi sempre/di solito/…" → avviso ECCEZIONI NON SPECIFICATE; (4) titolare che chiede SOLO altra sede → domanda di conferma; (5) MMG (M/P) mai via dispo_set. Un elenco di date sparse ("il 3, il 10, il 17") → più `dispo_aggiungi`, non è un insieme.
    - **Raffinamento (caso 3, emerso dal giudizio a tavolino).** Weekend/mese SENZA turno esplicito: la parte notturna è comunque NON ambigua → `dispo_set` turni ["N"] sull'ambito + UNA sola "domanda" per il diurno il cui `seSi` è `dispo_set` ambito "weekend" turni ["G"] (il diurno esiste solo nei weekend), con `giorno` OMESSO nella domanda (è un insieme). Così si toglie l'enumerazione anche qui e la forma diventa deterministica. Distinto dal giorno SINGOLO ("sono disponibile il 2" → resta `dispo_aggiungi` N + domanda per-giorno); pointer aggiunto in cima a WEEKEND AMBIGUO.
    - **Verifiche.** `elaboraSchema` **byte-identico** in `engine_test.mjs` (il diff mostra solo le 2 funzioni pure aggiunte + la riga di export); **206/206 unit test** invariati; nuovi test **46/46** (espansione) + **245/245** (equivalenza); **simulazione 100.000 scenari: 0 violazioni su 34.341.312 check** (conteggio IDENTICO alla voce 33 → zero regressioni); esbuild `turni-guardia-medica.jsx` e `docs/app.jsx` OK; smoke Playwright OK (nessun errore console). `engine_test.mjs` e `docs/app.jsx` rigenerati e in sync. Giudizio a tavolino offline su 8 casi prima del push: 5 puliti e deterministici, il caso 3 raffinato, 2 esitazioni minori note (una preesistente: senza-incarico + numero + "quasi"; una cosmetica: decomposizione di "mese N + weekend G" in due dispo_set, risultato sempre corretto).

42. **Cinque ritocchi al prompt AI, emersi dal collaudo con mail reali (motore byte-identico)** — tutti nel prompt di `chiediAI` (sezione `sys`), **nessuna modifica al motore** (`engine_test.mjs` byte-identico a HEAD); 206/206 unit test invariati, `docs/app.jsx` rigenerato e in sync. (1) **Espressioni di periodo → intervalli fissi**: aggiunta "ESPRESSIONI DI PERIODO" alla sezione INSIEMI DI GIORNI — "inizio mese"→{1,7}, "metà mese"→{11,20}, "fine mese"/"gli ultimi giorni"→{24,fine}, "prima metà"→{1,15}, "seconda metà"→{16,fine} (mese di riferimento = STATO ATTUALE; naming del mese di riferimento equivalente); si usano con dispo_set, + `tetto_mese` se accompagnate da un numero. Rimosse "metà mese"/"a fine mese"/"inizio agosto" da DATE VAGHE (ora definite); restano vaghe "verso il 20"/"intorno al 15"/"la prima/seconda settimana"/"qualche giorno" ("verso"/"intorno"/"settimana"/"qualche" = confini non netti). (2) **Senza-incarico con GIORNI PRECISI non si blocca**: il blocco "numero di guardie mancante" scatta SOLO su richiesta VAGA (né giorni né numero); se il medico indica date specifiche o un insieme determinato ("il 21 a Spilimbergo", "i notturni feriali", "dal 3 al 7"), quei giorni SONO il numero → inserisci, niente blocco, niente tetto_mese (i giorni stessi limitano). (3) **"dove capita"/sede indifferente = SOLO la sede**: chiarito nella DISPONIBILITÀ GENERICA che l'indifferenza riguarda solo la sede, MAI i giorni/turni — non espandere a "tutto il mese/tutti i turni"; applica le 5 sedi pari solo ai giorni/turni effettivamente indicati (se assenti, regole ordinarie). (4) **"quasi" + numero mensile**: solidificata la regola ECCEZIONI NON SPECIFICATE — con un numero certo (es. "quasi tutte le sere, max 8") imposta comunque `tetto_mese`:8 E dai l'avviso 🔴 sulle eccezioni, ma NON espandere i giorni finché il coordinatore non chiarisce. (5) **Niente testo duplicato**: aggiunta regola alla spec delle "domande" — ogni avviso 🔴/domanda ❓ compare UNA sola volta; la "spiegazione" resta un riassunto brevissimo e non ripete il testo della domanda/avviso (che vive solo in "citazione"+"domanda"), mai la stessa frase in due campi. Nota sulla causa: la duplicazione vista nel collaudo nasce dall'AI che copiava il testo sia in `spiegazione` sia nella `domanda` (la chat mostra `spiegazione` e la card mostra la `domanda` → stesso testo due volte); l'eco strutturale di `spiegazione` (bolla chat + card "Sto per applicare") resta by-design e non è stato toccato (è render, non motore). Verificato: esbuild OK, `engine_test.mjs` byte-identico a HEAD, 206/206 unit + 46/46 + 245/245 verdi.

43. **`dispo_set` — nuovo ambito `giorni_settimana` (i giorni della settimana nominati) + segnale del diurno "a sorpresa"** — estensione del lavoro voce 41, stesso problema di affidabilità: quando un medico dichiara disponibilità su specifici giorni della settimana ("il lunedì e il mercoledì", "da martedì a giovedì", "tutti i venerdì") era l'AI a espandere le date del mese, e sbagliava (nel collaudo il caso Iengo "da martedì a giovedì" produceva date errate — saltati giovedì, inclusi lunedì). Ora è il MOTORE a espandere. Sviluppato a passi con ok esplicito dell'utente.
    - **Passo 1 — motore.** `espandiAmbito` accetta un nuovo ambito `{ giorni_settimana: ... }` in due forme: ELENCO `["lun","mer"]` oppure INTERVALLO `{ da:"mar", a:"gio" }` (espanso in ordine lun→dom con **wraparound**, es. ven→lun). Token = prime 3 lettere (`"lun"`=`"lunedì"`, maiuscole/nomi interi ok, sconosciuti ignorati) → indice `getDay`; match sul `dow` di `turniDelGiorno`, **indipendente da festivo/prefestivo** (a differenza di `"feriali"` che li esclude — distinzione documentata). Turno omesso → `["N"]`. Nuova funzione pura `diurniNascosti(slots, anno, mese, extras)`: i giorni della serie che cadono su festivo/prefestivo **infrasettimanale** (non weekend), quindi con diurno G che esiste ma non è stato inserito. Test `test_espansione_ambito.mjs` esteso a **154** (da 46): espansione `giorni_settimana` vs calcolo INDIPENDENTE (JS `getDay` + festivi hardcodati) su 4 mesi × ~15 casi (elenco, intervallo, wraparound, degenere, token sconosciuti, escludi, sab/dom); spot: "da martedì a giovedì" = esatti mar/mer/gio senza lunedì; distinzione feriali↔giorni_settimana (dicembre 8/25 festivi in settimana); `diurniNascosti` vs calcolo indipendente (9 casi × 4 mesi) inclusa la **forma-intervallo (caso Iengo)** che deve far scattare il segnale sull'8 dicembre.
    - **Passo 2 — prompt + wiring (Opzione B).** Prompt: nuovo bullet in INSIEMI DI GIORNI (giorni-settimana nominati → `giorni_settimana`, elenco/intervallo, default `["N"]`, distinzione da "feriali", nota "ai festivi in settimana ci pensa il sistema") + forma aggiunta alla dichiarazione dell'azione `dispo_set`. Componente (nessun motore): `etichettaAmbito` gestisce `giorni_settimana`; in `applicaAzioni` il blocco `dispo_set`, **solo per `giorni_settimana`** (elenco E intervallo), dopo aver inserito i notturni chiama `diurniNascosti` e, se trova giorni, genera **UNA domanda Sì/No per la serie** (raggruppa i giorni): "❓ [nome]: i giorni … sono festivi/prefestivi e hanno anche il turno diurno — vuoi aggiungere anche il diurno?" → Sì = `dispo_aggiungi` G di quei giorni (stessa sede/livelli/preferito), No = lascia solo la notte. Mai aggiunto d'ufficio. Plumbing: `applicaAzioni` ritorna `domandeSuggerite`, mostrate da `applicaProposta`/`rispondiDomanda`.
    - **Verifiche.** `elaboraSchema` **byte-identico** (le 3 funzioni pure aggiunte non lo toccano; Passo 2 è tutto COMPONENTE → `engine_test.mjs` invariato rispetto al Passo 1); **206/206 unit** + 154/154 espansione + 245/245 equivalenza verdi; **sim 100.000 scenari: 0 violazioni su 34.341.312 check** (identica alla voce 33); esbuild OK; smoke Playwright pulito (0 errori console). `engine_test.mjs` e `docs/app.jsx` rigenerati e in sync. Limite noto: la costruzione della domanda diurno è glue di COMPONENTE (come il resto di `applicaAzioni`), non unit-testata; `diurniNascosti` che la alimenta è testata. Cablato solo per `giorni_settimana` (elenco+intervallo); gli intervalli di date numeriche `{da,a}` NON emettono il segnale (facile estenderli, `diurniNascosti` è generica).

44. **Affidabilità delle correzioni (8a/8b): verifica dello stato REALE + azione atomica `azzera_medico`** — due interventi collegati, emersi dal collaudo: l'AI a volte dichiarava di aver fatto qualcosa (azzerare, impostare un tetto) mentre nello stato reale l'operazione era incompleta/assente. **Nessuna modifica al motore di assegnazione** (`elaboraSchema` byte-identico); 2 funzioni PURE aggiunte + wiring COMPONENTE + prompt.
    - **8a — vedere lo stato REALE (dai dati, non "raccontato" dall'AI).** Funzione pura `statoRealeMedico(mid, dispo, maxTurniMese)` → oggetto strutturato letto dai dati veri: `disponibilita` (giorno/turno/verde+livelli/blu+livelli/preferito/NO, ordinati), `tettoMese`, `tettiSettimanali` (chiavi `SETT:`), `preferenzeTurno` (chiavi `TURNOPREF:`). Esposta in DUE modi che riusano lo stesso dato: (a) **comando chat** — l'AI risponde `{"tipo":"stato_medico","medico":"X"}` (read-only, nessuna conferma) e l'APP stampa il readout formattato (`formattaStatoReale`), mai l'AI a memoria; (b) **pannello nel tab Medici** — pulsante 🔍 per riga che espande lo stato reale del medico. Principio: il testo lo costruisce sempre il codice dai dati, così l'AI non può "raccontare" uno stato diverso dal vero.
    - **8b — azione atomica "azzera e reinserisci".** Nuova azione `{"az":"azzera_medico","medico":"X"}`: cancella IN BLOCCO tutte le disponibilità del mese di X (slot + tetti settimanali `SETT:` + preferenze turno `TURNOPREF:`) e il tetto mensile `maxTurniMese[X]`, lasciando INTATTI recupero ore e turni extra volontari (dati durevoli, non "disponibilità"). Funzione pura `azzeraDispoMedico(dispo, mid)`. In `applicaAzioni` una **PRE-PASSATA** applica tutti gli `azzera_medico` PRIMA di ogni altra azione dello stesso batch, qualunque sia il loro ordine → i reinserimenti (`dispo_set`/`dispo_aggiungi`/`tetto_*`) atterrano sempre sulla dispo già ripulita: "correggi da capo" atomico e **senza residui**, indipendente dall'AI (che nel collaudo lasciava giorni vecchi non tolti — caso Iengo). Il prompt istruisce a usarla per "rifai/correggi da capo X" con azzera_medico + i reinserimenti nello stesso elenco `azioni`. Aggiunte anteprima proposta + riepilogo.
    - **Verifiche.** `elaboraSchema` **byte-identico** (`engine_test.mjs` diff = solo `statoRealeMedico`+`azzeraDispoMedico`+export); nuovo `test_stato_azzera.mjs` **7/7** (readout corretto, read-only, azzeramento totale che non tocca altri medici né lascia `SETT:`/`TURNOPREF:`, purezza, e lo **scenario Iengo azzera+reinserisci senza residui**); **206/206 unit** + 154/154 espansione + 245/245 equivalenza; **sim 100.000 scenari: 0 violazioni su 34.341.312 check**; esbuild OK; smoke Playwright del pannello 🔍 (apre/chiude, 0 errori console). `engine_test.mjs` e `docs/app.jsx` rigenerati e in sync. Limite noto: il wiring in `applicaAzioni` e i formatter di presentazione sono glue di COMPONENTE (non unit-testati); le 2 funzioni pure che li alimentano sono testate.

45. **Campo "Max turni a settimana" nel tab Medici — SOLO interfaccia + wiring (motore byte-identico)** — il motore gestiva GIÀ il tetto settimanale (`settimanaDi`/`capSettimanale`, blocco in `candidatiOrdinati`, §3.8, chiavi `SETT:<lunedì>`), ma dall'interfaccia era impostabile solo via l'azione AI `tetto_settimana` per singola settimana. Aggiunto un **unico campo numerico** "Max turni sett." per medico accanto a "Max turni mese". Comportamento: valore N → scrive `dispo[mid]["SETT:"+lunedì] = {maxTurni:N}` per **TUTTE** le settimane ISO (lun-dom) che contengono almeno un giorno del mese corrente, **incluse le troncate ai bordi** (era il buco di De Candido: es. agosto 2026 → settimana del 27 lug che copre 1-2 ago, e quella del 31 ago); campo vuoto → rimuove tutte le chiavi `SETT:` del medico. Il valore mostrato è il tetto se **uniforme** su tutte le settimane del mese, altrimenti vuoto. L'enumerazione senza buchi ai bordi sfrutta il fatto che `settimanaDi(giorno)` restituisce il lunedì della settimana, quindi la distinta su tutti i giorni del mese copre esattamente le settimane che lo toccano. Il tetto settimanale a mano compare anche nel pannello "stato reale" (8a, voce 44), che legge le stesse chiavi `SETT:`. **NON tocca `elaboraSchema`**: `settimaneDelMese`/`capSettimanaleUniforme`/`setCapSettimana` sono helper di COMPONENTE che scrivono le chiavi lette dal motore. Verifiche: `engine_test.mjs` **byte-identico a HEAD** (UI-only), nuovo `test_tetto_settimana_ui.mjs` (enumerazione con bordi inclusi 27 lug/31 ago, salti di 7 giorni, scrittura/lettura via `capSettimanale`, svuotamento, mese corto febbraio), 206 unit + gli altri verdi, `docs/app.jsx` in sync, smoke Playwright end-to-end (imposta 2 → 6 tetti settimanali nello stato reale inclusi i bordi; svuota → "nessuno"; 0 errori console).
    - **Estensione — tetti DIVERSI per settimana (pannellino "per settimana", motore byte-identico).** Accanto al campo unico, un pulsante **📅** apre una riga espandibile (stesso pattern del pannello 🔍 stato) con **un campo per ogni settimana ISO che tocca il mese** (`settimaneDelMese`, bordi troncati inclusi), etichettata con l'intervallo lun→dom ("28 lug–3 ago") e un **pallino •** sulle settimane con festivo/prefestivo (rilevato via `turniDelGiorno`, es. la settimana di Ferragosto). Ogni campo scrive/rimuove SOLO la sua chiave `SETT:+lunedì` (`setCapSettimanaDi`), così una settimana può divergere dalle altre (es. "2 a settimana ma 1 a Ferragosto"). Il campo unico resta il caso normale ("riporta tutte le settimane allo stesso valore") e ora mostra placeholder **"misto"** + pulsante 📅 evidenziato quando le settimane non sono tutte uguali (`tettiSettMisti`). Tutto compare automaticamente nel pannello "stato reale" (8a), che elenca ogni `SETT:`. Solo COMPONENTE (`setCapSettimanaDi`/`tettiSettMisti`/`infoSettimana` + stato `settAperto` + riga espandibile): `elaboraSchema` intatto, `engine_test.mjs` **byte-identico a HEAD**. `test_tetto_settimana_ui.mjs` esteso a **9/9** (tetti divergenti `[2,2,1,2,2]` letti da `capSettimanale`, rimozione mirata di una settimana, uniforme vs misto, pallino festivo sulla settimana di Ferragosto); smoke Playwright end-to-end (pannello con 6 settimane + 1 pallino, una settimana a 1 → campo unico "misto", stato reale con `max 1` e `max 2` divergenti; 0 errori console).

46. **Settimana a cavallo di due mesi — vincolo settimanale dal mese precedente (PASSO 1: VINCOLO, V-A, motore byte-identico)** — ogni mese è elaborato in isolamento, quindi la settimana ISO a cavallo (es. quella del 27 lug che contiene 1-2 ago) non "vede" i turni fatti a fine luglio. Aggiunto l'input del coordinatore + il vincolo, **senza toccare `elaboraSchema`** (opzione V-A: si sfrutta che il motore legge già i tetti per-settimana `SETT:`). **Meccanica V-A:** per la sola settimana a cavallo la chiave diventa `SETT:cavallo = { maxTurni: EFFETTIVO, dichiarato }`, dove `dichiarato` è il tetto del coordinatore e `maxTurni = max(0, dichiarato − turniLuglio)` è ciò che **il motore legge** (`capSettimanale` legge `.maxTurni`, ignora `dichiarato` → nessuna modifica al motore). Un **unico punto di ricalcolo** (`valoreSett`/`toggleTurnoPrec`) ricompone effettivo+dichiarato dopo ogni cambio del tetto O dei turni di luglio, così il dichiarato non si perde mai. I **display helper** (`capDichiaratoDi`, e quindi `capSettimanaleUniforme`/`tettiSettMisti`) leggono il **dichiarato**: se il coordinatore mette 2 su tutte le settimane, il campo unico mostra **2** (non "misto") anche se il motore vede l'effettivo ridotto sul cavallo; la riduzione appare solo come annotazione ("2 − 1 lug → 1"). **Interfaccia (mini-calendario, dentro il 📅):** solo nei mesi che iniziano a metà settimana (giorno 1 ≠ lunedì, `settimanaCavallo`) e solo se il cavallo ha un tetto, un blocco mostra i giorni del mese precedente nella settimana ISO del giorno 1 (`giorniLuglioCavallo`, es. 27–31 lug) con checkbox **N** sempre e **G solo se quel giorno aveva davvero il diurno** (weekend/festivo/prefestivo, via `turniDelGiorno`). **Storage:** `dati.turniPrecedenti[mid][dataLuglio] = {N,G}` (per-giorno; le DATE servono al PASSO 2, il conteggio N+G al vincolo) — mai passato al motore. **Cosa NON fa:** non consuma il tetto MENSILE, non tocca le altre settimane. **Verifiche:** `engine_test.mjs` **byte-identico a HEAD** (`elaboraSchema`/`capSettimanale` intatti — solo aggiunto il campo `dichiarato`, ignorato dal motore); `test_tetto_settimana_ui.mjs` a **15/15** (rilevazione cavallo vs mese che inizia di lunedì; giorni 27-31 lug; dichiarato 2 + 1 luglio → motore vede 1 con dichiarato preservato; ricalcolo al variare di luglio incl. luglio ≥ tetto → 0; altre settimane intatte; senza tetto nessun effetto); **206 unit + gli altri verdi**; **sim 100k invariata (0 violazioni)**; smoke Playwright end-to-end (blocco cavallo con 5 celle N, 1 turno luglio → cavallo effettivo 1 nel motore, campo unico resta 2, 0 errori console). **PASSO 2 (distribuzione sopra-tetto) — FATTO:** aggancio a `elaboraSchema` di **esattamente 2 righe** (non più byte-identico, ma comportamentalmente identico al path di default). (1) firma: nuovo 8° parametro `riferimentiCavallo = {}`; (2) nel loop di ridistribuzione (`MEDICI.forEach`, riga ~948, il ramo che gira SOLO per chi ha `cap !== null` **e** supera il tetto), `giorniFissi` non parte più vuoto ma **seedato** coi giorni di luglio del medico: `const giorniFissi = [...(riferimentiCavallo[m.id] || [])]`. I giorni di luglio sono rappresentati come **offset non-positivo** (giorno D di luglio → `D − daysInPrevMonth`; luglio ha 31 giorni: 31→0, 30→−1, 27→−4), così `scegliConRiferimento` (farthest-point su `|c.giorno − g|`) allontana i turni TENUTI ad agosto dai turni di fine luglio, esattamente come già fa tra livelli. **Cosa NON cambia:** chi-vince (`isBetterPriority`), il tetto (numero di turni tenuti = `cap`), il monte ore, il pool dei candidati, la titolarità. Il seed influenza **solo la SCELTA di quali** turni eccedenti tenere, e **solo per chi supera un tetto** (sotto tetto la ridistribuzione non gira → ininfluente). **Lato componente:** nuovo helper `riferimentiCavalloDi()` che converte `dati.turniPrecedenti[mid][dataLuglio]={N,G}` in `{mid: [offset,…]}` (solo giorni con N o G), passato ai due call site (`elabora()` e `applicaAzioni` daElaborare). **Verifiche:** diff del motore = **2 righe esatte** (firma riga 807, `giorniFissi` riga 948); `test_cavallo_distribuzione.mjs` **5/5** (path di default include il 3 ago equidistante; col dato luglio i tenuti si allontanano — 3 ago escluso, `min(tenuti) > 3` — ma restano **4 = tetto**; `{}` identico a non passarlo; ogni turno tenuto resta di X; sotto tetto nessun effetto); intera suite unit verde; **sim 100k invariata (0 violazioni)** — la prova ampia che il path di default non regredisce.
47. **Azione `turno_precedente` — registrare in chat i turni di fine luglio (fase 3 AI, `elaboraSchema` intatto)** — terza via d'ingresso (dopo il checkbox 📅 e il vincolo V-A) per lo stesso campo `turniPrecedenti`: il **coordinatore** scrive in chat "BERTUZZI ha fatto un notturno il 30 luglio" e l'app registra il turno nella settimana a cavallo, riducendo il tetto effettivo del cavallo (V-A) e seminando la distribuzione (PASSO 2). **Divisione del lavoro (come `dispo_set`):** l'AI fa SOLO il linguaggio — estrae chi/giorno/`N`-`G` (se detto), `presente:false` per togliere; se manca il **giorno** chiede; **niente calcoli**. Il **MOTORE** (handler in `applicaAzioni`, piazzato PRIMA della validazione-giorno generica perché il giorno è del mese PRECEDENTE) fa tutte le verifiche di calendario con le funzioni deterministiche esistenti (`settimanaCavallo`/`giorniLuglioCavallo`/`turniDelGiorno`) e fa da **RETE DI SICUREZZA**, avvisando e NON registrando quando l'istruzione è impossibile: (1) mese senza cavallo (inizia di lunedì); (2) giorno fuori dalla settimana a cavallo / inesistente / non-luglio ("il 15 lug non è nella settimana a cavallo… i giorni sono 27–31"); (3) "diurno" su un feriale che ha solo il notturno ("il 29 lug è feriale, non esiste il diurno — intendevi il notturno?"). **Turno omesso:** sui feriali (solo N) default N; sui giorni con SIA diurno SIA notturno (weekend/festivo) è ambiguo → il sistema **CHIEDE** (domanda Sì/No "era il diurno?", `seSi`→G / `seNo`→N), non decide da solo. **Scrittura:** `turniPrecedenti[mid][data] = {N|G:true}` (SET, non toggle; `presente:false` toglie il turno o l'intero giorno) + ricalcolo del tetto effettivo del cavallo identico a `toggleTurnoPrec`, sul `dispo` LOCALE del batch. `turniPrecedenti` è ora **threaded** in `applicaAzioni` (copia locale + incluso nel `setDati` finale, accanto a dispo/extras/…). **Prompt:** nuova azione nel catalogo + regola **PASSATO vs FUTURO** (agganciata a MESE DI RIFERIMENTO FISSO): `turno_precedente` è l'UNICA azione che parla del mese precedente, SOLO da dichiarazione del coordinatore su un turno GIÀ SVOLTO ("ha fatto/ha coperto"); una **mail di disponibilità** di un medico (futuro, "sono disponibile") NON diventa MAI `turno_precedente`. **`elaboraSchema` intatto (`engine_test.mjs` byte-identico a HEAD):** tutto è in componente + prompt. **Verifiche:** `test_turno_precedente.mjs` **10/10** (registrazione → tetto ridotto + campo scritto senza perdere altri campi/medici; cancellazione via chat → tetto ripristinato; "toglilo" senza turno azzera il giorno; cavolate 1–4 → nessuna scrittura + avviso; feriale con turno omesso → N; giorno con G+N e turno omesso → domanda `seSi`/`seNo`; mese senza cavallo → avviso) — rispecchia l'handler appoggiandosi alle funzioni di calendario REALI del motore; intera suite unit verde; **corpus prompt** (`test_email_generator.mjs`) esteso con 3 categorie (`turno_precedente_registra` 10, `turno_precedente_mail_futura` 10 — TEST #1 la mail non lo innesca, `turno_precedente_giorno_mancante` 8 — TEST #7 l'AI chiede il giorno; 1478 casi / 39 categorie), verificabile solo con la run batch reale (API).
48. **Distribuzione temporale §3.11 — generalizzazione: sparge SEMPRE quando la disponibilità supera il tetto (non solo con cap esplicito < monte ore)** — la voce 20 correggeva l'ammucchiamento SOLO quando mordeva un `maxTurniMese` esplicito più restrittivo del monte ore (`cap < implicito`). Ma l'ammucchiamento nasce **ogni volta** che un medico è disponibile su più turni del proprio tetto (§3.11 = `min(monte ore implicito, Max turni mese)`), **indipendentemente dal perché** il tetto morde: il caso più comune — INDET disponibile tutte le notti + `tetto_mese:8` (= monte ore) — restava ammucchiato (`[1..8]`) perché il vecchio gate non scattava (`cap ≥ implicito`). **Causa** (dimostrata algebricamente): il gate testava `poolP1.length` (il pool del passaggio 1), ma il blocco monte ore §3.4 limita già quel pool a `≤ implicito ≈ tetto`, quindi `poolP1 > tetto ⟺ cap < implicito` — identico al vecchio gate, un no-op per il caso comune. **Fix** (motore, ~riga 916): il pool viene ricalcolato con l'oracolo per-medico esente dal blocco monte ore **ogni volta** che `tetto[m.id] != null` (tutti i contrattualizzati), non più solo quando `cap < implicito`. Più un **gate perf/correttezza-neutra**: il ricalcolo scatta solo se `poolP1.length === implicito` (m ha davvero esaurito il monte ore in P1; il pool esente ⊇ P1 e differisce SOLO in caso di esaurimento) — evita un `eseguiMese` inutile per ogni contrattualizzato non ammucchiato. **Cosa NON cambia (invarianti rigidi):** monte ore mai superato, budget turni extra mai superato, tetto mai superato, titolarità — solo *quali* notti cambia (sparse invece che consecutive), mai *quante*. **Verifiche:** BERTUZZI (`tetto_mese:8`, notturni tutto agosto) → `[1,5,10,14,18,22,27,31]` sparsi (era `[1..8]`); `test_distribuzione_temporale.mjs` **10/10** (4 casi che codificavano l'ammucchiamento aggiornati al comportamento sparso — incl. il titolare DET38 su 14 turni e il singolo candidato senza backup — + nuovo caso esplicito `tetto_mese:8`); `test_stesso_cat2.mjs` e `test_turni_extra.mjs` aggiornati (asserzioni basate sul CONTEGGIO — 9 turni / 2 extra — non più sulle posizioni consecutive); **sim 100k: 0 violazioni su 34.478.743 check**. **Effetto collaterale POSITIVO:** i residui §3.1a (limite noto tollerato) passano a **0 in 100.000 scenari** → i 2 test *positivi* seed-pinned del guardiano (voce 33) non trovano più il loro residuo e sono stati **rimossi** con nota (`test_guardiano_titolarita.mjs`: resta il test di non-falso-positivo; la verifica a scala è `INV-TITOLARE` nella sim). `docs/app.jsx` in sync.
49. **Slot obbligatori — punti fissi nella distribuzione §3.11** — un medico può marcare slot specifici che vuole tenere ASSOLUTAMENTE **se li vince per gerarchia**: entrano sempre nei turni tenuti, fanno da punti fissi attorno a cui l'equidistante costruisce, e **consumano il tetto** (tetto 8 + 3 obbligatori → 3 fissi + 5 distribuiti). Se il medico non è disponibile o non vince lo slot, è **ignorato silenziosamente**. **Dati:** nuova chiave ortogonale `dispo[mid]["OBBL:"+slotKey] = true` (stessa logica di `SETT:`/`TURNOPREF:`). **Motore (§3.11, ~riga 948):** nel loop di ridistribuzione, prima di riempire `kept`, si raccolgono gli obbligatori VINTI (`pool.filter(v => dm["OBBL:"+v.slotKey])`); vanno sempre in `kept`, si aggiungono a `giorniFissi` come seed, e `residuo = cap − obbligatori`; il resto va all'equidistante sul pool rimanente (escludendo gli obbligatori). Possono **scavalcare la priorità di livello** (un obbligatorio di livello peggiore è tenuto comunque); se sono più del tetto se ne tiene un sottoinsieme equidistante (tetto rigido). **Vuoto = comportamento identico a prima** (nessuna chiave OBBL: → residuo = cap, filtro `kept` vuoto). **Azione AI:** `{"az":"slot_obbligatorio","medico":…,"giorno":…,"turno":"N|G"}` (+`presente:false` per rimuovere), con frasi trigger ("il 15 lo voglio assolutamente", "il ferragosto lo faccio di sicuro"); non crea disponibilità. **Stato:** `statoRealeMedico` espone `slotObbligatori`; lo stato AI ha `slotObbligatoriPresenti` (e `disponibilitaPresenti`/`disponibilita` ora escludono le chiavi `OBBL:`, che altrimenti le inquinavano). **⚓ PIN SEDE:** il valore `OBBL:` può essere `true` (pin LIBERO, qualsiasi sede vinta va bene) **oppure una stringa-sede** (pin SEDE: scatta solo se la **sede vinta in P1** combacia, altrimenti ignorato). Per supportarlo, le entry del pool portano ora anche `sede` (nome della sede fisica vinta, `null` per i turni extra); il filtro `obblVinti` diventa `o === true || (typeof o === "string" && o === v.sede)`. Struttura invariata per i pin senza sede. L'azione `slot_obbligatorio` accetta un campo opzionale `"sede":"Spilimbergo"`; `statoRealeMedico.slotObbligatori` e `slotObbligatoriPresenti` (AI, formato `g15N@Spilimbergo`) espongono la sede. **UI:** nel popup cella la riga 📌 "Voglio questo turno" (attiva solo se Disponibile) + un ⚓ accanto alla ☆ di ogni sede fisica (pin sede-specifico); nella griglia badge 📌 in alto a sinistra (pin libero) e ⚓ in alto a destra (pin sede). **Fix ☆:** la stella inattiva era quasi invisibile (`T.border`) → ora grigia visibile (`T.textMuted`), dorata piena quando attiva. **MMG come punti fissi automatici (opzione C):** un turno MMG (extra) vinto in P1 è trattato come un obbligatorio automatico — **conta nel tetto** come un turno qualsiasi (invariante `test_max_turni_mese` INTATTO: l'MMG consuma il tetto ed è assegnato) **ma non è mai cedibile** dalla distribuzione, e il suo giorno fa da seed: le guardie libere si distribuiscono attorno ai giorni MMG (es. tetto 4 + 1 MMG → 1 MMG fisso + 3 notturni attorno). Implementato estendendo il filtro dei "punti fissi": `pool.filter(v => v.extra || <è OBBL vinto>)`; le entry del pool portano ora anche `extra`. **Perché non la variante "seed puro"** (aggiungere solo il giorno MMG a `giorniFissi` senza proteggere lo slot): l'MMG resta nel pool di cessione e — spinto via dal proprio stesso seed — verrebbe CEDUTO, perdendo un turno vinto e rompendo `test_max_turni_mese` (verificato empiricamente prima di scegliere C). **azzera_medico:** cancella anche gli `OBBL:` (è un wipe totale di `dispo[mid]`). **Verifiche:** `test_distribuzione_temporale.mjs` **14/14** (+ ⚓ pin sede: match → tenuto, mismatch → ignorato) (BERTUZZI `tetto_mese:8` + obbligatori 1N/15N/22N → `[1,4,8,11,15,22,26,31]` coi 3 fissi presenti e gli altri 5 attorno; obbligatorio non vinto → ignorato; obbligatorio di livello peggiore → tenuto scavalcando il livello); `test_stato_azzera.mjs` **7/7** (`slotObbligatori` letto, la chiave non inquina la disponibilità); intera suite unit verde; **sim 100k con generazione OBBL: casuale (pin liberi + pin sede) + MMG casuali — 0 violazioni su 34.475.533 check** (i rami dei punti fissi, del pin-sede e dell'MMG-ancora esercitati a scala, nessun invariante violato). `docs/app.jsx` in sync.

50. **Quattro fix al prompt AI in un solo commit — motore byte-identico** — tutti nel prompt di `chiediAI` (regione COMPONENTE), **nessuna modifica al motore** (`engine_test.mjs` byte-identico a HEAD, diff vuoto). Migliorano l'interpretazione delle email senza toccare l'assegnazione.
    - **Fix 1 — `tetto_mese` NON dedotto dal settimanale.** Quando un medico indica il numero SOLO su base settimanale ("massimo 2 a settimana") e da nessuna parte c'è un totale mensile dichiarato, l'AI imposta ORA **solo** i tetti settimanali (`tetto_settimana` ripetuto per ogni settimana lun-dom del mese), **senza** derivare un `tetto_mese ≈ N × settimane`. Il motore applica già il vincolo settimana per settimana e il tetto mensile è comunque implicito nel monte ore contrattuale — il vecchio `tetto_mese` derivato era una forzatura non dichiarata. **Supera la decisione della voce 37 Regola 4 / L698** (che imponeva sia il settimanale sia il mensile convertito): la conversione a mensile era arbitraria e restringeva più del dovuto. Nuova spiegazione: "ℹ️ [nome] ha indicato [N] turni a settimana → impostato tetto [N]/settimana su tutte le settimane del mese. Nessun tetto mensile (non dichiarato)." (Nota: il corpus `test_email_generator.mjs` categoria `tetto_settimanale` è già su una SINGOLA settimana e non codificava il `tetto_mese` derivato → nessuna modifica al corpus richiesta.)
    - **Fix 2 — `turni_extra` dedotti autonomamente.** Nuova regola "CALCOLO AUTONOMO" in TURNI EXTRA VOLONTARI: se il coordinatore imposta per un CONTRATTUALIZZATO una disponibilità o un tetto che implica PIÙ turni del monte ore mensile (es. `tetto_settimana × settimane del mese > monte ore implicito = round(debito/12)`), l'AI calcola la differenza e aggiunge AUTOMATICAMENTE `{"az":"turni_extra","turni":N}` insieme alle altre azioni, senza aspettare una richiesta esplicita. Esempio: BERTUZZI INDET (96h = 8 turni impliciti), tetto 2/settimana × 5 settimane = 10 possibili → `turni_extra:2`. Vale SOLO per i contrattualizzati (i senza incarico non hanno monte ore).
    - **Fix 3 — `slot_obbligatorio`: frasi trigger estese + conferma su richiesta del medico.** Nel catalogo azioni, estese le frasi trigger per il pin sede (⚓: "il 15 lo voglio ma solo a Spilimbergo", "il 22 solo a Maniago"), il pin libero (📌: "voglio assolutamente il 15 notturno", "il 22 sera non lo cedo") e la **rimozione** ("togli il pin dal 15", "non vincolare più il 22", "il 15 non è più obbligatorio"). Aggiunta una regola: quando è il **MEDICO** (non il coordinatore) a esprimere una preferenza FORTE e SPECIFICA su un singolo giorno+turno, l'AI NON applica `slot_obbligatorio` d'ufficio ma lo **PROPONE con una domanda di conferma al coordinatore** (meccanismo "domande", `seSi` = lo slot_obbligatorio) prima di vincolarlo.
    - **Fix 4 — CONFERMA IMPLICITA copre le nuove azioni.** La REGOLA GENERALE: CONFERMA IMPLICITA ora dice esplicitamente che vale per QUALSIASI azione proposta, inclusi `slot_obbligatorio` (il pin proposto con conferma, Fix 3) e i `turni_extra` dedotti autonomamente (Fix 2): "Aggiungo 2 turni extra a BERTUZZI per coprire il tetto?" → "sì" → emette `turni_extra:2`.
    - **Verifiche.** `engine_test.mjs` **byte-identico a HEAD** (diff `--stat` vuoto — cambia solo la regione COMPONENTE); esbuild OK; intera suite unit verde (distribuzione 15/15, stato_azzera 7/7, turni_extra 7/7, max_turni_mese 7/7, equivalenza 245/245, espansione 154/154, ecc.); `docs/app.jsx` rigenerato e in sync. Sim 100k NON necessaria (motore intatto). Verifica del comportamento AI dal vivo (14 casi) rimandata: bloccata dall'assenza di credito API.

51. **Calendario precompilato nello STATO ATTUALE — l'AI legge, non calcola più a mente (motore byte-identico)** — quinto fix della stessa tornata, ma **non prompt-only**: risolto un difetto di correttezza per cui la sezione prompt "CALENDARIO PERPETUO" faceva calcolare all'AI, con la congruenza di Zeller + Gauss (Pasqua) + tabella festività, se un giorno fosse feriale/weekend/festivo/prefestivo e se avesse il diurno G — un calcolo a memoria error-prone. Il campo calendario **non era passato** nello `stato`: c'erano solo `mese` (stringa), `mmgAttivi` e — solo post-elaborazione — `schema`. La richiesta iniziale ("leggi il calendario nello stato") era quindi falsa contro il codice; **scelta Opzione A** (rendere vera la regola) invece della sola riformulazione del prompt (Opzione B, che avrebbe lasciato il calcolo a mente).
    - **Motore byte-identico:** nessuna modifica a `elaboraSchema` (`engine_test.mjs` diff `--stat` vuoto). Il nuovo campo è costruito nel COMPONENTE (builder dell'oggetto `stato`) riusando le funzioni pure già esistenti `turniDelGiorno`/`FESTIVI_MAP`/`PREFESTIVI`.
    - **Nuovo campo `stato.calendario`:** oggetto `{ "g{giorno}": "{dow} {tipo} · {turni}" }` per OGNI giorno del mese in lavorazione, dove `{dow}` = giorno-settimana (dom..sab), `{tipo}` = feriale | weekend | festivo | prefestivo (priorità festivo > prefestivo > weekend > feriale), `{turni}` = "diurno+notturno" (esiste G+N) oppure "solo notturno" (feriale semplice). Es. agosto 2026: `g14:"ven prefestivo · diurno+notturno"`, `g15:"sab festivo · diurno+notturno"` (Ferragosto, il festivo scavalca il weekend), `g17:"lun feriale · solo notturno"` — verificati contro la funzione reale.
    - **Prompt riscritto:** la sezione "CALENDARIO PERPETUO — … Zeller/Gauss" diventa "CALENDARIO DEL MESE — LEGGI LO STATO, NON CALCOLARE": punta al campo `calendario`, ne descrive il formato, e i punti 1–5 (congruenza di Zeller, festività fisse, Gauss per Pasqua, prefestivo, weekend) sono **rimossi** e sostituiti da "leggi il campo". **Preservati intatti** nella stessa sezione: MESE DI RIFERIMENTO FISSO, PASSATO vs FUTURO, e la regola "nessun ragionamento/calcolo nel testo visibile, solo il JSON". Aggiunta anche (Fix #5 della richiesta) la REGOLA GENERALE in testa a INTERPRETAZIONE EMAIL: "⚠️ CALENDARIO — NON RAGIONARE MAI DA SOLO … leggi SEMPRE il campo calendario nello STATO ATTUALE" (ora vera perché il campo esiste).
    - **Perché è sicuro togliere il calcolo a mente per il mese precedente:** l'unica azione che tocca il mese precedente (`turno_precedente`, voce 47) fa TUTTE le verifiche di calendario nel MOTORE (`settimanaCavallo`/`giorniLuglioCavallo`/`turniDelGiorno`), l'AI non calcola date lì; e MESE DI RIFERIMENTO FISSO àncora ogni altra data al mese in `stato` — esattamente ciò che `calendario` copre. Nessun caso residuo richiede il calcolo a memoria.
    - **Verifiche.** `engine_test.mjs` **byte-identico a HEAD**; esbuild OK; intera suite unit verde (distribuzione 15/15, espansione 154/154, equivalenza 245/245, stato_azzera 7/7, cavallo 5/5, e tutte le altre); `calendario` verificato contro `turniDelGiorno` su 7 giorni-trappola di agosto 2026 (1 weekend, 13 feriale, 14 prefestivo, 15 Ferragosto festivo, 16 weekend, 17 feriale, 31 feriale); `docs/app.jsx` rigenerato e in sync. Sim 100k non necessaria (motore intatto). Verifica AI dal vivo rimandata (no credito API). Nota: la sezione "CASI DA SEGNALARE"/"WEEKEND AMBIGUO" e le regole diurno/notturno continuano a funzionare identiche, ora alimentate dal campo invece che dal calcolo.

52. **Turni MMG con SEDE — unificati ai turni ordinari (bug strutturale, motore)** — i turni MMG (`turno.extra===true`, mattina M / pomeriggio P) avevano una logica di assegnazione SEPARATA e senza sede: `elaboraTurno` faceva `slots=[ordinati[0].id]` (il primo per gerarchia, sede ignorata) e il rendering li mostrava SEMPRE a Maniago (hardcode). **Cicatrice all'origine:** l'assunzione "il MMG si svolge sempre a Maniago" era stata fatta autonomamente, senza conferma del coordinatore. Fix: un MMG passa ORA dalla STESSA FASE 1/FASE 2 degli ordinari (sede, titolarità, gerarchia, preferenze, livelli), con l'UNICA differenza legittima delle ore (6 invece di 12) e il fatto che esiste solo se attivato dal coordinatore. **Decisioni di business confermate dal coordinatore prima di implementare** (design-first, per non ripetere la cicatrice): **1a** l'MMG resta punto fisso protetto in §3.11 (voce 49, opzione C: conta nel tetto ma non è mai ceduto); **2a** eleggibile SOLO chi ha dichiarato la sede ATTIVATA (come gli ordinari; nessuno l'ha dichiarata → SCOPERTO); **3a** sede mancante (dati vecchi) → turno attivo ma scoperto + avviso, nessun blocco; + FASE 2 blu attiva (il fisico può coprire un'altra sede a distanza) e il dropdown sede nel tab MMG.
    - **Struttura dati.** L'azione diventa `{"az":"mmg","giorno":16,"fascia":"M","sede":"Spilimbergo","attivo":true}`; salvata in `extras` come `{ M:true, M_sede:"Spilimbergo" }` (idem `P`/`P_sede`). `turniDelGiorno` costruisce il turno MMG con `sede: ex.M_sede||null`. Disattivare azzera la sede.
    - **Motore (`elaboraTurno`).** Rimosso il branch `if(turno.extra)` separato; il MMG entra nel percorso ordinario con `sediFisiche` = **la sola sede attivata** (`[SEDI5.indexOf(turno.sede)]`, imposta — niente ramo dinamico `nFisici===1` che sceglierebbe la sede dalla preferenza del medico), `target=sediFisiche`. Sede mancante → `sediFisiche=[]` → scoperto + avviso di migrazione. `turnoOut` ora espone `sede`. Unificati anche `livelloVintoDi`, `estraiVinti`, il build del pool §3.11, `aggiornaContoMensile` e la valutazione dei ★ preferiti: tutti leggono i FISICI (`r.fis`) come per gli ordinari. **L'unica cosa conservata dal vecchio ramo è il flag `extra:true` sulle entry del pool** — è ciò che rende l'MMG un punto fisso protetto (1a): `obblVinti` filtra `if(v.extra) return true`. Il guardiano titolarità continua a saltare gli `extra` (avviso additivo, conservativo).
    - **Rendering.** Excel: rimosso l'hardcode Maniago, il vincitore compare nella colonna della sede attivata (letta dal risultato del motore, `notaSlot` per le coperture a distanza), solo la sede attivata è segnalata SCOPERTO. Griglia schema: l'MMG usa le 5 colonne SEDI5 come un ordinario, `daCoprire=[sede attivata]`, badge "sede MMG mancante" in migrazione. Tab "2 · Coperture MMG": accanto a ogni checkbox Mattina/Pomeriggio un `<select>` sede (bordo rosso se attivo senza sede). Stato AI: `mmgAttivi` mostra la sede (`g15:M@Spilimbergo`); `schema` espone `sedeMMG` + la mappa `sedi` completa.
    - **Prompt.** Azione `mmg` con campo `sede` obbligatorio all'attivazione (+ frasi trigger e la sede nelle domande di attivazione "MMG non attivo"); `mmgAttivi` documentato col formato `@sede`.
    - **Verifiche.** Nuovo `test_mmg_sede.mjs` **5/5** (eleggibilità per sede 2a, gerarchia a parità di sede, scoperto se nessuno dichiara la sede, migrazione 3a senza blocco, copertura a distanza FASE 2); aggiornati i 4 test che assumevano `slots[0]` sede-less (`test_max_turni_mese`, `test_distribuzione_temporale` — l'ancora MMG voce 49 ora con `M_sede`, `test_spaziatura_settimana`, `test_preferiti2` — avviso ★ ora sede-based); **intera suite unit verde**; **sim 100k con MMG generati CON sede casuale + invariante MMG unificato (fis-based) — 0 violazioni su 36.306.568 check** (il conteggio sale da 34.4M perché ora anche i MMG assegnati passano dai controlli fisici); smoke Playwright end-to-end (MMG a Spilimbergo → medico nella colonna Spilimbergo, non Maniago; MMG senza sede → badge "sede MMG mancante"; dropdown sede nel tab; 0 errori console). `docs/app.jsx` rigenerato e in sync; esbuild OK.

53. **Tab 2 (Coperture MMG) — copertura a distanza del coordinatore + solo feriali** — tre aggiunte al tab di attivazione MMG, seguito della voce 52. Le due UI sono pure; la copertura a distanza tocca il motore (scelta 1b del coordinatore, NON byte-identica ma isolata agli MMG).
    - **Copertura a distanza (blu) scelta dal coordinatore.** Accanto al dropdown sede di ogni fascia MMG attiva, un secondo dropdown "a distanza" (compare solo quando c'è la sede fisica), con opzioni filtrate dai vincoli territoriali reali (`sediBluRaggiungibili`: Claut solo da Maniago, Anduins solo da Spilimbergo/Meduno — stessi di `risolviBlu`). Salvato in `extras` come `M_blu`/`P_blu`; `turniDelGiorno` lo porta su `turno.blu`. **Motore:** nel SOLO path MMG di `elaboraTurno` (FASE 2), `turno.blu` viene attribuito al VINCITORE fisico come sua blu effettiva **in una copia LOCALE della dispo** (mai mutata quella reale), poi `risolviBlu` la copre con gli stessi vincoli territoriali degli ordinari; se non raggiungibile dalla sede attivata, resta scoperta. Sul percorso ordinario `turno.blu` è undefined → `dispoBlu===dispo` → **comportamento invariato**. `turnoOut` espone `blu` (per rendering/sim). Non consuma tetto/debito (la copertura a distanza non conta mai, come per gli ordinari). Prompt: campo opzionale `blu` nell'azione `mmg`; `mmgAttivi` mostra `M@Spilimbergo→Anduins`.
    - **Solo feriali semplici nel tab.** Le checkbox Mattina/Pomeriggio compaiono SOLO per i feriali semplici: weekend, festivi e prefestivi sono nascosti (in quei giorni il MMG è "anticipo diurno" già gestito dal calendario). La classificazione viene SEMPRE da `turniDelGiorno` (`giorniMese[i]` ne è l'output: `g.weekend || g.festivo || g.prefestivo` → `return null`), mai a memoria.
    - **Verifiche.** `test_mmg_sede.mjs` a **7/7** (+2: `M_blu` coperta dal vincitore anche se non dichiarata da lui; `M_blu` territorialmente bloccata → scoperta); sim 100k con `M_blu`/`P_blu` casuali + invariante copertura-a-distanza che accetta `t.blu` per gli MMG — **0 violazioni** (conteggio nel commit); intera suite unit verde; smoke Playwright (tab con solo feriali; dropdown "a dist…" filtrato per territorio; 0 errori console). `docs/app.jsx` in sync; esbuild OK.
    - **Nota sim — raffinamento della tolleranza INV-TITOLARE (non un cambio motore).** Lo shift dello stream RNG del generatore (le nuove estrazioni `M_blu`/`P_blu`) ha fatto emergere in seme=552/2031-1 un caso di limite noto §3.1a su un **NOTTURNO** (BEKAEVA titolare di Maniago spostata mentre Maniago va a un non-titolare, con **4 contrattualizzati che contendono Maniago**: ZURLO/BEKAEVA/FOSCHIANI titolari + MARTINETTI). Non è una regressione: turno ordinario, path del motore byte-identico (la copertura MMG `M_blu` è gated su `turno.extra`), e `provaFisica`/`correggiTitolarita`/`isBetterPriority` non sono stati toccati. L'euristica di tolleranza della sim contava i contrattualizzati **fisicamente presenti** (`t.fis`, qui 2) invece dei **contendenti la sede contesa** (qui 4): la catena di ricollocazione spinge fuori alcuni contendenti (verso una blu/inutilizzati), quindi la presenza fisica sottostima la catena — rischio già annotato nel commento (caso seed 28). Corretta l'euristica: conta i contrattualizzati che hanno dichiarato la sede contesa come verde (≥3 → limite noto tollerato e RIPORTATO; ≤2 → violazione vera → fallimento). Tolleranza sempre stretta (un vero bug a 2 corpi resta hard), classificazione più fedele a §3.1a.

54. **Distribuzione §3.11 — `scegliConRiferimento` ottimale: minimizza il gap massimo (non più greedy farthest-point)** — quando ci sono giorni FISSI (`giorniFissi` non vuoto: obbligatori/MMG/settimana a cavallo/livelli di sede migliori), la scelta di quali turni eccedenti tenere passa da un greedy farthest-point (che massimizzava il gap MINIMO) a un algoritmo che **minimizza il GAP MASSIMO** tra giorni consecutivi dell'insieme completo (pin + scelti), code ai bordi dello span incluse. È la formalizzazione corretta di "distribuzione uniforme": niente buchi lunghi. **Analisi a monte** (enumerazione esatta): sul caso BERTUZZI (notti 1-31, pin 3+22, tetto 8 → 6 liberi, C(29,6)=475.020) l'ottimo è gap massimo **4** (50 combinazioni); il vecchio greedy dava **5**. Su 4.000 casi random il greedy era subottimo nel **~78%** dei casi (in media +1,69 giorni di buco, max +8). Il vecchio `[1,4,8,11,15,22,26,31]` (obbligatori 1/15/22) lasciava il buco pin-pin 15→22=7; il nuovo `[1,6,11,15,20,22,27,31]` lo riempie (gap max 5).
    - **Algoritmo (polinomiale, nessuna enumerazione):** ricerca binaria sul gap massimo G + copertura greedy (giorni-candidato minimi per tenere ogni gap ≤ G, farthest-reach — ottimo classico); il G minimo con "servono ≤ n" è l'**ottimo esatto**. Poi ricostruisce e rifinisce i turni residui col farthest-point. **Costo ~0,01 ms** anche col caso peggiore reale (pool 31, n=14) — nessun cliff (il brute force puro sarebbe C(31,14)=2,3 mld → freeze; scartato). Ragiona sui **giorni distinti** (due turni lo stesso giorno sono a distanza 0), poi mappa agli slot; se n eccede i giorni distinti aggiunge gli slot residui. Deterministico.
    - **Casi base INVARIATI:** senza pin (`giorniFissi` vuoto) resta l'equidistante posizionale (`scegliIndiciEquidistanti`) → la stragrande maggioranza della sim e dei test non cambia; `n ≥ candidati` → tutti.
    - **Verifiche.** Nuovo `test_scegli_riferimento.mjs` **6/6** (BERTUZZI gap 4 = ottimo brute; riempimento buco 15→22; **ottimalità su 300 casi random vs brute force**; casi base invariati; giorni duplicati → esattamente n slot; determinismo). Aggiornati i 2 test con pin in `test_distribuzione_temporale.mjs` (CROSS-LIVELLO `[1,7,30]`→`[1,7,28]` gap 21<23; obbligatori `[1,4,8,11,15,22,26,31]`→`[1,6,11,15,20,22,27,31]` gap 5<7). `scegliConRiferimento`/`scegliIndiciEquidistanti` ora esportate per il test. **Sim 100k: 0 violazioni** (cambia solo QUALI turni, mai QUANTI → tutti gli invarianti di tetto/monte ore/titolarità intatti). `docs/app.jsx` in sync; esbuild OK.

55. **MMG — REVERSIONE della sede/copertura scelte dal coordinatore: l'MMG compete come un turno ordinario** — annulla la parte "coordinatore sceglie la sede/blu" delle voci 51/53: un turno MMG (M/P) è attivato SOLO come esistenza (checkbox), e per il resto compete ESATTAMENTE come un turno ordinario — la **sede e la copertura a distanza le decide il motore** in base alle disponibilità dei medici (verde/blu nel tab 1). Resta della voce 52 il concetto giusto: MMG dentro FASE 1/FASE 2 come un ordinario, e MMG vinto = **punto fisso protetto in §3.11** (`extra:true` sul pool entry, non cambia). Struttura di sedi identica a un notturno: Maniago/Spilimbergo/Meduno fisiche, Claut/Anduins solo a distanza (`sediFisiche = turno.id === "G" ? [0..4] : [0,1,2]`, e M/P → [0,1,2]). Uniche differenze legittime dagli ordinari: ore 6 e "esiste solo se attivato".
    - **Rimosso ovunque** `M_sede`/`P_sede`/`M_blu`/`P_blu`: da `extras`, da `turniDelGiorno` (il turno MMG torna `{id,label,ore:6,extra:true}` senza `sede`/`blu`), da `turnoOut`, dall'azione `mmg` (solo `giorno`/`fascia`/`attivo`), da `mmgAttivi` (torna `g15:MP`), dal prompt (nessun campo `sede`/`blu`), dallo `stato.schema` (niente `sedeMMG`). Rimossi dal COMPONENTE `sediBluRaggiungibili`/`setExtraSede`/`setExtraBlu`; il tab 2 ha di nuovo solo le checkbox Mattina/Pomeriggio (nessun dropdown). Motore: rimossi il target forzato `if(turno.extra) target=sediFisiche`, l'iniezione `M_blu` in FASE 2 (`risolviBlu` su `dispo` puro), e l'avviso di migrazione "sede MMG mancante"; scoperte su tutte e 5 le sedi come un ordinario. Rendering (Excel + griglia): l'MMG usa il percorso ordinario — nella griglia si comporta come un notturno (`treFisiche = isNotte || isExtra`: Claut/Anduins a distanza). Corretta anche l'azione `schema` (override manuale): `si = SEDI5.indexOf(a.sede)` per tutti (non più `t.extra ? 0`).
    - **Verifiche.** `test_mmg_sede.mjs` riscritto **5/5** (la sede viene dalla dispo non dal coordinatore; sede contesa → vince il più forte; copertura a distanza dalla blu del medico; nessun disponibile → scoperto; MMG conta nel tetto ed è punto fisso §3.11). Ripuliti i `M_sede` morti nei test che li passavano (max_turni/distribuzione/spaziatura/preferiti2, tutti verdi — il medico dichiara la sede nella dispo e vince come prima). Generatore sim tornato a `{M,P}` senza sede/blu; invariante copertura-a-distanza torna a richiedere la blu del medico. **Intera suite unit verde**; **sim 100k: 0 violazioni**. `engine_test.mjs`/`docs/app.jsx` rigenerati; esbuild OK. (La tolleranza INV-TITOLARE per contendenti-la-sede, voce 53, resta: è indipendente e più fedele a §3.1a.)

56. **Distribuzione §3.11 — `scegliConRiferimento`: tiebreak COPERTURA A DISTANZA (blu) come swap-nudge** — quarto parametro opzionale `giorniBlu` (Set di numeri-giorno su cui il medico ha una blu dichiarata). A parità di **gap massimo OTTIMALE** (l'invariante min-maxGap della voce 54 resta primario e intatto), un **post-pass di soli scambi** sostituisce un giorno scelto SENZA blu con uno scartato CON blu, ma **solo se non peggiora il gap oltre l'ottimo `best`** (metrica `maxGapDi` = diff massima consecutiva su `{L,R} ∪ fissi ∪ scelti`, identica al `maxGapTail` dei test). Conteggio invariato (`kGiorni`), deterministico (iterazione ordinata). **Motivazione empirica**: la blu è memorizzata **per-slot** (`dispo[mid]["data|turno"].blu`), quindi PUÒ variare per-giorno dello stesso medico; il prompt AI la genera parziale via `dispo_set` su un sotto-periodo (es. "copro Maniago a distanza dal 10 al 20"). Misura effetto-blu puro (max-blu vs max-sum): blu **uniforme → 0,0000%** (inerte); blu **per-giorno → +~2%** copertura a distanza (sia in scarsità che in abbondanza). Lo swap-nudge cattura quel +2% **solo sul caso a blu parziale**, a costo zero di spaziatura.
    - **No-op garantito**: `giorniBlu` assente/`null`/vuoto → guard falsa → output byte-identico; blu **uniforme** (tutti i giorni-candidato in `giorniBlu`) → nessun giorno scelto è "senza blu" → zero swap → identico. Scatta **solo su blu PARZIALE**. Casi base (`n<=0`, `n>=candidati`, ramo equidistante `!giorniFissi`) **invariati**, ignorano `giorniBlu`. `elaboraSchema`: `giorniBluDelMedico` calcolato una volta per medico dal `pool` (`normDispo(dm[slotKey]).blu.length`), passato a entrambe le chiamate (obbligatori + per-livello); gli MMG (`extra`, blu vuota per voce 55) non entrano mai.
    - **Verifiche.** `test_scegli_riferimento.mjs` **9/9**: i 6 esistenti (ottimalità min-maxGap vs brute force) restano verdi → l'ottimo non regredisce; +3 blu (lo swap preferisce il giorno con blu a gap ottimo invariato; blu uniforme/vuota/null byte-identica; 300 casi random → gap sempre ottimo, copertura blu mai in calo, in ≥1 caso migliora). Generatore sim: una frazione (~25%) dei medici con blu dichiara la copertura a distanza **solo su una finestra `{da,a}`** del mese (blu per-giorno) → esercita lo swap-nudge sotto gli invarianti. **Intera suite unit verde**; **sim 100k: 0 violazioni**. `engine_test.mjs`/`docs/app.jsx` rigenerati; esbuild OK.

57. **Vincolo di FINESTRA SETTIMANALE — minimo di turni voluti in una settimana ISO** — nuova chiave `dispo[mid]["SETTWK:"+lunedì] = N` (helper `finestraSettimanale`), ORTOGONALE a `SETT:` (massimo settimanale) e `OBBL:` (slot preciso). È un **minimo**: il coordinatore dà solo il numero, il **motore sceglie autonomamente QUALI** N turni tenere tra quelli vinti per gerarchia in quella settimana, li àncora e distribuisce il resto del mese attorno (min-maxGap). Chiave **normalizzata al lunedì** (come `SETT:`, via `settimanaDi`): "settimana del 10" e "del 12" = stessa chiave (nodo ①). Motore in **due parti** dentro `elaboraSchema`: **Part A (bias, nel loop §3.11)** — per ogni settimana con minimo, forza i migliori N vinti (livello ↑, poi cronologico — nodo ③) unendoli a `obblVinti`, con gli `OBBL/MMG` già presenti in quella settimana che **contano verso il minimo** (nessun doppio conteggio: `bisogno = max(0, N − giàPin)`); **Part B (report, post-schema)** — sullo schema definitivo conta i turni fisici realmente tenuti nella settimana e, se `< N`, emette un avviso distinguendo il motivo ("ne ha vinti per gerarchia solo M" vs "limitato dal tetto mensile o dalla distribuzione"). Il **tetto mensile resta RIGIDO** (nodo ②a): se i minimi non entrano nel tetto, la gestione `obblVinti>cap` decide e Part B avvisa — mai una violazione del tetto per soddisfare un minimo. Le **settimane troncate** ai bordi del mese contano SOLO i giorni in-mese (il `pool` è già in-mese): se offrono meno di N si tengono tutti + avviso, mai contando giorni del mese precedente.
    - **Azione AI** `{"az":"finestra_settimanale","medico","giorno","minTurni"}` (`presente:false` o `minTurni 0` → rimuove); `giorno` identifica la settimana (normalizzata al lunedì). Prompt: sezione dedicata distinta da `tetto_settimana`/`slot_obbligatorio` con frasi trigger ("voglio almeno 3 turni la settimana del 10", "nella settimana di Ferragosto almeno 2"). Stato: `finestreSettimanali` in `statoRealeMedico` (+ `formattaStatoReale`, + contesto AI, + filtri `disponibilita*` che escludono `SETTWK:`); `azzeraDispoMedico` le cancella (azzera l'intero oggetto del medico). Preview azione + anti-loop come le sorelle settimanali.
    - **UI**: nel pannellino 📅 (tab Medici) ogni box-settimana ha ora un secondo campo **"min"** sotto al tetto (`setMinSettimanaDi`), editabile anche sulla settimana a cavallo (nodo ④). **Pannello avvisi (punto 5)**: banner **collassabile sopra la griglia** nel tab 4 (Schema), che mostra tutti gli `avvisi` del motore (finestre non soddisfatte, sede preferita, titolarità) — visibile solo dopo l'elaborazione, **NON in Excel** (solo aiuto a schermo, prima dell'export).
    - **Verifiche.** Nuovo `test_finestra_settimanale.mjs` **10/10** (bias tiene ≥N e distribuisce; controprova senza vincolo; vinti<N→tieni tutti+avviso; conflitto tetto→rigido+avviso motivo-tetto; finestra+OBBL non doppio-contati con tetto stretto; **settimana troncata inizio/fine mese** conta solo i giorni in-mese, mai luglio; troncata soddisfacibile→nessun avviso; chiave normalizzata al lunedì; stato+azzera). Sim: generatore emette `SETTWK` occasionale (12%) + nuova **INV-FINESTRA** (nessuno shortfall SILENZIOSO: minimo non soddisfatto ⇒ avviso obbligatorio). **Intera suite unit verde**; `test_stato_azzera` aggiornato per la nuova chiave dello stato vuoto; **sim 100k: 0 violazioni**. `engine_test.mjs`/`docs/app.jsx` rigenerati; esbuild OK.

58. **UI — sfoltimento dei testi esplicativi (teoria/spiegoni via, chiarimenti non-ovvi tenuti)** — pulizia puramente estetica del COMPONENTE (motore intatto, `engine_test.mjs` invariato). Criterio: **RIMUOVERE** spiegoni di funzionamento, paragrafi di teoria, descrizioni del motore e istruzioni ovvie; **TENERE** (in forma compatta) ciò che non è intuibile da un utente nuovo: significato dei simboli (📌/⚓/★/pallino verde-blu), vincoli territoriali (Claut solo da Maniago, Anduins solo da Spilimbergo/Meduno a distanza), differenze semantiche tra concetti simili (tetto=massimo vs minimo settimanale, pin libero vs pin sede).
    - **Rimossi**: intro Inserimento rapido, help del tetto settimanale nel rapido, "Dopo puoi correggere…", caption "se lo vinco…", il `<p>` teoria del tab Medici (6 frasi), + i `title` di tooltip di sola teoria (iniquità, colonne Turni extra/Max mese/Max sett., titolarità lunga — quella breve resta —, extra ottenuti, toggle stato reale 🔍, toggle turni precedenti).
    - **Ridotti a legenda/nota compatta**: `<p>` tab 1 → *"📌 slot obbligatorio · ⚓ obbligatorio solo su una sede · pallino verde = sede fisica, blu = a distanza · Claut solo da Maniago, Anduins solo da Spilimbergo/Meduno (max 1 a distanza)"*; help popup cella → *"Di notte Claut e Anduins solo a distanza; ☆ = sede preferita (non decide chi vince)"*; `<p>` tab MMG → *"Compaiono solo i feriali semplici (weekend e festivi esclusi)"*; `<p>` tab Schema → *"Stesso nome su più sedi = copertura a distanza"*; label rapido "Sedi (verde = in sede, blu = a distanza)" e "Periodi non disponibili (es. ferie)"; tooltip 📌 popup → *"📌 lo tieni se lo vince; ⚓ = solo se ottiene quella sede"*; tetto sett. → *"Massimo turni/settimana (per valori diversi usa 📅)"*; min (voce 57) → *"Minimo turni/settimana: il motore sceglie quali"*.
    - **Verifiche**: esbuild OK; **Playwright smoke** (4 tab renderizzano, pannello 📅 col campo "min" ok, Assistente AI apre, zero errori console/pagina); `docs/app.jsx` rigenerato (propagazione identica). Nessun test da aggiornare (UI pura).

59. **Pulizia dead code (componente)** — audit con ESLint (`no-unused-vars`/`no-unreachable`) + `tsc --noUnusedLocals/Parameters`. Rimossi 3 elementi morti nel COMPONENTE (motore intatto, `engine_test.mjs` invariato): le costanti di stile `btnPrimary` e `card` (mai referenziate) e la variabile locale `const t = schema[gi].turni[ti]` nell'handler azione `schema` (l'aggiornamento usa `ti/si/mid`, `t` mai letto). **Volutamente NON toccati** (decisione del coordinatore): i param inutilizzati `turno` in `livelloVintoDi`/`aggiornaContoMensile` (regione motore red-line, churn regen+sim non giustificato) e i cosmetici (`catch (e)` inutilizzati, destructuring `[y,m]`). `giorniTra` **tenuto**: risulta "unused" nell'app ma è esportato e testato da `test_spaziatura_settimana.mjs`. Audit manuale: nessun residuo di feature rimosse (piene/ripiego/M_sede) in codice — solo in commenti-doc/prompt (intoccabili); 0 branch irraggiungibili; 0 state write-only; 0 import inutilizzati. **Verifiche**: esbuild OK; ESLint 11→8 warning; Playwright smoke verde; `docs/app.jsx` rigenerato.

60. **UI — rinomina campi 📅 + avvisi in linguaggio umano (motore byte-identico)** — quattro modifiche di sola UI, chiavi/logica motore invariate. (1) Nel pannellino 📅 il tetto settimanale (prima senza label) ha ora una label **"Al massimo"** accanto al campo (chiave `SETT:` invariata); (2) il minimo settimanale (voce 57) è etichettato **"Vorrei assolutamente"** (chiave `SETTWK:` invariata); (3) rimossa l'intestazione esplicativa in cima al pannello 📅 ("Tetto turni per singola settimana ISO…"). (4) **Avvisi del banner tab 4**: titolo → *"⚠️ Da verificare prima di esportare"*; poiché la stringa SCOPERTO nasce nel MOTORE (`elaboraTurno`, "con N medici presenti, restano SCOPERTE (nessuna disponibilità verde o blu dichiarata): …"), il ripasso è un **transform di sola visualizzazione** su `dati.avvisi` (const `avvisiUI`, il motore e l'array passato all'AI restano identici): sostituisce "nessuna disponibilità verde o blu dichiarata" → "nessun medico disponibile come sede fisica o copertura a distanza", "con 1 medici presenti" → "con 1 solo medico presente", e **filtra** gli avvisi SCOPERTO con 1 solo medico presente (ovvi/rumorosi: l'unico motivo è la mancanza di medici; l'avviso SCOPERTO resta solo con 2+ medici). **Verifiche**: esbuild OK; `engine_test.mjs` **byte-identico** (nessuna riga motore toccata); Playwright smoke (📅 mostra "Al massimo"/"Voglio", 0 errori); transform verificato su stringhe campione; `docs/app.jsx` rigenerato.

61. **Distribuzione §3.11 — pin del TURNO PREFERITO (§3.9) come punto fisso** — quando un medico vince sia il diurno (G) sia il notturno (N) dello STESSO giorno e ha dichiarato una preferenza (`TURNOPREF`), il turno **preferito** diventa un pin in `kept` (via il set `prefForzati`, unito a `obblVinti` insieme a obbligatori/finestre): la distribuzione §3.11 gli costruisce attorno invece di cederlo, così nel PASSAGGIO 2 il medico lavora davvero il turno che voleva. **Fix "A"** (scelto dal coordinatore): si pinna **solo** il turno preferito; il non preferito resta candidato ordinario (tipicamente ceduto a un backup, che lo copre in P2). Si attiva SOLO quando vince ENTRAMBI i turni del giorno (`pool.some(altroKey)`): con un solo turno vinto, `prefForzati` è vuoto → comportamento identico a prima. Nota di meccanismo: `giorniFissi` sono **ancore da cui allontanarsi** (spaziatura), NON slot tenuti — aggiungere solo il giorno lì avrebbe **ceduto** entrambi i turni; il pin corretto passa da `kept`, come obbligatori/finestre. **Verifiche**: nuovo `test_pref_pin_distribuzione.mjs` **4/4** con **controprova diretta** (stesso scenario: senza preferenza §3.11 cede il 14 diurno; con preferenza lo mantiene, tetto rispettato) + no-op se vince un solo turno + determinismo — la sim 100k da sola NON basta (cedere il preferito non viola invarianti). **Intera suite unit verde**; **sim 100k: 0 violazioni**. `engine_test.mjs`/`docs/app.jsx` rigenerati; esbuild OK.

62. **RIMOZIONE completa della ☆ "sede preferita"** — eliminato ovunque il campo `preferito` dell'entry dispo e tutto ciò che lo riguardava (motore + UI + prompt AI + test). ⚠️ **Impatto sul motore verificato e accettato dal coordinatore**: `preferito` NON entrava nella gerarchia (assente da `candidatiOrdinati`/`elaboraTurno`/`provaFisica`/`risolviBlu`), MA alimentava `slotHaPreferiti` → l'ordine `conPref`/`resto` (i turni con un preferito elaborati per primi): rimuoverlo rende l'elaborazione **puramente cronologica** (§3.4) → **gli schemi cambiano** negli scenari con preferiti + debito conteso (NON byte-identico). Rimossi: `slotHaPreferiti`, `conPref/resto` (→ `ordineVoci = voci`), il campo in `costruisciEntryDispo`/`normDispo`/entry-literal, il blocco avvisi "★ preferita non ottenuta", `preferito` da `statoRealeMedico`; in UI il toggle ☆/★ nel popup cella + `setPreferitoSede` + display ★; nel prompt AI il campo `"preferito"`, il `PREF:`, i trigger ☆. **NON toccata** la preferenza di TURNO (`TURNOPREF`, §3.9/voce 61), cosa diversa. **Test**: eliminato `test_preferiti2.mjs` (13 test, era interamente la feature); ripuliti gli altri (`turnoDisp` senza `preferito`; `test_stato_azzera`/`dispo_set_equivalenza`/`rapido2`/`preferenza_turno` senza il campo; generatori sim senza generazione preferito). Anche **task 2**: legenda tab 1 rinominata 📌 "Ci tengo a questa data" · ⚓ "Ci tengo a questa data solo se sono in questa sede". **Verifiche**: 0 residui `preferito`/★/☆ (solo la preferenza di TURNO resta); esbuild OK; ESLint nessun nuovo warning; **intera suite unit verde**; **sim 100k: 0 violazioni** (schemi cambiati ma invarianti intatti); Playwright smoke OK; `engine_test.mjs`/`docs/app.jsx` rigenerati.

63. **Prompt AI — nota su come combinare pin 📌/⚓ e turno_pref** — solo prompt (motore byte-identico). Aggiunta nella sezione azioni, tra `slot_obbligatorio` e `finestra_settimanale`, una nota che chiarisce quando il medico "ci tiene a un GIORNO", con 4 casi + frasi trigger: **solo pin 📌** (indifferente diurno/notturno → pin su ENTRAMBI G e N, nessun turno_pref); **pin 📌 + turno_pref** (ci tiene al giorno E preferisce un turno → pin su G e N + turno_pref; se vince entrambi §3.9 cede il non preferito); **pin ⚓** (ci tiene MA solo se ottiene una sede → slot_obbligatorio con `sede`; ignorato se non ottiene quella sede); **pin ⚓ + turno_pref** (giorno + sede specifica + turno preferito). **Verifiche**: esbuild OK; `engine_test.mjs` byte-identico; `docs/app.jsx` rigenerato.

64. **Prompt AI — pin libero 📌 vs pin sede ⚓: ⚓ solo per restrizione a un sottoinsieme PROPRIO delle sedi dichiarate** — solo prompt (motore byte-identico). Regola in `slot_obbligatorio`, versione definitiva dopo la restrizione della prima stesura (che era troppo larga: ⚓ su TUTTE le sedi dichiarate è funzionalmente identico a 📌, perché il pin libero non può comunque scattare su una sede non dichiarata → ridondante). Semantica corretta: **⚓ (`slot_obbligatorio` con `sede`) SOLO quando il medico restringe il PIN a un sottoinsieme PROPRIO delle sedi che ha dichiarato disponibili quel giorno** (tra le sedi su cui è disponibile ne indica alcune, non tutte, come condizione per tenere il turno). Esempio ⚓: disponibile a Spilimbergo e Meduno, "il 19 lo tengo solo se sono a Spilimbergo" → ⚓ Spilimbergo. In TUTTI gli altri casi → pin libero 📌 (senza `sede`), anche se il medico ha dichiarato meno di 5 sedi: escludere una sede dalla DISPONIBILITÀ è già sufficiente. Esempio 📌: disponibile a Spilimbergo/Meduno/Claut/Anduins (non Maniago), "il 23 non lo cedo" senza vincolare la sede del pin → UN pin libero 📌 su 23N (non quattro ⚓). **Verifiche**: esbuild OK; `engine_test.mjs` byte-identico; `docs/app.jsx` rigenerato.

65. **Prompt AI — esclusione di sede ("tutte le sedi TRANNE X"): omissione dall'elenco, mai `dispo_no`** — solo prompt (motore byte-identico). Bug reale osservato: a fronte di "disponibile ovunque tranne Maniago" l'AI (1) dichiarava tutte e 5 le sedi ignorando l'esclusione (agganciava la regola "TUTTE LE SEDI PARI") e (2), messa davanti all'errore, proponeva un `dispo_no` su Maniago (che però è per giorno+turno, non ha campo `sede`). Il prompt non copriva il caso: l'unica regola "tranne/eccetto/salvo" era esclusivamente sui GIORNI (→ `dispo_no`). Aggiunto nella sezione SEDI, subito dopo "TUTTE LE SEDI PARI", il blocco **ESCLUSIONE DI SEDE**: quando il medico esclude una o più sedi ("tutte tranne Maniago", "ovunque meno che a Spilimbergo", "non a Meduno", "a parte Claut", "eccetto Anduins") si dichiarano nel campo `sedi` SOLO le sedi rimanenti (5 − escluse), a livelli pari; le escluse si OMETTONO; MAI `dispo_no` per rimediare. Distinzione esplicita: se dopo "tranne" c'è una SEDE → omissione dall'elenco; se ci sono GIORNI/date → resta `dispo_no` (regola preesistente). Aggiunta anche **MAIL 41** al file `mail_test_prompt_ai.md` (FOSCHIANI "tutte le sedi tranne Maniago, notti" → 4 sedi, no dispo_no, no domanda diurno). **Verifiche**: esbuild OK; `engine_test.mjs` byte-identico; `docs/app.jsx` rigenerato.

66. **UI — selettore modello dell'assistente AI (Sonnet / Opus) persistito** — solo UI + chiamata API (motore byte-identico). Il model string della chiamata `/v1/messages` (prima hardcodato `claude-sonnet-5`) diventa lo stato React `aiModel` ("sonnet"/"opus"), mappato al model string vero da `MODELLI_AI` (`sonnet`→`claude-sonnet-5`, `opus`→`claude-opus-4-8`) al momento della fetch. Selettore a **segmented control** nel pannello chat, in una striscia dedicata sotto l'header ("Modello AI" + due pill con etichetta umana e descrizione: **Sonnet** "veloce, modello attuale", **Opus** "più accurato, più lento"), stile coerente col tema (`T.primary`, `T.surfaceAlt`, ombra tenue sul pill attivo). Default **Sonnet**; disabilitato durante `aiBusy`. Persistenza in **localStorage** (`gmn_ai_model`, unica preferenza persistita dell'app finora): scelta Opus + reload → resta Opus. La chiamata API **non** passa `temperature`/`top_p`/`top_k` (verificato: non erano presenti, 400 su Opus 4.8) né `effort` (default già `high` su Opus 4.8). **Verifiche**: esbuild OK; `engine_test.mjs` byte-identico; `docs/app.jsx` rigenerato; Playwright smoke (render selettore, default sonnet, clic Opus → persistenza dopo reload, ritorno a Sonnet, zero errori console) + smoke generale OK.

67. **Prompt AI — 7 bug emersi dal test A/B (Sonnet vs Opus): stessi errori su entrambi = buchi del prompt, non del modello** — solo prompt (motore byte-identico). Metodo: ogni fix è una REGOLA GENERALE (qualunque medico/mese/data, trigger come esempi aperti con sinonimi, non liste chiuse), non patch nominali. (1) **Date inesistenti / mese sbagliato**: promosso il bullet in `CONTRADDIZIONI` a regola forte — MAI correggere/rimappare d'ufficio ("31 settembre"→"31 agosto" in silenzio è errore grave), 🔴 dedicato + nessuna azione per quel giorno; il guard motore (giorno fuori-mese) non copre un numero che per caso esiste nel mese. (2) **Solo pin 📌/⚓ o niente, nessun "preferenziale"**: ristretta la NB di `slot_obbligatorio` a preferenza "FORTE, SPECIFICA e VINCOLANTE"; vietate categorie intermedie inventate ("slot preferito/preferenziale/soft-pin") che il motore non conosce (turno_pref §3.9 è altra cosa); ripuliti 2 commenti di codice stale della ☆ rimossa (voce 62). (3) **Preferenza morbida ≠ pin**: nuovo blocco `PREFERENZE MORBIDE NON VINCOLANTI` in CASI DA SEGNALARE — "se possibile/mi piacerebbe/non è indispensabile/vedete voi" → nessuna azione, MAI domanda Sì/No (il Sì pianterebbe un vincolo rigido rifiutato dal medico), al più nota testuale. (4) **Esclusione ricorrente di giorni-settimana**: `escludi` accetta solo numeri-giorno (motore, `.map(Number)`) → esclusione ricorrente ("il martedì e il giovedì") va espressa col COMPLEMENTO `giorni_settimana` = base ∖ esclusi (calcolato sull'ambito di base reale, non sempre 7 giorni: "notturni feriali tranne il martedì" → lun/mer/gio/ven). (5) **turno_pref su giorno senza diurno**: controllo A MONTE del `calendario` — se "solo notturno" (feriale) NON emettere turno_pref (inerte); condizione impossibile ("se ci fosse il diurno…" su un feriale) → 🔴, nessuna azione. (6) **SENZA INCARICO ambiguo** (decisione utente): insieme massimale/ricorrente/intervallo/giorni-settimana = insieme DETERMINATO → NON blocca, inserisce normalmente MA con 🔴 (senza incarico senza numero mensile); blocco totale SOLO per il vero vago ("qualche notturno", "quando serve"); se c'è un numero → `tetto_mese` con quel numero (invariato); MAI domanda Sì/No. (7) **`tetto_mese` a vuoto**: vietato emetterlo con `maxTurni` null/assente/0 come azione "nuova" (null è solo per RIMUOVERE un tetto su richiesta esplicita) — eliminava l'azione-fantasma "Max turni mese → nessun limite". **Verifiche**: esbuild OK; `engine_test.mjs` byte-identico; `docs/app.jsx` rigenerato (7 nuove stringhe presenti); Playwright smoke generale OK (zero errori console/pagina).

68. **Prompt/stato — regressione pin-invertito su Sonnet + enumerazione settimane (Bug 9) + domande non binarie** — solo prompt + un campo di STATO nel COMPONENTE (`engine_test.mjs` byte-identico, hash invariato). Tre problemi dal 2° giro A/B. **P1 — pin "TIENI-lo" letto come `dispo_no` (grave, su Sonnet)**: causa = collisione pre-esistente tra i trigger pin negativi ("non me lo toccate/togliete") e i trigger `dispo_no` di superficie simili ("togliete il X"), aggravata dal maggior volume di testo di divieto introdotto in voce 67 (interferenza diffusa sul modello più debole; BERTUZZI è INDET, non c'entra il blocco senza-incarico). Fix: **DISCRIMINATORE OBBLIGATORIO "TIENI-lo vs TOGLI-me"** in testa a INDISPONIBILITÀ — criterio SEMANTICO (oggetto del togliere = il TURNO che il medico vuole conservare → PIN 📌; = il MEDICO che vuole essere escluso → `dispo_no`), trigger come esempi aperti, precedenza su ogni regola di blocco, cross-reference bidirezionale dai trigger pin. **P1b — "quasi/praticamente sempre"**: distinta la *disponibilità ampia generale* ("ci sono praticamente sempre" ≈ tutto il mese → NON blocca) dalla *regolarità con eccezioni implicite* ("faccio quasi sempre i notturni" → resta blocco `ECCEZIONI NON SPECIFICATE`). **P2 — Bug 9, enumerazione settimane** (Sonnet saltava la settimana a cavallo, Opus faceva doppioni): il campo `calendario` non conteneva le settimane → aggiunto un campo **`settimane`** nello stato (componente, riusa `settimanaDi()` del motore), array di `{lunedi, giornoRappresentativo}` con TUTTE le settimane lun-dom che toccano il mese (inclusa quella a cavallo, il cui rappresentativo è SEMPRE un giorno in-mese = 1) + regola GENERALE "leggi `settimane` ogni volta che ragioni sulle settimane (tetto_settimana, finestra_settimanale, 'la settimana del X', conteggio), non calcolarle a mente"; corretto l'esempio stale "× 5 settimane" in CALCOLO AUTONOMO (il numero si legge dalla lunghezza di `settimane`). **P3 — domande Sì/No malformate** (VALERI, Opus): vietata la formulazione aut-aut ("vuoi X o Y?") e le domande "opzionali" che offrono un'azione non richiesta; la "domanda" dev'essere letteralmente rispondibile con Sì (→seSi)/No (→seNo). **Verifiche**: `engine_test.mjs` byte-identico (hash `c155ffce…` invariato); esbuild OK; `docs/app.jsx` rigenerato; algoritmo `settimane` validato su più mesi (ago/feb/nov 2026: 6/5/6 settimane, rappresentativi sempre in-mese); Playwright smoke generale OK.

69. **Prompt AI — sfoltimento ridondanze (round 1, solo tagli a rischio basso)** — solo prompt (motore byte-identico, hash `c155ffce…` invariato). Primo giro di riduzione del "grasso" del prompt (~18.250 parole), **zero contenuto rimosso**, solo ridondanza/rumore. (C) Rimosso il paragrafo `⚠️ RIPETUTO PERCHÉ CRITICO` (doppione dichiarato della regola feriale già in `🔒 CONTROLLO OBBLIGATORIO` di WEEKEND AMBIGUO) + tolto il prefisso decorativo "IMPORTANTISSIMO —" da MESE DI RIFERIMENTO FISSO. (A2) La frase "rispondi SOLO JSON / non esiste canale nascosto" era scritta per intero due volte (coda del paragrafo calendario + `STILE DI RISPOSTA`): tenuta solo quella di STILE DI RISPOSTA. (A1) Le due sezioni autonome "leggi il calendario, non calcolare a mente" (`CALENDARIO DEL MESE` + `REGOLA GENERALE: CALENDARIO`) consolidate: la seconda ridotta a cross-ref, preservato il suo esempio-negativo (genericizzato, tolta la data stale "14 agosto"). (A3) Il blocco senza-incarico near-verbatim per `ore_extra` e `turni_extra` fuso: `turni_extra` ora rimanda alla regola di `ore_extra`, mantenendo direttiva locale e stringa avviso 🔴 esatta. (B6) I due esempi JSON MMG (mattina/pomeriggio) erano identici al 90%: tenuto quello della mattina, il pomeriggio ridotto a una riga ("identico, `fascia`/`turno` = P"). (B3) `TUTTE LE SEDI PARI`: i ~18 sinonimi di indifferenza-sede compressi in criterio + esempi rappresentativi con **lista aperta** ("vale qualunque frase con lo stesso senso"). **Totale ~330 parole in meno**, nessuna regola indebolita. **NON toccati** (rimandati al corpus 41 mail): A4/A5/A6/A7 (duplicati load-bearing), B1/B2/B4/B5 (liste-trigger che ancorano mapping). Nota: il `⚠️ RIPETUTO` rientrava sia in C (da togliere) sia in A6 (da non toccare) — prevalso C perché doppione dichiarato al 100%. **Verifiche**: `engine_test.mjs` byte-identico; esbuild OK; `docs/app.jsx` rigenerato; Playwright smoke OK.

70. **Prompt AI — Bug 10 (pin senza disponibilità), Bug 11 (contraddizione aritmetica), cosmetico spiegazione** — solo prompt + 6 mail di test (motore byte-identico, hash `c155ffce…` invariato). **Bug 10** (pin emesso su uno slot senza disponibilità dichiarata): nella NOTA COMBINARE il pin andava "su ENTRAMBI i turni del giorno (G+N)" gating sul fatto che il *giorno* avesse entrambi i turni, non sulla disponibilità *dichiarata* → un medico "solo notturni" prendeva il pin anche sul diurno di un superfestivo (slot senza dispo: inerte, sporca la griglia). Nuova regola in testa ai casi COMBINARE: **il pin (📌/⚓) va SOLO sui turni per cui il medico ha dichiarato disponibilità** (intersezione {turni che esistono quel giorno} ∩ {turni dichiarati}); pin su G+N solo se ha dichiarato entrambi (contro-esempio salvo: "notti + diurni weekend" + "giorno o notte è uguale"). **Bug 11** (contraddizione aritmetica non rilevata): la sezione CONTRADDIZIONI copriva solo quelle testuali → aggiunta regola **CONTRADDIZIONE ARITMETICA**: prima di emettere un tetto + dei minimi, sommare — (a) somma dei minimi `finestra_settimanale` ≤ `tetto_mese`; (b) `finestra_settimanale` (min) ≤ `tetto_settimana` (max) stessa settimana; se sfora → 🔴, **nessuna azione** (come le contraddizioni testuali). **Cosmetico**: nuova regola "la `spiegazione` rispecchia le AZIONI emesse, non l'intenzione del medico" (se per un punto c'è solo un 🔴 e nessun `dispo_no`, il riassunto non deve dire "NO il X"). **Test**: aggiunte MAIL 42-47 a `mail_test_prompt_ai.md` (42=Bug 10, 45=Bug 11 — falliscono prima del fix; 43/44/46/47 = comportamento esistente). **Verifiche**: `engine_test.mjs` byte-identico; esbuild OK; `docs/app.jsx` rigenerato; Playwright smoke OK. Aggiunto `CLAUDE.md` (+ nota in testa a questo file) con la regola permanente su file di test / cartella `uploads`.

### PENDING (sessione dedicata al MOTORE, non fatto ora)
- **Guard motore per `slot_obbligatorio` su slot senza disponibilità** (collegato al Bug 10): l'apply del motore (riga ~3316) accetta un `OBBL:` anche su un turno per cui il medico non ha dichiarato disponibilità — resta salvato ma inerte (non produce assegnazioni sbagliate, sporca solo la griglia). Il fix prompt (voce 70) è la difesa primaria e per ora basta. ⚠️ Il commento a riga 3316 dichiara l'ortogonalità pin/disponibilità come **INTENZIONALE**: prima di aggiungere un guard va capito **se e perché** quella scelta era voluta (non trattarlo come una svista). Da valutare in una sessione dedicata al motore, col giro completo: sim 100k, unit test, diff.

71. **Prompt AI — regressione da interferenza (3° episodio): `turni_extra` inventato + parse coarse su mail densa** — solo prompt (motore byte-identico, hash `c155ffce…` invariato). Testando la MAIL 4 (VALERI) su Sonnet, tre regressioni: (1) Bug 4 regredito (esclusione ricorrente mar/gio → solo i primi due invece del complemento `giorni_settimana`); (2) diurno esteso a tutto il mese invece del solo weekend; (3) **`turni_extra:8` inventato** (grave, 96h). **Root del #3:** `CALCOLO AUTONOMO` (turni_extra da tetto) leggeva un "massimo 2 a settimana" del MEDICO come carico implicito (2 × settimane > monte ore → extra) — un'inversione: un tetto è un LIMITE, non un obiettivo. Amplificato da due miei commit: voce 68 aveva riscritto l'esempio con i numeri esatti di VALERI (2/sett × 6) **senza il contesto "coordinatore"**; voce 70 (Bug 11) aveva messo il modello in modalità aritmetica sui tetti. **Fix:** aggiunto **PRINCIPIO GENERALE** — "nessuna azione che AUMENTA il carico (turni_extra/ore_extra/estensioni oltre il monte ore) può derivare da un vincolo che lo RIDUCE; «massimo N» è un limite, non un obiettivo; un medico che si auto-limita toglie disponibilità, non dichiara capacità"; e ristretto `CALCOLO AUTONOMO` a **SOLO un'azione deliberata del COORDINATORE** (mai da una mail-medico) — un "massimo N a settimana" scritto dal medico → SOLO `tetto_settimana`, MAI `turni_extra`. Esempio riscritto in chiave coordinatore. **Preserva MAIL 18** (coordinatore "tetto 3/settimana" → turni_extra legittimo). **#1/#2** sono interferenza diffusa (complemento riga 2445 e split weekend riga 2452 INTATTI, mai toccati): il fix #3 toglie la distrazione aritmetica ma la risoluzione di #1/#2 NON è garantita → se restano rotti dopo il re-test, passo dedicato di rinforzo salienza (senza toccarne la sostanza). **Verifiche**: `engine_test.mjs` byte-identico; esbuild OK; `docs/app.jsx` rigenerato; smoke OK.

---

## 11. CONVENZIONI DI CODICE

- Il file è un **single-file React component** — non spezzarlo in più file.
- Nessuna libreria esterna (niente axios, date-fns, lodash, ecc.).
- Il motore (funzioni prima di `// ============ COMPONENTE ============`) è **puro JavaScript** senza hooks React.
- I tipi di TypeScript NON sono usati — il file è `.jsx`. Il compilatore tsc serve solo per rilevare errori sintattici reali (filtro su codici TS1xxx, TS2304, TS2339, TS2552, TS2454).
- Lo storage usa `window.storage` (API specifica di Claude.ai artifacts) — NON usare `localStorage`.
- Le chiamate API ai modelli usano `fetch("https://api.anthropic.com/v1/messages")` senza API key (gestita da Claude.ai).

---

## 12. COME SEGNALARE E CORREGGERE UN BUG

**Per un bug concettuale (comportamento sbagliato):**
1. Scrivi un test Node.js che riproduce il caso atteso e verifica che fallisce
2. Identifica la funzione del motore responsabile (quasi sempre `elaboraTurno`, `provaFisica` o `provaBlu`)
3. Correggi rispettando gli invarianti (§5) e la gerarchia titolarità→categoria→debito→graduatoria (fisica) / categoria→titolarità→debito→graduatoria (distanza)
4. Rilancia tutti i test — zero fallimenti prima di considerare il fix completo

**Template test minimo:**
```javascript
import { MEDICI, byId, CAT_INFO, SEDI5, dk, elaboraSchema } from './engine_test.mjs';
const base = () => { const d={}; MEDICI.forEach(m=>d[m.id]={}); return d; };
const N = (g) => `${dk(2026,7,g)}|N`;
const disp = (v=[], b=[]) => ({ verde:v, verdeLiv:{}, blu:b, bluLiv:{}, no:false, preferito:null });
// ... test case ...
```

**Per modificare il motore:** lavora SOLO sulle funzioni tra riga 114 e 555 (vedi §2). La UI non dovrebbe mai contenere logica di assegnazione.

---

## 13. PROMPTS E SISTEMI ESTERNI

**Assistente AI nell'app** — usa il system prompt in `chiediAI`. Conosce tutte le regole di business (gerarchia, titolarità, debito), il formato JSON per modificare disponibilità (verde/blu) e schema. Il prompt è nel codice e può essere aggiornato.
- Modello `claude-sonnet-5`, `max_tokens: 16000`.
- **Sezione `CALENDARIO PERPETUO — GIORNO DELLA SETTIMANA E FESTIVITÀ`** (subito dopo `REGOLA TEMPORALE FONDAMENTALE`): istruisce esplicitamente l'AI a NON affidarsi alla memoria per calcolare che giorno della settimana cade una data — usa la congruenza di Zeller passo per passo (formula completa nel prompt, risultato 0=sabato...6=venerdì), l'elenco delle festività fisse italiane (incluso il 31 dicembre, festivo a sé per ASFO — non nazionale, ma allineato a `festiviFissiDi`, §3.6, per cui il prompt precisa esplicitamente che il 30, non il 31, è il suo prefestivo), e l'algoritmo di Gauss per Pasqua/Pasquetta (stessa formula usata in `pasquaDi`, §3.6, così prompt e motore calcolano lo stesso identico calendario). Valido per qualsiasi anno, nessun limite temporale. Aggiunta dopo un bug segnalato: l'AI aveva generato la domanda "vuoi aggiungere anche il diurno?" per un giorno feriale (deducendo male il giorno della settimana a memoria) — vedi anche il rinforzo "WEEKEND AMBIGUO" più sotto in questa stessa lista. Il 31 dicembre è stato aggiunto in un secondo momento, dopo aver verificato che il motore lo trattava già come festivo ma il prompt no (disallineamento tra i due, corretto qui).
- **Regola sull'ambito di "seSi"/"seNo" (bug "domande multiple")**: le azioni di una domanda Sì/No devono riguardare ESCLUSIVAMENTE il giorno e il medico di quella specifica domanda — rispondere Sì o No non deve mai inserire disponibilità per altri giorni menzionati altrove nella stessa email originale, anche se sembrano casi analoghi. Ogni domanda è un'unità isolata. Aggiunta dopo un bug segnalato in cui la risposta a una domanda aveva effetto anche su giorni non esplicitamente coperti da quella domanda.
- **Italiano semplice, senza gergo tecnico**: il prompt istruisce l'AI a scrivere sempre in "spiegazione"/"testo" come parlerebbe un collega, mai termini interni ("round", "azioni", "az", "slot", "array", "state", "JSON"). Esempio guida nel prompt: invece di "Fatte 8 di 10 azioni" → "Ho inserito 8 disponibilità su 10, continua per le restanti" (la parola concreta si adatta al contenuto reale: disponibilità, turni, modifiche, preferenze...). L'esempio "Fatte X di Y" nella sezione limiti round è stato aggiornato di conseguenza.
- **Sezione `INTERPRETAZIONE EMAIL DISPONIBILITÀ`** nel prompt (sostituita per intero con una versione molto più estesa fornita dall'utente): raccolta esaustiva, per categoria, delle frasi italiane più comuni usate dai medici nelle email di disponibilità, con l'azione da produrre per ciascuna. Categorie coperte: sedi fisiche prima scelta/verde livello 1 (arrivo diretto, preferenza esplicita, disponibilità diretta, esclusività, motivazioni contestuali); sedi fisiche seconda/terza scelta/verde livello 2-3 — SEMPRE verdi, mai blu (alternative esplicite, accettazione di sede alternativa, necessità/emergenza, indifferenza tra sedi, gradazione implicita); copertura a distanza/blu (solo frasi che esplicitano chiaramente l'assenza fisica); indisponibilità/NO esplicito (ferie/assenze programmate, giorni singoli diretti, caso speciale mattina/sera → NO solo su G o solo su N); disponibilità generica su tutte le sedi pari; turni diurno/notturno (entrambi, solo notturno, solo diurno, e il caso "weekend ambiguo" — medico non specifica G o N → inserisce SOLO il notturno e aggiunge sempre un avviso "🔴 ATTENZIONE: [nome] giorno [X] non ha specificato diurno o notturno — inserito solo notturno. Verificare con il medico se intendeva anche il diurno."; questo caso e il relativo avviso valgono SOLO per weekend/festivi/prefestivi, che hanno sia G che N — il prompt precisa esplicitamente che NON si applicano mai ai feriali semplici, che hanno solo il notturno: lì non esiste alcuna ambiguità da segnalare perché il diurno non esiste in quel giorno); MMG/PLS (turni separati dagli ordinari, sempre dichiarati esplicitamente dal medico — REGOLA GENERALE che precede ogni sotto-caso: prima di inserire QUALSIASI turno MMG con `dispo_aggiungi`, anche quando la frase sembra mappare direttamente a un turno ("MMG mattina" → M), va sempre controllato `mmgAttivi` nello STATO ATTUALE per verificare che quel turno sia attivo proprio quel giorno; se non lo è, mai un inserimento silenzioso, sempre una "domanda" Sì/No: "MMG mattina"/"mattutino MMG"/"MMG 8-14"/"PLS mattina" → turno M; "MMG pomeriggio"/"MMG 14-20"/"PLS pomeriggio" → turno P; "faccio il diurno MMG" senza specificare mattina/pomeriggio → sia M che P se entrambi attivi quel giorno, altrimenti solo quello attivo; "copro il [giorno]" SENZA menzionare MMG/PLS → notturno ordinario, non un MMG; "mattina/pomeriggio/diurno del X" senza dire MMG o PLS → controlla `mmgAttivi` nello STATO ATTUALE (elenca i giorni con turni MMG/PLS attivi, es. "g15:M", "g15:P", "g15:MP"): se il giorno ha un MMG attivo corrispondente lo inserisce, altrimenti ignora la frase perché nei feriali il diurno ordinario non esiste); recupero ore (→ azione `ore_extra`; se espresso in TURNI invece che in ore — es. "ho 4 turni da recuperare" — conversione automatica senza chiedere conferma: ore = turni × 12); casi da segnalare al coordinatore SENZA inserire nulla (sede non identificabile, date vaghe, condizionali/incerti, contraddizioni, informazioni insufficienti) con avviso "🔴 ATTENZIONE: [descrizione] per [medico] — non ho inserito nulla per questo punto. Verificare con il medico prima di procedere.". Il prompt istruisce l'AI ad applicare il principio più vicino per analogia per le frasi non elencate. Gli avvisi "🔴 ATTENZIONE" (sia turni ambigui che casi da segnalare) sono l'UNICA eccezione al limite di ~20 parole della "spiegazione" (vedi sopra): vanno scritti per intero anche se la allungano.
- **WEEKEND AMBIGUO — rinforzo anti-feriale (bug segnalato dall'utente)**: l'AI aveva generato la domanda "vuoi aggiungere anche il diurno?" per un medico su un giorno feriale semplice (giovedì, solo notturno) — la regola di esclusione feriali esisteva già in fondo alla sezione ma non veniva sempre rispettata. Rinforzata spostando il controllo (giorno feriale → sezione non applicabile) in cima, PRIMA degli esempi, con un caso concreto lavorato (giovedì 7 agosto) invece che solo una regola astratta; il paragrafo "🔴 ATTENZIONE ALLA DIFFERENZA" ora specifica esplicitamente "per i soli weekend/festivi/prefestivi"; l'avviso finale è ripetuto con l'introduzione "RIPETUTO PERCHÉ CRITICO" e un invito a ricontrollare il giorno della settimana in caso di dubbio prima di generare la domanda. Nessuna modifica di logica — è un rinforzo di prompt engineering (ripetizione + esempio concreto), dato che l'informazione sul giorno della settimana non è nello STATO ATTUALE ma dedotta dall'AI dalla conoscenza generale del calendario. Copertura di test: nuova categoria `feriale_no_domanda_diurno` (30 casi) in `test_email_generator.mjs`, stessa identica frase ambigua della categoria `weekend_ambiguo` ma su un giorno feriale — verifica che NON venga mai generata alcuna domanda né inserito il diurno.
- **MMG/PLS — "sedi" non può mai essere vuoto (fix bug trovato con `test_email_report.mjs`)**: un test su 1200+ email simulate ha rivelato che l'AI produceva spesso `dispo_aggiungi` con `"sedi":[]` per i turni MMG (M/P) quando l'email non indicava alcuna sede — un'azione sintatticamente valida ma verificata (contro il motore reale) come funzionalmente INERTE: senza almeno una sede in `verde`, `candidatiOrdinati` esclude il medico dai candidati e il turno resta scoperto, nonostante il coordinatore veda "disponibilità inserita" in chat. Aggiunta una seconda regola generale nella sezione MMG E PLS, subito dopo quella su `mmgAttivi`: se la sede è indicata nell'email o comunque inequivocabile dal contesto, usala; altrimenti NON inserire alcuna azione e genera l'avviso "🔴 ATTENZIONE: sede MMG non specificata per [nome] giorno [X] — verificare con il medico." Copertura di test: categoria `mmg_attivo` in `test_email_generator.mjs` aggiornata per includere sempre una sede nell'email e verificare `sedi` non vuoto nell'atteso; nuova categoria `mmg_sede_non_specificata` (20 casi) che verifica esplicitamente il nuovo ramo di ambiguità.
- Sezione `STILE DI RISPOSTA E LIMITI` nel prompt: NESSUN limite fisso al numero di azioni per risposta (storico: 3-4 → 3 → 8 → 15 → rimosso), "spiegazione" limitata a una frase breve, vietato citare/ripetere per esteso il testo incollato dall'utente. La regola ora è di completezza per medico: l'AI inserisce SEMPRE tutte le disponibilità di un medico nello stesso round, non le spezza mai a metà tra due round. Solo se la richiesta è così ampia da rischiare di superare il budget di token, l'AI dà priorità a completare interi medici per round (mai un medico a metà), indica nella "spiegazione" solo il conteggio in italiano semplice, e imposta il campo strutturato opzionale `"altreAzioniRestanti":true` nella risposta JSON di tipo "modifiche" (default false/omesso). Il fallback dopo una risposta troncata (vedi sotto) resta più conservativo, a 2 azioni, per non rischiare un nuovo troncamento.
- **Sezione `REGOLA GENERALE: MEMORIA TRA ROUND`** nel prompt: le informazioni dichiarate da un medico nell'email/messaggio originale valgono per TUTTI i round della conversazione, non solo il primo — un'indisponibilità (ferie, NO esplicito, impegno) resta valida finché non viene esplicitamente ritirata dal medico, e va ricontrollata PRIMA di ogni inserimento di disponibilità in qualsiasi round successivo, anche molti round dopo. "Dal X al Y" (ferie/assenze) include sempre X, Y e tutti i giorni intermedi. I NO espliciti (`dispo_no`) vengono già tracciati in `azioniGiaEseguite` esattamente come le disponibilità positive — nessuna modifica di codice necessaria: `dispo_no` ha già `medico`+`giorno`+`turno` come qualunque altra azione, quindi rientra automaticamente nello stesso filtro di `applicaProposta` che popola sia il riepilogo compatto in chat sia il registro (§13).
- **Pulsante "Continua →"**: quando l'ultima risposta ha `altreAzioniRestanti:true`, appare automaticamente un pulsante nella chat (visibile solo a proposta risolta, cioè dopo Conferma o Annulla — mai insieme al riquadro di conferma azioni, per evitare di passare al round successivo prima che quello corrente sia stato applicato). Un click invia `"continua"` come messaggio successivo senza bisogno di digitarlo. Stato `azioniRestanti` (booleano), azzerato a ogni nuovo invio e ricalcolato dalla risposta che arriva.
- **Anti-loop multi-round**: il prompt istruisce esplicitamente l'AI a controllare la cronologia della conversazione prima di proporre azioni — una sua "PROPOSTA: ..." seguita da qualunque messaggio diverso da "Proposta annullata, nessuna modifica applicata" (quindi anche "Modifiche applicate ✓" o "Applicata con avvisi: ...") indica azioni già applicate con successo, da non riproporre mai più nei round successivi (nemmeno riformulate). Solo un annullamento esplicito rende quelle azioni ripetibili. Corregge un bug osservato in produzione in cui l'AI, su "continua", riproponeva le stesse modifiche già confermate invece di procedere con quelle nuove.
- **`stato.disponibilitaPresenti`**: checklist essenziale (per OGNI medico, anche con array vuoto) di quali slot giorno+turno hanno già una disponibilità inserita (di qualsiasi tipo, incluso NO) — solo "g15G"/"g16N" ecc., senza dettagli di contenuto (quelli sono già in `stato.disponibilita`). Aggiunta perché la sola cronologia della chat non bastava a evitare il loop: l'AI ora ha una fonte di verità direttamente confrontabile con una richiesta o email incollata (aggiornata ad ogni round in base a quanto realmente salvato in `dati.dispo`, non a quanto l'AI *pensa* di aver proposto) per determinare con certezza cosa manca ancora.
- **Riepilogo compatto in "Modifiche applicate ✓"**: `applicaProposta` ora costruisce, dalle azioni appena confermate, un promemoria di una riga tipo "Modifiche applicate ✓ — BERTUZZI: g1G g1N g2G · WANG: g5N (annullabile con ↶)." — raggruppa per medico i token "g{giorno}{turno}" delle azioni che hanno sia `medico` che `giorno`+`turno` (dispo_aggiungi/dispo_no/dispo_togli/turno_pref/schema con medico non null); azioni senza questa tripla (mmg, ore_extra, tetto_settimana, tetto_mese, elabora) non compaiono nel riepilogo. Solo nel caso di successo pieno (non nel ramo "Applicata con avvisi: ...").
- **`azioniEseguite` (registro anti-loop di sessione)**: state React puro (array di stringhe "MEDICO g{giorno}{turno}"), MAI persistito su storage — esiste solo durante la conversazione AI corrente. Popolato dopo ogni conferma con successo (stesse azioni del riepilogo compatto sopra, appiattite in singole voci). Inviato all'AI ad ogni round come `stato.azioniGiaEseguite`, con nota nel prompt che lo indica come fonte di verità più affidabile della cronologia della chat per l'anti-loop (§13, aggiornata anche la sezione `STILE DI RISPOSTA E LIMITI`): qualunque tripla medico+giorno+turno lì presente è definitivamente già applicata e non va mai riproposta. Azzerato (insieme a `aiMsgs`, `proposta`, `domande`, `azioniRestanti`, `troncato`, `completato`, `aiInput`) dal pulsante **"Nuova conversazione"** nell'header del pannello AI.
- **Refactor `applicaAzioni`/`riepilogoDi`**: la logica di applicazione delle azioni (il grande `forEach` su tutti gli `az` possibili) e la costruzione del riepilogo compatto/registro sono state estratte da `applicaProposta` in due funzioni condivise — `applicaAzioni(azioni)` (applica un elenco di azioni a `dati` e ritorna `{errori, dispoModificata, daElaborare}`) e `riepilogoDi(azioni)` (ritorna `{riepilogo, nuoveVociRegistro}`) — così da poterle riusare identiche anche per le "domande" Sì/No (vedi sotto), senza duplicare codice.
- **Domande Sì/No dell'AI**: per le ambiguità con una scelta binaria chiara (entrambe le risposte corrispondono a un'azione concreta e ben definita — es. attivare o no un turno MMG mancante, aggiungere o no il diurno su un weekend non specificato), l'AI usa il nuovo campo opzionale `"domande"` nella risposta JSON di tipo "modifiche" (può coesistere con "azioni", o essere l'unico contenuto della risposta se non ci sono azioni dirette): `{"giorno":11,"medico":"MARZANO","citazione":"vorrei fare la mattina MMG l'11","domanda":"Vuoi attivare questo turno?","seSi":[...azioni...],"seNo":[]}`. `"citazione"` è OBBLIGATORIA: la frase esatta scritta dal medico nel testo incollato (non un riassunto), così il coordinatore vede subito il contesto originale. In `"citazione"`/`"domanda"` il prompt vieta esplicitamente abbreviazioni o codici interni (niente "g11"/"g8N"/"MA"/"SP": sempre "giorno 11"/"notturno"/"Maniago"/"Spilimbergo" per esteso). Per il caso specifico "MMG richiesto ma non attivo" la domanda è SEMPRE la formulazione standard "Vuoi attivare questo turno?" — mai un avviso testuale, senza eccezioni — con `seSi` che attiva il turno MMG e inserisce la disponibilità, `seNo` sempre vuoto (nessuna azione, non si inserisce nemmeno l'altro turno). La UI (`domande.map` nel pannello AI) renderizza ogni card come `❓ {medico} {giorno} {mese in minuscolo} ({giorno della settimana}): ha scritto "{citazione}" — {domanda}` (giorno della settimana calcolato al volo con `new Date(anno, mese, d.giorno).getDay()`, non richiede stato aggiuntivo). Per le ambiguità SENZA un'azione concreta definibile (sede non identificabile, date vaghe, condizionali, contraddizioni) l'AI continua a usare il testo "🔴 ATTENZIONE" nella spiegazione (invariato).
  - **Precedenza esplicita "notti"/"notturni" sulla domanda weekend-ambiguo**: il prompt chiarisce che la domanda "Aggiungo anche il diurno?" si fa SOLO quando il medico non menziona affatto il turno (né diurno né notturno). Se usa esplicitamente parole come "notti"/"notturni"/"notturno"/"la notte" (sezione SOLO NOTTURNO), quella è già una scelta di turno dichiarata: si inserisce direttamente e silenziosamente solo il notturno, senza generare alcuna domanda.
  - Stato `domande` (array, accumulato — non sovrascritto — a ogni round, per non perdere domande di round precedenti non ancora risposte).
  - UI: ogni domanda appare come una card propria (formato esatto sopra) con due pulsanti **Sì**/**No**, sotto l'eventuale riquadro di conferma della proposta.
  - `rispondiDomanda(idx, risposta)`: applica `seSi` o `seNo` tramite `applicaAzioni`/`riepilogoDi` (stessa logica di `applicaProposta`), aggiunge un messaggio di conferma in chat, rimuove la domanda risposta dall'elenco.
  - Il pulsante "Continua →" e il banner "Completato ✓" ora richiedono anche `domande.length === 0` (oltre a `!proposta`): tutte le domande in sospeso vanno risolte prima di procedere al round successivo o considerare il giro concluso.
- **Turni extra volontari (§3.10) — integrazione AI completa**: `stato.medici` include ora `turniExtra` (valore già dichiarato per il mese, 0 se non impostato — l'AI lo controlla prima di sovrascriverlo con una nuova azione, stessa nota già esistente per `oreExtra`). Nuova azione `{"az":"turni_extra","medico":"...","turni":N}` (12h ciascuno, 0 per azzerare, solo medici con contratto), gestita in `applicaAzioni` con lo stesso schema di `ore_extra`. Nuova sottosezione `TURNI EXTRA VOLONTARI` in `INTERPRETAZIONE EMAIL DISPONIBILITÀ` (contenuto fornito dall'utente): dichiarazione diretta o condizionale con numero esplicito → azione `turni_extra`; rifiuto esplicito → azione `turni_extra` con `turni:0`; dichiarazione generica SENZA numero → nessuna azione, avviso "🔴 ATTENZIONE: [nome] è disponibile per turni extra ma non ha specificato quanti — chiedere conferma prima di inserire."; frasi ambigue (es. "sono a disposizione") → ignorate, interpretate come disponibilità ordinaria.
- **Medico senza incarico: recupero ore e turni extra non si applicano (a livello di prompt, non di motore)**: sia in `RECUPERO ORE` sia in `TURNI EXTRA VOLONTARI` una regola in testa alla sezione impone all'AI di controllare `categoria` in `stato.medici` — se è "Senza inc.", non usare mai `ore_extra` né `turni_extra` per quel medico (qualunque cosa scriva sull'argomento), e segnalare invece un avviso "🔴 ATTENZIONE: [nome] è senza incarico e non ha monte ore contrattuale, ore da recuperare/turni extra non si applicano." Nessuna modifica al motore: `elaboraSchema` tratta già `debiti`/`debitiExtra` come `null` per i senza incarico indipendentemente da `extraOre`/`turniExtra` residui (nessun errore mai sollevato in quel punto); questa è solo una guardia lato AI per evitare che vengano proposte azioni prive di senso su questi campi.
- **Medico senza incarico: disponibilità ordinarie bloccate senza un numero di guardie mensili dichiarato**: nuova sottosezione in testa a `INTERPRETAZIONE EMAIL DISPONIBILITÀ` (prima di "SEDI FISICHE — PRIMA SCELTA", quindi valutata prima di ogni altra regola della sezione). Se un medico con categoria "Senza inc." dichiara sedi/giorni/turni ma non ha mai indicato, né in questo messaggio né in uno precedente della stessa conversazione, un numero complessivo di guardie desiderate per il mese, l'AI non deve inserire NESSUNA disponibilità per lui in quel round — solo l'avviso "🔴 ATTENZIONE: [nome] è senza incarico e non ha indicato il numero massimo di guardie mensili — contattare il medico e reinserire l'email con il numero specificato." Se il numero è presente (in questo o in un round precedente, coerente con `REGOLA GENERALE: MEMORIA TRA ROUND`), le disponibilità si inseriscono normalmente **E l'AI genera SEMPRE anche un'azione `tetto_mese` con quel numero** (vedi punto successivo) — è quello che rende il numero un vincolo REALE nel motore, non solo un avviso testuale letto dal coordinatore. Riguarda solo le disponibilità ordinarie dei senza incarico — non tocca MMG/PLS, recupero ore o turni extra (già coperti dalle rispettive regole). Copertura di test: due nuove categorie in `test_email_generator.mjs`, `senza_incarico_no_numero_guardie` (30 casi, nessun numero dichiarato → avviso, nessuna azione) e `senza_incarico_con_numero_guardie` (30 casi, numero dichiarato nella stessa email → inserimento normale).
- **Max turni mese (§3.11 punto 1) — integrazione AI completa**: `stato.medici` include ora `maxTurniMese` (valore già dichiarato per il mese, `null` se nessun tetto — l'AI lo controlla prima di sovrascriverlo con una nuova azione, stessa nota già esistente per `oreExtra`/`turniExtra`). Nuova azione `{"az":"tetto_mese","medico":"...","maxTurni":N}` (`null`/assente rimuove il tetto), gestita in `applicaAzioni` con lo stesso schema di `tetto_settimana`, ma valida per l'INTERO mese corrente e per QUALSIASI categoria (anche senza incarico) — a differenza di `ore_extra`/`turni_extra`, non è gated dietro `CAT_INFO[cat].ore !== null`. Collegata alla regola "numero di guardie mensili" (punto precedente): è il meccanismo che chiude il cerchio tra la dichiarazione testuale del senza incarico e un vincolo reale applicato dal motore (`capMensileDi`, blocco rigido in `candidatiOrdinati`).
- **Sezione `TETTO MENSILE (Max turni mese)` in `INTERPRETAZIONE EMAIL DISPONIBILITÀ`** (dopo `TURNI EXTRA VOLONTARI`, stessa struttura a sottosezioni): copre le frasi italiane comuni per dichiarare un tetto mensile, per QUALSIASI categoria (a differenza dei turni extra). Dichiarazione diretta con numero ("voglio fare al massimo X turni", "non più di X guardie al mese", "limitatemi a X turni", "questo mese faccio solo X guardie" e varianti) → `tetto_mese` con quel numero. Condizionale con numero ("se ne avete bisogno faccio fino a X turni") → stessa azione, il condizionale non cambia il trattamento (è comunque un tetto esplicito, non turni extra). Senza incarico ("questo mese faccio X guardie") → esplicitamente la STESSA dichiarazione richiesta dalla regola "NUMERO DI GUARDIE MENSILI": genera SEMPRE `tetto_mese` con quel numero, indipendentemente dalla presenza di altre disponibilità nello stesso messaggio (il caso di test dedicato `tetto_mese_senza_incarico` non include mai altre disponibilità, proprio per isolare questa verifica; anche `senza_incarico_con_numero_guardie`, §8bis, è stato aggiornato per richiedere sia `dispo_aggiungi` sia `tetto_mese`). Rifiuto esplicito ("non voglio limiti", "fate voi") → `tetto_mese` con `maxTurni:null` (rimuove un tetto già impostato — **mai `0`**, che escluderebbe il medico da ogni turno del mese, semantica opposta a `turni_extra:0` che invece azzera correttamente un budget). Generica senza numero ("vorrei lavorare meno questo mese") → nessuna azione, avviso "🔴 ATTENZIONE: [nome] vuole un tetto ai turni mensili ma non ha specificato un numero — chiedere conferma prima di inserire." Copertura di test: 5 nuove categorie in `test_email_generator.mjs` (`tetto_mese_diretto` 15, `tetto_mese_condizionale` 12, `tetto_mese_senza_incarico` 12, `tetto_mese_generico` 12, `tetto_mese_rifiuto` 9 — 60 casi totali). `test_email_runner.mjs` aggiornato di conseguenza: `buildStato` espone ora anche `maxTurniMese` per medico (letto da un nuovo campo opzionale `maxTurniMesePre` nello `statoOverride`, stesso schema di `oreExtraPre`/`turniExtraPre`).
- **Eccezioni "tranne"/"eccetto"/"salvo"/"a parte"/"escluso" in INDISPONIBILITÀ (fix bug segnalato)**: un medico aveva scritto una disponibilità ampia con un'eccezione (es. "disponibile tutto il mese tranne dal 1 al 7") e l'AI aveva invertito la logica, inserendo disponibilità invece di indisponibilità per i giorni dopo "tranne". Nuova sottosezione in `INDISPONIBILITÀ — NO ESPLICITO` (dopo "CASO SPECIALE MATTINA/SERA"): queste parole introducono sempre un'eccezione — tutto ciò che segue (fino a fine frase/proposizione) è INDISPONIBILITÀ (`dispo_no`), anche se la parte prima dichiara una disponibilità ampia; la direzione non va mai invertita, e se generale ed eccezione si sovrappongono sugli stessi giorni l'eccezione vince sempre per quei giorni specifici. Copertura di test: nuova categoria `eccezione_tranne_disponibilita` (30 casi) in `test_email_generator.mjs`, con le 6 formulazioni "tranne"/"eccetto"/"salvo"/"a parte"/"escluso"/"fuori da" a rotazione — verifica che i giorni dopo il connettivo ricevano sempre `dispo_no` e MAI `dispo_aggiungi`.
- **Disponibilità "ultima risorsa" in `CASI DA SEGNALARE AL COORDINATORE`**: nuova categoria per frasi come "solo se non trovate altri" / "come ultima risorsa" / "se proprio serve" / "se non trovate nessun altro" / "in caso di emergenza posso" / "preferirei evitare ma ci sono". A differenza di CONDIZIONALI E INCERTI (dove il medico esprime dubbio sulla propria disponibilità reale, da chiarire), qui il medico dichiara una disponibilità certa ma la subordina esplicitamente all'assenza di alternative: NON va inserita come verde normale per quel giorno specifico, ma le ALTRE disponibilità dello stesso medico senza questa condizione si inseriscono regolarmente — solo il giorno condizionato è segnalato invece che inserito. Avviso dedicato con citazione obbligatoria della frase esatta: "🔴 ATTENZIONE: [nome] è disponibile il [giorno] SOLO come ultima risorsa (\"[citazione]\") — non inserito automaticamente. Dopo aver elaborato lo schema, se quella sede resta scoperta, valuta se aggiungerlo a mano." Puramente informativo: mai una "domanda" Sì/No (nessuna azione automatica possibile su questo punto). Falso positivo esplicitamente escluso nel prompt: una condizione sulla SEDE ("se serve posso fare anche Meduno") è una normale disponibilità alternativa, non "ultima risorsa" — la condizione si applica solo quando il dubbio riguarda il LAVORARE quel turno, mai quale sede coprire. Nessuna copertura automatica in `test_email_generator.mjs` aggiunta per questa categoria (a differenza delle voci precedenti in questa lista) — solo modifica al prompt, verificata manualmente.
- **Gestione risposta troncata**: se la risposta dell'API si interrompe per limite di token (`data.stop_reason === "max_tokens"`) prima di completare il JSON, `JSON.parse` fallisce e NESSUNA azione di quel round è stata applicata. In questo caso l'app non mostra il fallback generico (testo grezzo): mostra un messaggio esplicito ("risposta troncata... nessuna modifica applicata") e forza comunque `azioniRestanti:true` (stato `troncato`), così il pulsante "Continua →" appare anche se il modello non ha potuto impostare `altreAzioniRestanti` da sé. In questo caso specifico il click NON invia `"continua"` (che presupporrebbe azioni già applicate da proseguire) ma rinvia la richiesta ORIGINALE dell'utente (salvata in `ultimaDomandaRef`, non aggiornata quando si invia un `testoForzato`) preceduta da un prefisso che spiega al modello che il round precedente va rifatto da capo, non proseguito — il prompt istruisce l'AI a ripeterla con massimo 2 azioni. Come rete di sicurezza aggiuntiva, anche quando il parsing riesce, `eTroncato` forza comunque `azioniRestanti:true` indipendentemente da cosa ha impostato il modello.
- **Cronologia mai troncata**: `chiediAI` invia SEMPRE l'intera conversazione (`aiMsgs`) all'API, non solo gli ultimi messaggi — il testo incollato dall'utente (email dei medici, disponibilità, ecc.) resta nel contesto per tutti i round successivi, anche su conversazioni lunghe con molti round.
- `chiediAI(testoForzato)` accetta un parametro opzionale: se assente usa `aiInput` (flusso normale, Invio/Invia), altrimenti invia direttamente il testo passato (usato dal pulsante "Continua").
- Errori HTTP dalla chiamata a `api.anthropic.com` (`!resp.ok`): mostrato in chat il messaggio completo restituito da Anthropic (`error.type` + `error.message`, più `request_id` se presente), non più un messaggio generico fisso.
- **Il JSON grezzo non deve MAI apparire in chat**: tre reti di sicurezza in `chiediAI`. (1) Se `JSON.parse(testo)` fallisce, prima di arrendersi si prova `estraiJsonBilanciato(testo)` — funzione dedicata (COMPONENTE, subito prima di `App()`) che cerca ogni "{" nel testo e ne segue la profondità delle graffe (rispettando le stringhe tra virgolette) fino alla sua chiusura, provando il parse di ogni candidato finché uno risulta un oggetto con un campo "tipo" valido. È più robusta della vecchia ricerca "prima { ultima }": quella si rompeva se il modello anteponeva un preambolo di testo contenente a sua volta graffe (es. notazione matematica come "h = (...) mod 7" nei calcoli del calendario perpetuo, §13) — caso reale che ha causato un leak di JSON grezzo in chat, corretto sia qui sia nel prompt (vedi sotto). (2) Se il parse riesce ma `obj.tipo` non corrisponde a nessun formato riconosciuto (es. "modifiche" con `azioni` vuoto/mancante) — prima il codice mostrava `testo`, cioè il JSON grezzo già validato: ora mostra `obj.spiegazione` o `obj.testo` se presenti, altrimenti un messaggio generico ("Non ho capito bene la richiesta, puoi riformulare?"). (3) Se anche l'estrazione fallisce, il testo residuo NON viene mai mostrato se contiene un solo carattere "{" in qualunque punto (non più solo se l'intero testo inizia e finisce con una graffa, controllo troppo permissivo che lasciava passare un misto testo-libero + JSON) — si sostituisce con "Non sono riuscito a interpretare la risposta, riprova."
- **Il prompt non deve mai istruire l'AI a "mostrare i passaggi" di un ragionamento**: la sezione `CALENDARIO PERPETUO` (§3.6/§13) diceva inizialmente di eseguire il calcolo di Zeller "mostrando i passaggi a te stesso nel ragionamento" — il modello ha preso questo alla lettera e ha scritto un preambolo visibile in chat (a volte letteralmente etichettato "Ragionamento interno (non mostrato all'utente)", pensando erroneamente che esistesse un canale nascosto), violando "RISPONDI SOLO con JSON, senza testo fuori dal JSON". Corretto specificando esplicitamente che il calcolo va fatto SOLO mentalmente, che non esiste alcun canale nascosto, e che qualunque testo scritto (anche etichettato come "interno") è comunque visibile all'utente; rinforzata la stessa istruzione anche nella riga "RISPONDI SOLO con un oggetto JSON valido" più in basso nel prompt.
- Le chiavi ortogonali `"SETT:"` e `"TURNOPREF:"` (§3.8, §3.9) sono escluse dallo stato `disponibilita` serializzato per l'AI (non hanno il formato di uno slotKey).
- `stato.medici` include anche `oreAssegnate`/`oreMancanti` per medico (stessi valori mostrati nel tab Medici — §6 punto 20): `null` se lo schema non è ancora elaborato; `oreMancanti` è `null` anche per i senza incarico (nessun monte ore).
- `stato.preferenzeTurno` elenca le preferenze di turno stesso giorno già dichiarate (§3.9), leggibile e impostabile dall'AI tramite l'azione `turno_pref` (valida solo sui giorni con sia diurno che notturno — l'AI segnala se richiesta su un giorno feriale semplice invece di impostarla).
- **Banner "Completato ✓"**: solo visivo, non cliccabile. Compare (`!proposta && completato`) subito dopo aver confermato (`applicaProposta`) un round che NON ha altre azioni in sospeso (`!azioniRestanti` al momento della conferma) — segnala che il giro di modifiche, anche multi-round, è terminato. Stato `completato`, azzerato a ogni nuovo invio come `azioniRestanti`/`troncato`; non si attiva con "Annulla" (`rifiutaProposta`), solo con una conferma effettiva.
- **Timeout lato client (55s, `AbortController`)**: il rilevamento del troncamento via `stop_reason` presuppone che la `fetch` restituisca comunque una risposta (valida o malformata). Nell'ambiente artifact di claude.ai, per risposte molto lunghe la `fetch` può restare bloccata senza mai risolversi né rifiutarsi — nessun errore, nessun dato: silenzio totale in chat, indistinguibile per l'utente da un'app bloccata. Per questo `chiediAI` avvia un `AbortController` con `setTimeout(..., 55000)` che interrompe la richiesta se non risponde in tempo; l'abort viene intercettato nel `catch` (`e.name === "AbortError"`) e trattato come il caso di risposta troncata (stesso messaggio esplicito, stesso stato `troncato`/`azioniRestanti`, stesso comportamento del pulsante "Continua →" che rinvia la richiesta originale invece di "continua").
- **Pannello AI più largo**: larghezza del riquadro laterale portata da 320 a 480px (era percepito troppo stretto per leggere risposte lunghe).

**Progetto Claude separato** — esiste un prompt di sistema separato (fuori da questa app) per processare email di disponibilità e produrre un file Excel. Non è nel file `.jsx`.

---

## 14. GITHUB PAGES (docs/)

`docs/` contiene una copia pubblicabile su GitHub Pages, poiché il progetto non usa bundler:
- `docs/index.html` — carica React 18, ReactDOM 18 e Babel standalone da `docs/vendor/` (vendorizzati localmente, nessuna dipendenza da CDN esterni), trasforma `docs/app.jsx` nel browser al volo
- `docs/app.jsx` — copia di `turni-guardia-medica.jsx` con 2 modifiche minime, non comportamentali, da riapplicare dopo ogni copia dal file root:
  1. `import { useState, ... } from "react"` → `const { useState, ... } = React;` (nessun bundler, React è un global)
  2. `export default function App()` → `function App()`, con `ReactDOM.createRoot(document.getElementById("root")).render(<App />);` aggiunto in fondo al file

  (Il file root non contiene più cast TypeScript orfani come `(e as any)` — rimosso anche lì dopo che un artifact fresco su claude.ai ha mostrato che poteva bloccare il rendering pure lì, non solo su Babel standalone.)

**Limiti su GitHub Pages** (non modificabili, solo da tenere presenti): l'assistente AI e lo storage persistente (`window.storage`) sono pensati per l'ambiente artifact di Claude.ai — su Pages falliscono silenziosamente (try/catch), quindi l'app funziona ma senza quelle due funzionalità.

Abilitazione: Settings → Pages → Deploy from a branch → branch del progetto, cartella `/docs` (passo manuale una tantum).

---

*Ultimo aggiornamento: luglio 2026. File app: turni-guardia-medica.jsx (~1646 righe).*
